# RoleFlow 当前项目交接

> 当前权威交接入口。更新于 2026-09-01。新会话先读根目录 `AGENTS.md`、本文件和 `NEXT_PHASE.md`，再检查当前代码与测试；不要用历史聊天、旧计划或历史发布说明推断当前状态。

## 1. 发布与候选状态

- 当前正式版本是 `v1.2.0`。精确发布身份以远程 `v1.2.0` 标签解引用后的提交、同名 GitHub Release 目标和该提交的 CI 为准；安装器完整性以 Release 同页的 `.sha256` 文件为准，不在这份会随提交变化的文档里硬编码自指 SHA。
- `codex/message-discovery-job-analysis` 基于 `86ea03b7c86e5070588eb61a615b3d6b41677650` 集成专用 Edge、消息发现和岗位分析改进，并按快进方式进入 `main`；发布过程不得强推或跳过精确 SHA 门禁。
- 合并前完整离线门禁：**已通过**。2026-08-25 在提交 `d60afe70b11707c0c99954e6c788c41107dcf36b` 上运行 `npm test`，退出码为 0，107/107 项通过，原始末行为 `All 107 offline checks passed.`。
- 消息发现功能候选：**已通过离线门禁**。2026-08-25 在提交 `ca581946a1211fb84824552d1847cf9db2dda27e` 上运行 `npm test`，退出码为 0，107/107 项通过，原始末行为同样是 `All 107 offline checks passed.`。
- 岗位分析与积分影子候选：**已通过离线门禁**。2026-08-25 对形成提交 `76a729cb0c00a17700093d9212f510a8ac76a664` 的代码运行 `node tests/run_all.js`，退出码为 0，108/108 项通过，原始末行为 `All 108 offline checks passed.`。
- v1.1.0 完整功能候选：**已通过离线门禁**。2026-08-26 在提交 `8b050b9d7abf702be02083aab29d35fa5fa13c89` 上运行 `npm test`，退出码为 0，108/108 项通过；发布时仍以 `v1.1.0` 标签所指精确提交的新鲜门禁为最终证据。
- v1.2.0 将消息学习与确认发送、策略轮次求职体检、整份定向简历、简历通用模拟面试、桌面工作台改版和消息岗位身份修复纳入同一正式版本。发布候选注册 131 项离线检查；最终证据以 `v1.2.0` 标签所指精确提交的发布工作流、安装资产和哈希为准。
- 2026-09-01 的首次安装验收发现两项运行问题：今日任务主卡在中等宽度或较长浏览器状态下可能被右栏挤成逐字换行，固定搜索页的一次 `Runtime.evaluate` 也发生过瞬时 15 秒超时。隔离修复提交 `d7e1d97` 为主卡设置可读列宽并在 1100px 下转为单列，同时仅对幂等的页面可见性读取增加一次瞬时错误重试；不重试导航、点击、填写或发送。
- 用户补充确认首次搜索体验：简历处理生成本次初始方案后，RoleFlow 从 A/B/C 优先级选择首个有效关键词，在后台给固定 BOSS 搜索页补上 `query`，保留城市、商圈等现有平台参数并清除旧页码。页面已有关键词、方案复用、标签页异常、登录失效或风控时不会覆盖或继续；失败不回滚画像、匹配卡和本地方案。实现提交 `355c548`，确认匹配卡后的页面会先引导用户在 BOSS 补充城市、地铁或商圈，再回来开始一轮岗位发现。
- 提交 `64e4283` 为已经完成首次简历处理的旧本地数据补上一次性续接：只选择最新完成、方案与画像版本一致、方案仍启用、且从未创建岗位发现 workflow 的 onboarding；已有搜索词或无有效关键词也记为已处理，不覆盖用户选择。浏览器、登录、风控或固定页异常不写完成标记，留待下次启动；同一进程不循环重试。完成标记复用 `events` 表且不保存简历、关键词或页面正文，没有数据库迁移或新依赖。该代码提交的新鲜完整 `npm test` 退出码为 0，132/132 项通过，末行为 `All 132 offline checks passed.`；测试没有访问真实 BOSS 或真实验收数据。
- 上述首次使用整改在实现提交 `355c548` 对应代码树运行新鲜完整 `npm test`，退出码为 0，132/132 项通过，原始末行为 `All 132 offline checks passed.`；使用 bundled Playwright 强制运行 `today_dashboard_smoke` 和 `dashboard_wave2_acceptance_smoke` 也均通过。验证只使用假浏览器、临时数据库、fixture 和本地 headless Edge，没有访问真实 BOSS、真实简历或正式安装数据，也没有填写、发送、投递或申请。
- 此前安装验收已完成本机安装、启动、专用 Edge 工作区自愈和真实 BOSS 只读扫描；该次验收没有执行真实沟通、投递或申请。2026-08-30 的后续真实全流程验收及外部写入范围以第 28 条为准。
- `codex/message-discovery-job-analysis` 已完成纯文字消息语义分流、岗位分析主视图、重试状态分离、真实积分影子报告，以及经真实 DOM 证据支持的简历请求/平台通知分流。位置确认与面试排期卡片没有结构证据，仍按未知内容安全停止。
- 当前分支在 `24c41b6` 之后已经完成消息学习第一阶段，并扩展为消息发现与确认发送闭环：已读/送达只计数，HR 新回复显示原文或安全卡片摘要、岗位分析和可编辑草稿；复制或标记已手动发送时直接学习，单条或批量确认会冻结当时目标与文字，再由固定后台消息页串行发送，不二次确认。该阶段开发取证期间只做过低频真实 BOSS 只读结构检查，没有执行真实填写、发送、简历同意/拒绝、投递或申请，也没有推送、合并、打包或 Release；2026-08-30 另有一次经用户单独授权的沟通批次，见第 28 条。
- 2026-08-28 对形成复审修复提交 `77bccaf` 的代码运行完整 `npm test`，退出码为 0，111/111 项通过，原始末行为 `All 111 offline checks passed.`；最终交付仍以最后提交上的再次验证为准。
- 当前分支已经按批准的新设计完成阶段二“策略轮次求职体检”：每条真实求职动作在 48 小时/周末顺延后成熟，并永久绑定发生时的策略轮次；默认 30/50/70 对应初步观察、可比较结论和正式诊断。达到 70 后仍留在本轮，只有招呼语、简历或求职策略真正变化时才开启新轮。
- 旧岗位和迟到结果始终更新旧轮；当前诊断只读当前轮。同方向的前后轮都达到各自 50 个成熟样本且历史边界可靠时才展示描述性变化；方向不同、历史边界不可靠或同时调整多项时明确停止归因。
- 2026-08-29 对阶段二用户流提交 `2d7708e` 运行新鲜完整 `npm test`，退出码为 0，130/130 项通过，原始末行为 `All 130 offline checks passed.`；7 项阶段二聚焦门禁全部通过。求职体检页面完成 1440px/390px 本地 headless Edge 检查，无横向溢出或控制台错误。
- 当前分支已经按批准的新设计完成阶段三“整份可编辑定向简历”：用户选择源简历和目标方向，系统自动选择 3–5 个代表岗位，生成完整可编辑全文与只读修改账本；用户全文自动保存，原简历和系统生成基线不变。启用当前全文会原子创建新简历版本与新的 `resume` 策略轮次。
- 2026-08-29 对阶段三原子启用提交 `eae13f4` 运行新鲜完整 `npm test`，退出码为 0，130/130 项通过，原始末行为 `All 130 offline checks passed.`；8 项阶段三聚焦门禁全部通过。定向简历页面完成 1440px/390px 本地 headless Edge 检查，无横向溢出、控制台错误或不可编辑问题。
- 当前分支已经按批准的新设计完成阶段四“简历通用模拟面试”：默认不要求岗位或邀约，只使用冻结简历、全局/经历范围的有效回答与事实以及既往通用训练弱点；每道新问题必须显示并引用真实简历证据，追问还必须逐字承接上一回答片段。旧岗位上下文模式保留为可选“岗位专项面试”，才会使用完整 JD 与适用的岗位/公司口径。
- 2026-08-29 对阶段四界面提交 `da3e33d` 之后的代码运行 7 项聚焦门禁和新鲜完整 `npm test`，均通过；完整门禁退出码为 0，130/130 项通过，原始末行为 `All 130 offline checks passed.`。模拟面试页完成 1440px/390px 本地 headless Edge 检查，默认/专项切换、岗位必填联动和简历证据显示正常，无横向溢出或控制台错误。
- 2026-08-29 又从用户视角完整重走消息、求职体检、定向简历和模拟面试，修复了已保存消息草稿刷新回退、异步失败导致 Dashboard 退出、未提交面试回答丢失、同页成功操作只改地址但不刷新内容，以及结果区不在当前视口等问题。对提交 `61c5520` 对应代码树运行新鲜完整 `npm test`，退出码为 0，130/130 项通过，原始末行为 `All 130 offline checks passed.`；本地假数据浏览器验收覆盖 1440×980、375×812 和面试 844×390 横屏，无横向溢出、控制台错误、页面异常或过小移动端操作控件。
- 2026-08-29 用本机已有的一份真实简历开始阶段四首轮本地验收。正常数据库打开路径已将本机 schema v16 迁移到 v25，迁移前自动备份到 `C:\Users\Administrator\AppData\Local\RoleFlow\Data\data\backups\jobs-before-v16-to-v25-2026-08-29T14-47-25-233Z-9576.sqlite`；`quick_check` 为 `ok`，简历文档、启用版本和正文长度均未改变。首个生成问题把“参与”夸大成“负责”，提交 `af39901` 随后同时收紧模型指令和保存前契约。重新生成的第二个会话保持“参与”边界，再中性追问实际承担的工作；页面无横向溢出、控制台错误或页面异常，答案框为空。4 项面试聚焦检查和新鲜完整 `npm test` 均通过，完整门禁退出码为 0，130/130 项通过，末行为 `All 130 offline checks passed.`。没有访问真实 BOSS，也没有把真实简历正文写入仓库、测试或文档；第二个会话等待用户本人回答，尚不能代表整轮问题质量或面试效果。
- 2026-08-30 完成 Dashboard 桌面端整体改造：共享外壳改为按“工作台 / 求职 / 沟通 / 成长”分组的侧栏；首页突出每天可重复执行的岗位发现与跨天反馈；岗位页结果优先；消息页改为左侧消息列表、右侧单条回复工作区；求职体检先显示当前结论和 30/50/70 证据进度；简历页以整份编辑器为主、修改依据为辅；面试页以当前问题为主。辅助页沿用同一视觉系统。没有新增前端框架、组件库、运行时依赖、数据库迁移或业务接口，也没有改变 48 小时/周末顺延、策略轮次、消息学习和外部动作边界。
- 桌面改造代码提交 `cd933706e139ed08d8184108bd7000c14e3f434c` 已通过严格本地离线浏览器验收：15 个页面状态 × 4 个视口，共 60/60；横向溢出、控制台错误、页面异常、请求失败、外部请求、无标签控件和标题层级错误均为 0。清单位于 `D:\DevData\RoleFlow-ui-redesign-acceptance\desktop-redesign-cd93370.json`。同一精确提交的新鲜 `npm test` 退出码为 0，131/131 项通过，原始末行为 `All 131 offline checks passed.`。验收只使用临时 SQLite、离线 fixture 和假浏览器；没有访问真实 BOSS、真实简历或外部服务。
- 2026-08-30 用户明确授权并完成一轮真实全流程验收：工作流 `3ad4bf20-4f3c-4248-88e0-7e0acf3f1fc9` 收集 69 个去重岗位、分析 57 个、选出 17 个；沟通批次 #3 串行 17/17 成功。首轮只读消息发现读取 40 条可见会话，形成 2 个项目内处理结果和 9 个未解决项；迁移与上下文修复后只保留 3 条城市身份不一致。最终只读恢复复验处理 8 组消息：6 组生成 12 份可编辑草稿，2 组因确实缺少候选人事实不生成猜测回复。除用户授权的批次 #3 外，没有发送消息回复、同意/拒绝简历、投递或申请。
- 同一验收修复了专用 Edge 后台/最小化时的消息读取：固定消息页与唯一临时详情页使用 CDP 恢复页面生命周期，但不恢复窗口或聚焦标签；临时详情关闭后只等待浏览器目标清单稳定，再核对精确基线，不重开、不重复访问。真实复验已跨过原关闭后失败点。完整证据及剩余问题见 `docs/superpowers/reports/2026-08-30-real-user-e2e-acceptance.md`。
- 真实验收后续本地收口提交为 `66d89d4`：迁移 26 只清理仍有 `hard_boundary/blocked` 证据的历史误分类，新任务也先认定确定性排除再判断详情不足；安全停止信号贯穿消息岗位上下文、岗位分析、模型请求与重试等待，取消后不写缓存或完成结果；仅开启思考模式的三类岗位深度结构化任务把输出/超时预算提高到首轮 8192/240 秒、截断重试 16384/300 秒，普通任务和非思考批量分析维持原预算。
- 在真实运行库的一致副本上执行迁移 26，69 个任务中的 `DETAIL_REQUIRED` 从 12 个变为 4 个，8 个已有排除证据的误分类全部清除；原运行库没有在验证中修改。形成 `66d89d4` 的代码树运行完整 `npm test`，退出码为 0，131/131 项通过，末行为 `All 131 offline checks passed.`。本轮收口没有访问真实 BOSS、调用真实模型或产生外部写。
- 随后已用 `-NoOpen` 重启正式本地 Dashboard，让正式运行库从 schema 25 迁移到 26；自动备份为 `C:\Users\Administrator\AppData\Local\RoleFlow\Data\data\backups\jobs-before-v25-to-v26-2026-08-30T11-31-26-488Z-3680.sqlite`，另有包含主库/WAL/SHM 的手工备份 `D:\DevData\RoleFlow-verification\pre-v26-original-20260830-193116`。迁移后 `quick_check=ok`，69 个任务为待补 4、误分类 0。真实本地工作流页显示已读 57、待补 4、无需详情 8；分析完成 69、当前未解决 0、本地规则处理 8。1440px 验收无横向溢出、控制台错误、页面错误、失败请求或外部请求，截图和 JSON 位于 `D:\DevData\RoleFlow-verification\v26-workflow-*`。
- 重启时重新检查出专用 Edge 中预先存在两个沟通页，旧 Dashboard 的内存快照此前未反映该漂移。只读确认两页同窗、均在后台且无未提交输入后，关闭了没有编辑器的空重复页；没有激活窗口、读取聊天正文、填写或发送。重新整理后 Dashboard、浏览器、工作区和只读就绪检查均为 `ready`，基线恢复为一个 Dashboard、一个 `BOSS-SEARCH`、一个 `BOSS-COMMUNICATION`。
- 最终消息复验又确认三处根因：旧 Edge 页面保留陈旧 `window.__bossMessageSnapshot`，队列规划遗漏 `friendKey`，真实模型把缺失事实写成 `{key,reason}` 并把草稿误写成 `responseItems(kind=draft)`。提交 `b8e6ccc` 让每次快照重装 helper、完整传递会话身份，并用提示词和运行时校验共同固定模型结构；严格身份守卫与事实契约均未放宽。整库备份后只撤销本次失败的 25 条分类事件和 8 个预览基线，再次运行得到 8 个结果、12 份开放草稿、2 个缺失事实提示、3 个城市不匹配待解决、0 发送批次，`quick_check=ok`。真实页面显示 12 个可编辑并已自动保存的草稿和批量发送面板；完整证据见验收报告和 `D:\DevData\RoleFlow-verification\readonly-message-discovery-contract-recovery-evidence.json`。
- 2026-08-31 修复消息会话与本地岗位的确定性关联：提交 `4c5f3f1` 让“广州”与“广州·区·商圈”按基础城市一致，并依次利用规范 BOSS 岗位编号、既有会话绑定、岗位名称、公司、薪资和城市缩小同名候选；真实字段或会话冲突仍保持未解决。提交 `c08918d` 让消息详情把裸岗位编号只在本地边界规范为 `boss:<jobId>`，不再为扫描已有岗位创建第二条裸编号记录。历史重复岗位、会话、草稿和事件没有自动合并或删除，页面仍显示真实逐会话待办数。
- 对提交 `c08918d` 对应代码树运行 `message_discovery_smoke`、`message_discovery_job_context_smoke`、`candidate_progress_storage_smoke`、`dashboard_message_discovery_smoke`，4/4 通过；新鲜完整 `npm test` 退出码为 0，131/131 项通过，原始末行为 `All 131 offline checks passed.`。本次实现与验证只使用 fixture 和临时数据库；没有读取、填写或发送真实 BOSS 消息，没有执行简历同意/拒绝、投递或申请。正式数据库仅在诊断阶段做过只读查询，没有改写其中现存的 3 条未解决会话或重复岗位。

## 2. 当前浏览器交付模型

- 默认普通路径是“RoleFlow 专用 Edge（推荐）”。`Start.bat` 和工作区启动默认使用内部 `portable` authority，但普通用户不需要安装 Edge Control，也不需要配置 CDP 或 `9222`。
- 第一次登录保存在 `%LOCALAPPDATA%\RoleFlow\BrowserProfile`。数据库、简历、模型设置、日志和报告保存在 `%LOCALAPPDATA%\RoleFlow\Data`。两者都独立于程序安装目录，覆盖升级后继续复用。
- 上述路径只保证同一 Windows 用户下的本机升级稳定，不代表跨机器自动同步或迁移。
- “使用当前 Edge（高级，需要浏览器连接组件）”必须显式运行 `Start.bat -BrowserMode edge`，并要求现有 Edge Control 扩展与桥接健康。“RoleFlow 专用 Edge（推荐）”失败时绝不自动回退、自动下载组件或切换 authority。
- Edge Control 不进入普通安装包或绿色包。
- 专用 Edge 已原生提供沟通结果所需的受限网络观察能力。它只观察两个允许的 BOSS 沟通端点，只在内存保留有界响应，不采集请求头、Cookie、请求正文或聊天内容；Edge Control 仍只是显式高级兼容路径。

## 3. 浏览器安全边界

- 静止基线是同一 Edge 窗口内一个 `BOSS-SEARCH` 和一个 `BOSS-COMMUNICATION` 固定页；每次浏览器运行前重新解析当前绑定的数值 tab ID。
- 用户启动工作区且未传 `-NoOpen` 时，`workspace-tabs` 可在就绪检查后调用一次 `Page.bringToFront`，就绪时引导到 Dashboard，未登录时引导到 BOSS。
- 上述启动引导是唯一前台例外。扫描、JD 读取、分析、消息发现、沟通、轮询、重试和恢复期间禁止激活 BOSS 页、切换窗口或恢复前台焦点。
- 消息发现先复用完整可信的本地 JD。只有新会话缺少完整 JD 时，才允许增加一个同窗口、`active: false`、串行且受共享节奏保护的后台临时详情页；核验、保存、checkpoint 后关闭并恢复两页基线。
- 专用 Edge 被最小化或完全遮挡时允许零个可见页面。读取固定消息页或已经证明安全的临时详情页前，可以调用 `Page.setWebLifecycleState(active)` 恢复页面计时器与异步回调；该调用不是前台激活，之后仍必须重新核对固定页、窗口、可见集合和目标身份。
- 当前岗位详情产品主线只用 `trusted_pane`。`search_page_api` 保留研究证据但不修、不验、不启用；`message_discovery_detail` 是唯一允许的独立详情例外。
- 登录、风控、页面丢失、标签身份漂移、目标不一致或结果不明确时立即停止，不自动重试。
- BOSS 默认只读。沟通、发送或申请等外部写动作仍需要针对不可变批次的明确授权；历史接受状态不是新批次授权。

## 4. 浏览器 profile 迁移与卸载

- 旧安装目录的 `.runtime\edge-profile` 不会自动迁移。迁移必须显式传入源目录并加 `-ConfirmMigration`。
- `scripts\migrate-browser-profile.ps1` 只复制、核对并写入稳定目录，不移动或删除源目录；目标已存在、路径重叠/不安全、身份不完整或 Edge 正在占用时停止。
- 普通交互卸载和静默卸载都保留 `%LOCALAPPDATA%\RoleFlow\BrowserProfile`。
- 删除浏览器登录资料需要单独确认；删除安装目录内数据库等数据的确认不能替代 profile 删除授权。

## 5. 产品与架构边界

- 继续使用渐进式模块化单体，不做大爆炸拆分、微服务、ORM、依赖注入容器或前端框架迁移。
- 不降低 JD 覆盖、召回或匹配质量。可能降低质量的方案必须先量化并由用户决定。
- 完整 JD 读取后先执行岗位资格闸门。明确实习、毕业届别不符或仅限在校生的岗位不会被模型提升，也不能进入沟通清单；弱提示和资料不足分别保留为可评估或待确认，不机械误杀。
- 筛选规则实质变化后必须使用全新空 operational baseline；失败或中断数据库不参与质量评测。
- 不启动 Wave 5，不修当前产品路径不可触发的理论问题。
- 测试使用保存 fixture、注入式进程/HTTP 探针和临时数据库，不访问 BOSS，也不再创建假的 `msedge.exe`。

## 6. 当前验证与交接协议

- 危险夹具扫描、`npm test`、installer `StageOnly`、`git diff --check` 和 `git status --short` 均为当前候选的必做离线门禁。
- 本轮实现提交依次为 `34fdea0`（专用 Edge 工作区自愈）、`4961db2`（JD 后岗位资格闸门）、`995ad3e`（专用 Edge 沟通验证能力）、`e1396a1`（最小化窗口自愈与升级互斥）和 `508781d`（网页兼容字形下的实习证据识别）。
- 消息发现实现提交依次为 `157ed8f`（在现有一次语义模型调用中拆分沟通意图与消息话题）、`50e094e`（把安全意图枚举贯穿结果与进度事件）、`ca58194`（页面按结论、岗位机会、草稿的顺序展示）、`d1d6cbf`（按真实结构识别竞争卡和简历请求卡）、`8f92287`（读取器有限白名单）、`8468b6c`（文字/平台/人工操作混合分流）和 `78bcdab`（页面同时显示人工操作和本地草稿）。没有新增模型调用、模型任务档位、依赖或数据库迁移。
- 消息回复学习实现提交为 `a5031ff`（草稿、回答记忆和事实修订存储）、`475f609`（复制/已发送后的本地学习服务）、`37c766e`（事实提取与启用回答复用）、`bb3905b`（发现结果持久化和重启恢复）、`d2f91e4`（可编辑草稿与“我的沟通资料”）、`a80a221`（串行化自动保存与完成，防止迟到保存覆盖已发送文字）和 `77bccaf`（收口安全摘要、否定事实、作用域、撤回、竞态、单一路径、事务回滚与事实权威顺序）。使用现有 SQLite、模型适配器和 Dashboard，没有新增依赖或浏览器写入；数据库迁移版本为 17。
- 消息确认发送实现提交为 `2721bf8`（可见会话分类与已读/送达计数）、`c9790fc`（迁移 22 与不可变授权批次）、`998d4dc`（HR 内容和岗位上下文展示）、`8a8a9b9`（确认服务）、`e9baa48`（后台守卫发送器）、`9c909b9`（串行执行器）、`01ef19d`（消息卡片单条/批量交互）、`2ffc87e`（同会话唯一选择、重启调和和清理归属）及 `15821f5`（写入前与回读时再次核对目标）。继续复用现有 SQLite、Dashboard、固定消息页、访问额度和随机节奏，没有新增依赖、模型调用、后台调度器或前端框架；当前数据库迁移版本为 22。
- 求职体检历史实现提交为 `2826780`、`c3fd183`、`f846c83`、`45e43fe`、`f3b395b`、`a2724e6`、`5be3160`、`7306c03`、`465f407` 和 `7510ab5`。本次策略轮次重构提交为 `efa2c53`（迁移 23、轮次存储和旧数据回填）、`874a8fc`（新动作不可变绑定）、`ee0d12a`（当前轮诊断与兼容前后轮比较）和 `2d7708e`（本地手动边界及新页面）。继续复用 SQLite、进度事件和服务端 Dashboard，没有新增依赖、模型调用、后台队列或浏览器写入。
- 定向简历历史实现提交为 `2bbced4`、`c24dd71`、`411ef06`、`d6c4394`、`b9609f2` 和 `1fbf5fd`。本次整份草稿重构提交为 `2b5c3fc`（六类修改原则与自动应用契约）、`5088126`（迁移 24、生成基线/用户全文分离与旧记录兼容）、`0540f9f`（代表岗位选择与完整草稿生成）、`3a44e36`（全文编辑器与串行自动保存）和 `eae13f4`（新简历版本与新策略轮次原子启用）。继续复用候选人事实、回答记忆、完整岗位 JD、现有模型适配器、SQLite 和服务端 Dashboard，没有新增依赖、外部服务或浏览器写入；当前数据库迁移版本为 24。
- 模拟面试历史实现提交为 `6ce3f35`、`c9943a0`、`bd633fd`、`899b929`、`24320d3`、`6aedc8b`、`b9f5557` 和 `b813b7a`。本次简历通用重构提交为 `81e277e`（迁移 25、会话类型和可空岗位）、`11968aa`（逐行简历证据与问题引用契约）、`a001718`（通用/专项范围隔离、跨方案通用弱点和旧会话内存兼容）及 `da3e33d`（默认通用界面、专项切换和证据展示）；真实简历首题复验后的 `af39901` 进一步阻止问题夸大简历职责强度。同模型配置下继续复用一个服务实例，重复或并发完成不会重复生成复盘；没有新增依赖、模型任务档位、外部题库、公司研究、语音服务或浏览器访问，当前数据库迁移版本为 25。
- 岗位分析实现提交依次为 `8900ee4`（同一方案下用最新岗位结果区分历史失败与当前未解决）、`de4c102`（流程页显示直接完成、失败后已解决和当前未解决）、`5138a36`（保留失败尝试但防止流程体检误报）、`b7ead39`（岗位主卡按结论、岗位、公司机会、匹配、薪资和风险展示）、`c88564c`（独立连续积分与安全保护）和 `76a729c`（二维表与积分离线比较报告）。没有数据库迁移、新依赖或生产判定切换。
- v1.1.0 预发布构建位于 `D:\DevData\RoleFlow-installer\release-8b050b9d7abf\output\RoleFlow-Setup-1.1.0.exe`，大小 39,891,075 字节，SHA-256 为 `fea7bbef5bb7dcc92812c19bf54b3a0d89743d64a2763583eaa440fd425e338e`；对应 `StageOnly` 有 3,144 个条目、2,586 个文件、175,268,212 字节，禁入项和伪造 Edge 创建模式均为 0。正式 Release 必须使用最终标签提交重新构建的同名资产，不能上传这个预发布证明包。
- v1.1.0 消息页已在本地假浏览器中模拟人工验收：人工简历操作位于草稿之前、固定安全文案生效、没有同意/拒绝按钮、草稿复制成功、外部网络请求为 0；截图保存在 `D:\DevData\RoleFlow-acceptance\v1.1.0-message-discovery.png`。本轮没有覆盖安装到真实 RoleFlow 安装目录，也没有启动安装后的真实 BOSS 工作区；此前 v1.0.0 的覆盖升级证据不能冒充 v1.1.0 安装验收。
- 最新真实只读基线运行 `0b747bea-3332-41f0-ad3d-36fa257cc3c4`：3/3 搜索目标完成，35 个岗位入库，所需 JD 28/28，35/35 分析到达终态；其中 26 次首轮模型分析成功、7 个硬规则/详情前置项跳过、2 个模型结构错误经本地批量重试后解决。最终队列为主投 6、可投 5、慎投 13、不推荐 10、另有 1 个活跃状态待核验、待语义分析 0。
- 在运行 `0b747bea-3332-41f0-ad3d-36fa257cc3c4` 的真实样本中，标题未写实习、日薪且 JD 使用兼容字形 `⽉` 的岗位已由规则闸门判为 `internship_role` / 不建议，未进入确认清单；该次基线结束时沟通批次和沟通项均为 0，且未出现 `BROWSER_TIMEOUT`、风险控制或页面丢失。2026-08-30 后续批次 #3 的结果见第 28 条。
- 两次质量基线前均将旧数据库归档到 `D:\DevData\RoleFlow-gate-d\archive`，只清空 operational 表，画像、简历、筛选方案、匹配卡和模型设置均保留。
- 岗位分析的重试状态问题已收敛：原始工作流任务和 `job_analysis_attempts` 继续保留历史失败；同一方案下最新分析完整时显示“首次失败，后续已解决”，不再计入当前未解决，也不会触发 `analysis_incomplete`。真实未完成或无法安全对应的岗位仍保持未解决。
- 真实积分影子已在冻结 Gate D 样本上运行，报告位于 `D:\DevData\RoleFlow-gate-d\comparisons\matrix-vs-scalar-2026-08-25.json`，SHA-256 为 `85abcf08c0c113f9f00334910618952e3a3e7dc7b8d0a93e9be297369d13d4f6`。51 条原始样本中 43 条可比较；二维表/积分一致 38 条、差异 5 条，全部是积分降级：4 条主投降为可投，1 条可投因缺少明确核心要求降为慎投；硬边界逃逸 0。二维表分布为 19/6/15/3，积分分布为 15/9/16/3（主投/可投/慎投/不推荐）。
- 当前 Gate D 人工确认标签为 0，同岗位重复模型组为 0，因此不能判断哪条路线更准确或更稳定。生产继续沿用二维表的决策已经冻结，“是否切换积分”不再是近期开发入口；现有报告只保留为历史比较证据。
- 消息发现现在把“正式面试邀约、询问意向、索要信息、补充信息、普通沟通、人工判断”与“项目、资格、薪资、到岗等话题”分开；只有明确邀约会推进到面试阶段。HR 新回复显示原文，已验证的简历请求显示固定摘要；每张卡片依次显示岗位理解、既有分析结论和本地回复草稿。已读与送达只保留数量和漏斗证据，不逐条展示。
- 回复草稿现在会在本机持久化并于输入停止约 600 毫秒后保存。复制或“已手动发送”是完成学习的明确动作；点击单条或批量确认则把当前目标、草稿修订和文字冻结为本次发送授权，成功后以同一文字完成学习和进度记录。未修改草稿不会变成权威事实，用户修改后的完整回答即使事实提取失败也会保留；只有仍启用的用户修改回答和当前事实会进入后续生成。“我的沟通资料”支持修改、删除、停用和查看事实修订历史。
- 学习记录不保存招聘方原始消息正文；只保存安全问题摘要、意图/话题、草稿及用户最终回答。公开消息发现状态仍不返回草稿或回答正文。
- “求职体检”只从本地权威事件读取：已投、已验证发起沟通或确认已发送回复才入池；扫描、查看、分析、推荐和仅收到入站消息都不入池。每条至少等待 48 小时，周末截止顺延到周一；新的已读会为该消息状态重新开始 48 小时。等待、未知、用户关闭/拒绝不计失败，用户纠正覆盖当前投影但保留历史事件。
- 默认 30/50/70 分别对应初步观察、可比较结论和正式诊断，可按求职强度保存为严格递增的其他值。每轮阈值在轮次开始时冻结；达到 70 后本轮继续积累，直到用户完成招呼语、定向简历或求职策略调整。当前诊断只读当前轮；旧岗位和迟到反馈不会移动。前后轮比较要求方向一致、边界可靠且双方都达到各自可比较门槛，多项同时调整不做单项归因。
- 阶段二本轮没有访问真实 BOSS，也没有使用真实用户求职数据。验证使用保存 fixture、临时 SQLite、真实本地 HTTP 服务和本地 headless Edge；1440px 与 390px 视觉检查无横向溢出、控制台错误为 0，截图位于 `D:\DevData\roleflow-ui-check`。真实数据分布、长期阈值效果和策略调整后的自然反馈仍未验证。
- 阶段三新实现的证据来自 Mock/脚本模型、保存 fixture、临时 SQLite、真实本地 HTTP 服务和本地 Edge 页面；没有访问真实 BOSS、用户真实简历或招聘平台写入。它证明本地流程和原子边界通过，不证明真实求职效果已经提升。
- 阶段四原始离线门禁使用 Mock/脚本模型、保存 fixture、临时 SQLite、真实本地 HTTP 服务和本机 headless Edge；随后又用本机已有真实简历验证了数据库迁移、首题生成和页面展示，并据此修复一次职责夸大。没有访问真实 BOSS，也没有把真实简历正文复制到测试或文档。当前证据只覆盖真实简历首题，不证明完整追问、复盘、重练质量或真实面试效果。公司/行业研究、外部题库、语音和任何真实平台访问均未实现。
- 2026-08-25 至 2026-08-26 对固定 `BOSS-COMMUNICATION` 页做过一次低频只读 DOM 取证：当前会话中同时存在平台竞争卡、询问英语/粤语的纯文字和附件简历请求卡。简历卡已核对 `.message-card-wrap.boss-green`、`.dialog-icon.resume`、标题区、按钮区及“拒绝/同意”动作；竞争卡已核对蓝色卡、竞争标题和单个“查看详细分析”动作。没有点击卡片或动作、没有填写/发送、没有投递/申请。后台截图接口超时且 Windows 捕获未产生文件，因此真实视觉截图仍未验收；实现回归使用按真实 DOM 缩减的 fixture。
- 2026-08-29 使用用户现有登录会话进行了新的低频只读 DOM 检查：当时 29 条可见会话中，12 条最后消息来自 HR、9 条我方消息显示已读、8 条显示送达；对两条 HR 回复会话只读选择并复核了会话、岗位和最新消息身份。已确认列表身份字段、详情消息 ID、`#chat-input` 编辑器和 `.btn-send` 按钮结构；没有填写编辑器、点击发送、接受或拒绝简历、投递或申请。选择器仍必须在每次真实动作前重新核对，当前离线通过不能替代一次用户明确授权的真实单条发送验收。
- 本次岗位分析与实现验证使用 Mock、保存 fixture、假浏览器、临时数据库和冻结 Gate D 文件；没有调用真实消息语义模型。真实消息语义准确率、位置/面试卡片结构、重复模型稳定性和真实卡片点击结果仍未验证。
- 交付必须列出修改文件、验证命令与结果、未验证前提、真实平台读写情况、stage 路径和提交哈希。
- 消息学习与确认发送、阶段二策略轮次求职体检、阶段三整份定向简历、阶段四简历通用模拟面试，以及本轮真实验收暴露的误分类、停止延迟、长输出超时、陈旧页面 helper、队列身份丢失和模型回复结构漂移均已完成实现与验证。下一步按真实使用反馈推进：用户可先检查当前 12 份真实草稿；只有针对当次不可变草稿重新授权后才验收真实回复发送。真实简历通用训练仍由用户本人回答后再判断整轮质量；位置确认和面试排期仍需新 DOM 证据。后续推送、合并、打包、版本变更和 Release 以执行当时的明确范围为准；历史批次 #3 授权不能复用。

## 7. 事实优先级

1. 当前代码、测试和本次实际运行证据；
2. `src/core/product_policy.js` 等当前策略源码；
3. `AGENTS.md`、本文件、`NEXT_PHASE.md` 和当前产品文档；
4. 历史发布说明、历史验收、spec、plan、报告和聊天记录。
