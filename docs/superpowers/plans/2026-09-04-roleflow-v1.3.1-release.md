# RoleFlow v1.3.1 发布与新用户模拟验收 Implementation Plan

> **For agentic workers:** Use executing-plans. Delegate only a bounded read-only release review.

**Goal:** 先盘点并按确认清理旧暂存，再推送公开 v1.3.1，下载公开资产模拟新用户流程。

**Architecture:** 复用现有隔离工作树、发布脚本和标签触发的 GitHub Actions。只更新版本/发布文档，不重新实现已验收功能；新用户检查使用独立 D: 数据、示例简历与本地模拟页面，不接管用户真实工作区。

**Tech Stack:** Node 22.23.1、PowerShell、Inno Setup、Git、现有 Dashboard 与测试设施。

## 约束

- 本轮用户已授权推送、发布及模拟新用户验收。真实 BOSS 沟通、发送或投递不在授权内。
- 复用 `D:\DevData\RoleFlow-worktrees\acceptance-fixes-20260904`，发布分支 `codex/release-v1.3.1`；基线 main `7ef1ccf`。
- 构建、公开下载和验收数据放 `D:\DevData\RoleFlow-release-v1.3.1`；缓存继续 D:，不复制新的开发环境。
- 删除仅限用户确认清单中的无用户数据旧 stage 目录；保留源码、所有工作树、当前安装版、安装器、验证记录、数据库、登录资料和备份。
- 不强推、不移动旧标签、不替换公开 v1.3.0 资产。

## Task 1：存储与候选

- [x] 检查磁盘、当前进程、Git 与远端。D: 初始剩余 15.80 GiB；22 个旧 stage 候选约 3.60 GiB，完整路径保存于 verification/cleanup-approved-candidates.json（文件名不代表用户已批准）。
- [x] 用户明确批准后重新核验并删除 22 个旧 stage，移除 3,863,194,687 字节；删除后 D: 可用约 19.19 GiB。精确记录见 `verification/cleanup-result.json`，其他资料保留。
- [x] 确认最新公开 v1.3.0，远端不存在 v1.3.1；package/lockfile 改 1.3.1，新增发布说明，修正安装版入门步骤和数据目录说明。
- [x] 独立只读复审 Critical 0 / Important 0，两项文档措辞已修正；四项关键检查通过，候选提交 `da3513cdee2f582c4ea21234f5a56e31d205db3b`。

## Task 2：精确候选与发布

- [x] 候选 SHA 上严格本地 Playwright，发布打包入口完整门禁 **143/143 通过，退出 0**；便携包生成成功。
- [x] 同 SHA 构建安装器；2,621 个暂存文件逐一哈希一致、禁入项 0，sidecar 一致，便携包必需入口齐全，独立安装环境自检 `SELF_CHECK_OK`。
- [x] main 快进至候选并推送、远端一致；注释标签 v1.3.1 已推送并解引用至该 SHA。
- [x] 标签工作流 `33841857321` 成功，公开 Release 非草稿、非预发布、三个资产齐全；全部下载并核对公开摘要及安装器 sidecar，分别保存本地/公开哈希。

## Task 3：发布后模拟验收

- [x] 公开便携包和独立 D: 数据环境自检 `SELF_CHECK_OK`；不覆盖用户安装注册或 8787 服务。
- [x] 用户页面验证空白入门、公开包示例简历上传/处理、匹配偏好卡确认、方案编辑和默认兼职/薪资；主动设置、键盘清空及重启保留通过。曾因自动化 fill 空字符串未实际清空而误报，已由提交前字段/数据库/重启证据纠正，未改产品代码。
- [x] 公开包另跑 12/12 项离线检查，通过筛选/分析协调/清单/假浏览器沟通/消息发现/学习/恢复。另在用户页新库导入 2 个 CLI 样例并查看分组，不把规则检查冒称真实语义分析或纯 UI 全链路。
- [x] 已停止本轮测试进程、关闭临时页；原有 PID 10008 服务保留。最新公开包、隔离验收数据及日志/哈希保留，未扩大 22 个旧 stage 的删除范围。
- [x] README、交接、下一入口和验收回执已更新，独立文档复审 Critical 0 / Important 0 / 新增 Minor 0；随本收口提交推送 main，发布标签保持不动。回执明确释放空间、发布目标、模拟覆盖与未验证项。

完整回执：`docs/superpowers/reports/2026-09-04-v1.3.1-release-acceptance.md`。下一入口仅为两项非阻塞提示改进；真实平台与全新 Windows 安装向导未在本轮重验。
