# RoleFlow v1.3.1 发布与新用户模拟验收 Implementation Plan

> **For agentic workers:** Use executing-plans. Delegate only a bounded read-only release review.

**Goal:** 先盘点并按确认清理旧暂存，再推送公开 v1.3.1，下载公开资产模拟新用户流程。

**Architecture:** 复用现有隔离工作树、发布脚本和标签触发的 GitHub Actions。只更新版本/发布文档，不重新实现已验收功能；新用户检查使用独立 D: 数据、示例简历与本地模拟页面，不接管用户真实工作区。

**Tech Stack:** Node 22.23.1、PowerShell、Inno Setup、Git、现有 Dashboard 与测试设施。

## 约束

- 本轮用户已授权推送、发布及模拟新用户验收。真实 BOSS 沟通、发送或投递不在授权内。
- 复用 `D:\DevData\RoleFlow-worktrees\acceptance-fixes-20260904`，发布分支 `codex/release-v1.3.1`；基线 main `7ef1ccf`。
- 构建、公开下载和验收数据放 `D:\DevData\RoleFlow-release-v1.3.1`；缓存继续 D:，不复制新的开发环境。
- 删除仅限用户确认清单中的无用户数据旧 stage 目录；保留源码、所有工作树、当前安装版、安装器、验证记录、数据库、登录资料和备份。
- 不强推、不移动旧标签、不替换公开 v1.3.0 资产。

## Task 1：存储与候选

- [x] 检查磁盘、当前进程、Git 与远端。D: 初始剩余 15.80 GiB；22 个旧 stage 候选约 3.60 GiB，完整路径保存于 verification/cleanup-approved-candidates.json（文件名不代表用户已批准）。
- [ ] 收到用户清理选择后，重新验证精确路径、重解析点、私有文件及进程占用，执行限定清理并记录前后空间；不批准则保留且继续发布。
- [x] 确认最新公开 v1.3.0，远端不存在 v1.3.1；package/lockfile 改 1.3.1，新增发布说明，修正安装版入门步骤和数据目录说明。
- [ ] 独立只读发布复审、关键检查、提交候选。

## Task 2：精确候选与发布

- [ ] 候选 SHA 上设置严格本地 Playwright，运行 `scripts/package-release.ps1 -IncludePortableNode -OutputName RoleFlow-v1.3.1-portable.zip` 的完整内置门禁，记录真实总数与退出码。
- [ ] 同 SHA 运行 `scripts/build-installer.ps1 -SkipTests -PortableNodeRoot D:\hermes\node -BuildRoot D:\DevData\RoleFlow-release-v1.3.1\installer -OutputDir D:\DevData\RoleFlow-release-v1.3.1\dist`；核对暂存禁入、安装器与 sidecar、便携包必需入口。
- [ ] 快进 main 到已验证候选，推送并核对远端一致；创建指向该提交的 v1.3.1 注释标签并推送。
- [ ] 等待标签工作流成功；核对公开 Release 非草稿、非预发布、三个资产齐全；下载公开安装器/校验文件与便携包，分别保存公开哈希，不能冒充本地证明包字节相同。

## Task 3：发布后模拟验收

- [ ] 使用公开包和独立空白 D: 数据做环境自检；不覆盖用户安装注册或 8787 服务。
- [ ] 从用户页面验证空白初始状态、示例简历上传和处理、画像确认、方案编辑及默认兼职/薪资；记录每步结果与页面证据。
- [ ] 使用既有离线模拟运行检查筛选→分析→清单→沟通→消息发现与重启恢复，明确哪些是用户页面操作、哪些是注入式模拟，不声称真实 BOSS 端到端通过。
- [ ] 只停止本轮新建的测试进程；保留小型截图/日志/哈希及最新公开安装器，删除本轮可重建的大型临时副本需沿用明确清理范围。
- [ ] 更新 README、交接、下一入口和本计划的实际结果并推送；报告释放空间、发布 URL、SHA、模拟验收覆盖与未验证项。
