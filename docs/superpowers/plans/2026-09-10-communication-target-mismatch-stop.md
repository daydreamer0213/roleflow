# 普通沟通遇到岗位不匹配立即中断

## 目标与已有决定

主线仍是智联接入、统一消息页和求职体检的最终验收。本次只补齐架构核对发现的一处现有约定落差，不扩展功能、不重新设计授权。

`AGENTS.md` 已规定：确认清单内串行沟通，岗位不匹配立即停止。当前两个平台适配器都可能返回 `target_mismatch`，共享执行器却只将当前项记为待核对，然后继续后续项；旧回归也错误地允许继续。这是本地状态处理缺陷，不改站点字段、页面识别、等待节奏或风险判断，不需要通过真实发送复现。

设计：保留当前项 `target_mismatch` 和待核对记录，复用现有整批中断处理，让批次与本轮同时中断；使用明确的原因和用户提示。未点击的剩余项保持待处理，只有用户主动继续才执行，不重试已错配项。不把未发送记为投递，也不要求用户核对一个根本没有发出的发送结果。普通下架/缺少按钮仍按既有逻辑跳过。

## 执行步骤

- [x] 先补失败回归：BOSS 批量与单项、智联当前单项验收入口，均不访问后续岗位、不点击、不新增投递统计；确认批次/本轮原因不是普通单项检查点。
- [x] 最小实现：共享执行器复用中断路径；已有用户错误提示映射补充岗位不一致及下一步。
- [x] 验证主动恢复只处理剩余项；下架/按钮不可用仍能继续；页面显示明确原因，内部编号放在折叠信息中。
- [ ] 定向回归、只读独立复核；更新交接，冻结提交并在精确 SHA 运行完整离线门禁。

## 边界和验收

仅使用临时数据库与假浏览器。保留当前 8787 新用户安装环境和 8788 旧验收资料，不重置、不替换安装文件；不访问真实招聘平台、不沟通或回复、不合并、不发布、不改版本。新安装包仍是 `6644c58`，其 160 项离线结果不是本次代码的验证证据。
