# 沟通续接修复实施计划

> 对应已批准设计：`docs/superpowers/specs/2026-09-04-communication-resume-recovery-design.md`。在隔离分支执行，按最小回归先失败、再实现的顺序完成，不重复请求方向确认。

## 步骤

- [x] 1. 核验基线与针对性测试。为 CLI/重新绑定改条件可继续、两个页面可见中断、空页恢复未完成本轮补充失败回归。
- [x] 2. 删除 CLI 与 Dashboard 的搜索条件等值检查，保留真实目标/绑定/不明确结果保护。
- [x] 3. 复用错误指导和现有布局显示批次中断；修正概览和标题，避免“没有阻塞”的矛盾信息。
- [x] 4. 初始搜索准备仅为空页补回同方案未完成本轮的冻结条件与关键词，已有页面不覆盖。
- [x] 5. 独立只读复审，定向回归与完整离线门禁；检查差异、更新交接、提交本地修复并在精确 SHA 复验。

## 验证

- `communication_cli_authority_smoke`：不同搜索条件仍绑定当前返回页，失败也恢复当前页；错误固定标签与浏览器能力仍拒绝。
- `dashboard_communication_batch_smoke`：改变条件可重新绑定/继续；清单不变、点击数不变、不导航、不增加标签；歧义仍拒绝。
- `workflow_dashboard_smoke`：工作流仍在复核时的批次中断也有原因和处理指引；人工处理仍优先。
- `initial_search_page_smoke` 与 `onboarding_progress_ui_smoke`：恢复空页，不覆盖既有关键词/筛选，按候选人/方案选择未完成本轮。
- 风险相关既有执行器/适配器/存储回归，严格本地浏览器检查与新鲜 `npm test`。

## 交付范围

只提交本地分支；不推送、不合并、不打包、不改版本，不接管当前真实批次。兼职与薪资修复候选保持独立。

## 已取得证据

- 修改前 5 项关键基线通过；新增回归分别复现了 CLI 旧条件阻塞、空页丢条件、批次错误不显示、服务端未传未完成本轮。
- 10 项风险相关定向检查通过。独立只读复审发现已绑定暂停 CLI 和通用模式空页两项遗漏；均补充先失败回归、最小修复及通过结果，最终复核无剩余 Critical/Important。
- 两页 × 两种中断（旧条件检查 / 结果不明确）× 两种宽度（1440 / 390）共 8 项本地 headless Edge 渲染检查通过：可见原因、继续按钮/歧义处理分流、键盘展开技术信息、无横向溢出或脚本执行错误、外部请求 0。证据位于 `D:\DevData\RoleFlow-verification\communication-resume-recovery`；这不是真实 BOSS 发送验收。
- 实现提交 `fb8ade6a05c0f129b4e453611d87e85fa550b106` 对应代码树的新鲜严格完整门禁为 142/142，退出码 0；该精确提交再次通过 10 项定向检查与 8 项页面检查。首次门禁发现的旧展示对象契约缺少新增错误字段已修正，失败经过保留在验收报告。
