# 智联真实验收：轮次来源与登录页头误判修复

本计划处理真实产品验收刚复现的两处缺陷，不重开此前已关闭的六项综合审查问题，不扩展功能或外部动作授权。当前用户已授权助手使用隔离简历副本走流程，普通初次招呼至多一次，停止于回复草稿保存；不发送回复、不处理简历、不投递。

## 当前证据

基线 `95cb094c78f9acc29b0da5ba50266d635d950742` 新鲜严格完整门禁 157/157、退出码 0，起止 SHA 相同且工作树干净。主控随后从实际本地 UI 验收：

1. 选择智联、准备并保存实际搜索条件、等待页面就绪，点击一次“开始一轮岗位发现”，却返回同方案下 2026-09-03 的 BOSS 沟通中断轮次。没有点击旧轮次任何动作。只读追踪确认客户端传入 site，而应用层的启动前复用查询没有传 site；存储层缺少 site 参数意味着查询所有平台，不是默认 BOSS。后续 Dashboard 查询已经按来源过滤，但被提前返回绕过。
2. 点击“开始只读发现”后提示智联登录失效。当前真实后台 DOM 仍有 20 个会话；可见 `.home-header__c-login` 是 `.home-header__right` 下的已登录账户区域，包含 `c-login__top` 等子节点，没有 form/input/iframe；没有 `.login` 或 `.login-panel`。现有登录检查把所有可见 class 含 login 的节点都视作登录挑战，仅排除了两个普通页头链接，未排除已登录账户区及其子节点。

计数复查确认旧 BOSS 记录不变，尚无新的智联岗位、草稿、沟通批次或回复发送/投递记录。真实证据保留在仓库外 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/live-acceptance-20260909.md`，不把原消息或个人账户名称写入 fixture。

## 最小实现

- 启动复用查询传递已规范化的所选平台，服务端注入存储查询时保留该平台。使用既有存储 site 过滤，不改变存储默认的全平台语义，也不改变 `preparePlanForNewWorkflow` 对全方案重新评分前的全平台保护。
- 登录检测仅排除当前真实证明的常规页头账户控件及其后代，保留页头结构约束。真正可见登录弹窗、风险提示、页面丢失继续停止；不按“有会话”绕过登录检查，不做宽泛的所有页头豁免。
- 不改选择器节奏、配额、共享浏览器互斥、数据库结构、框架或依赖，不激活平台页面。主控不修改真实数据库来绕过旧中断记录。

## 文件及验证

生产范围：`src/application/workflow/index.js` 的开始查询，`src/dashboard/server.js` 对应注入，`src/adapters/sites/zhaopin_message_reader.js` 的共享登录判断。测试范围：`tests/workflow_application_smoke.js`、`tests/dashboard_zhaopin_smoke.js`、`tests/zhaopin_message_reader_smoke.js`；必要时仅补充现有消息合成 fixture 的页头。

- [x] 先失败回归：旧 BOSS interrupted + 当前 ZL start 不复用旧 run；同来源仍复用；不同来源不会启动旧沟通。真实 HTTP 注入也覆盖 site，而非仅 fake getter。`09e131e` 先复现应用层参数遗漏和实际 HTTP 旅程返回旧轮次，再通过覆盖检查。
- [x] 先失败回归：已登录页头父节点和子节点不阻断正常消息读取/选择；真实可见登录挑战仍停止；原普通页头链接与风险检查保留。`09e131e` 先复现正常账户页头 LOGIN_REQUIRED，再通过读取/选择及边界检查。
- [x] 最小修复、覆盖检查、语法/差异检查、独立针对这两项改动的复核；不重复全分支综合复核。`09e131e5a431dc6af220f94c3b2d9168de500902` 的 workflow application、严格 ZL Dashboard 旅程、ZL message reader、ZL message discovery 四项检查及六文件语法/差异检查退出码 0。独立任务复核确认计划符合、质量通过，无新增重要问题；仅保留既有 SQLite/换行提示。
- [ ] 主控冻结最终代码后重新完整验证，再仅重启自己管理的 8788 服务，保留隔离资料，从真实 UI 继续原验收。

这是已有“按平台开始”和“只在真正需要登录时停止”合同的修正，没有新的架构取舍或用户决策。
