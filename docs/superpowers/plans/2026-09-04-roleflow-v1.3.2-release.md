# RoleFlow v1.3.2 发布与两轮人工验收准备 Implementation Plan

> **For agentic workers:** Follow executing-plans proportionately. Delegate only a bounded read-only release review; no BOSS operations or user-data mutations by reviewers.

**Goal:** 将后台沟通修复合并、推送并公开发布 v1.3.2，下载核验安装包，交给用户依次进行无记录首次安装和再次使用的两轮人工验收。

**Architecture:** 复用现有 D: 工作树、Inno Setup 和标签触发的 GitHub Actions；只更新发布元数据与说明，不改已复审的沟通实现。人工验收由用户操作，资料隔离与现有运行实例另行核验，不把换安装目录当成清空资料。

**Tech Stack:** Node 22.23.1、PowerShell、Git、现有 Inno Setup、GitHub Actions。

## Global Constraints

- 用户本轮已明确授权打包、推送、合并和发布；真实 BOSS 沟通/发送/投递仍不在代理操作范围。
- 起点 `8ca9df80c56b33ac22742620b8a795aebaa4f4a5`；本地和远端 main 为 `b852acc`。复用 `D:\DevData\RoleFlow-worktrees\acceptance-fixes-20260904`，发布分支 `codex/release-v1.3.2`。
- 构建、公开包和验证资料保存在 `D:\DevData\RoleFlow-release-v1.3.2`；复用 Node、依赖和 Inno Setup，不复制新开发环境。
- 不强推、不移动旧标签、不覆盖旧公开资产、不清空真实数据库或登录资料。不对旧工作树或历史 stage 扩大删除范围。
- 无记录与再次使用由用户在同一套新的验收资料上依次操作。当前账户备份隔离或另一 Windows 测试账户的选择先询问，不擅自执行资料迁移或账户创建。

## Task 1：目录诊断与发布候选

- [x] 核验干净工作树、远端 main、最新公开 v1.3.1 和不存在的 v1.3.2 标签。
- [x] 目录问题定位：Inno `DefaultDirName={localappdata}\Programs\RoleFlow` 不含版本，`UsePreviousAppDir=yes` 保留用户此前选择；注册表 DisplayVersion=1.3.1、InstallLocation=`D:\DevData\RoleFlow-first-use-acceptance\v1.2.0\app\`，程序 package.version=1.3.1。不是错误安装旧程序，不自动搬动已安装实例。
- [x] 将 package.json 和 lockfile 的根版本改为 1.3.2，README/release_boundary 与新发布说明同步；建议这台机器使用不含版本的 `D:\Apps\RoleFlow`。
- [x] 独立只读复审新发布差异，运行安装器相关离线检查、浏览器与沟通关键检查，提交候选 `23b94cf`；复审后补齐文档索引中的旧版本入口，无程序行为改动，不代表运行真实安装向导。

## Task 2：精确候选构建

- [x] 设置 D: TEMP/TMP/npm 缓存及已安装 Playwright，`ROLEFLOW_REQUIRE_PLAYWRIGHT=1`。
- [x] 在精确候选 `638d669` 运行 `scripts/package-release.ps1 -IncludePortableNode -OutputName RoleFlow-v1.3.2-portable.zip`；新鲜严格完整门禁 144/144、退出码 0，没有复用旧 SHA 的结论。
- [x] 同 SHA 用 `scripts/build-installer.ps1 -BuildRoot D:\DevData\RoleFlow-release-v1.3.2\installer -OutputDir D:\DevData\RoleFlow-release-v1.3.2\local -SkipTests` 并行编译；必须等该 SHA 完整门禁成功后才进入发布。
- [x] 2,621 个 stage 文件哈希一致，禁入项 0、便携必需入口齐全、安装器 sidecar 一致、独立 D: 数据自检 `SELF_CHECK_OK`；本地包哈希见发布回执。

首个候选 `23b94cfc5a37afd75929be9f4ea885314e0fdbe5` 已完成本地严格完整 144/144（退出码 0）及便携包构建，日志位于 `D:\DevData\RoleFlow-release-v1.3.2\verification\package-23b94cf.log`。GitHub 候选分支检查 run `33860763520` 成功。文档索引补正后的最终候选仍重新完整构建；不得把该首轮结果冒称为最终 SHA 的门禁。

## Task 3：合并、推送与公开发布

- [x] main 快进至 `638d669`，6 项合并后聚焦回归通过，main 和两分支均推送；精确 main 的 CI run `33861738497` 成功。
- [x] 创建并推送新注释标签 v1.3.2，peeled SHA 为 `638d6699b85a8e5cdea3a66370320a061bfd48ce`；没有修改旧标签。
- [x] 只读 API/公开页面核验发布 run `33862701527` 成功，Release ID `382640573` 为最新正式版、非草稿非预发布，三项公开资产齐全。
- [x] 三项公开资产均下载到本次 D: published 目录，大小和 SHA-256 与 GitHub 摘要一致、安装器 sidecar 一致；便携禁入项 0。本地与公开包哈希分别记录，不声称字节相同。

## Task 4：人工验收交接

- [x] 公开包同目录提供《两轮人工验收说明.md》，建议固定 `D:\Apps\RoleFlow`；第一轮从空白本地资料开始，第二轮不重置、不重装，验证再次使用。
- [x] 按用户选择停止已确认身份的旧服务、确认专用浏览器资料未占用，完整备份并两次逐文件核验 5,135 个文件后改名隔离原资料。没有删除资料，也没有自动安装、卸载或访问真实 BOSS；人工两轮操作由用户完成。
- [x] 更新当前交接、下一入口及 `docs/superpowers/reports/2026-09-04-v1.3.2-release-acceptance.md`，两轮真实结果仍待用户反馈。
