# 智联招呼成功弹窗识别修复

## 范围与授权

用户要求继续修复，完成后推送当前集成分支保存，最终人工验收后才考虑合并、发布。不重复既有真实招呼，不发送回复、简历或申请；现有工作树和依赖不变。具体技术设计按用户已有授权直接推进。

## 真实依据与根因

真实唯一招呼已由匹配的接口响应和对应IM出站文字证明成功。旧读取器只扫描通用dialog/modal或fixture-modal，并要求整个容器textContent等于“已向对方发送打招呼语”。

本次后台只读检查了真实搜索页的DeliverGreetingModal组件及已加载chunk-common客户端：根节点是`.deliver-greeting-modal`，标题为`.deliver-greeting-modal__title`，还包含正文、“留在此页”和“继续沟通”按钮。它由v-show隐藏/显示，不必有role=dialog。两处旧假设均不符合这个真实结构。现存组件处于隐藏默认状态；本次没有重新触发发送，也不声称重新观察到当时成功弹窗。

证据在仓库外`D:/DevData/RoleFlow-zhaopin-message-context-20260908/greeting-success-modal-20260909.json`。只读未调用站点组件方法、改动页面或抢前台。

## 推荐方案

在现有沟通快照中增加这个已观察到的专用根节点，并仅对该根节点读取其专用标题。继续要求可见且恰好一个成功弹窗；隐藏、错误标题、缺失标题、多个成功弹窗均不认可。兼容既有通用弹窗判定，真实fixture升级为专用结构和正文/按钮。

保持完整成功条件：本次发送时间之后、对应岗位的普通招呼响应成功且无待完成请求，当前搜索卡片/详情身份一致，再核验成功弹窗；既有同标签IM核验不变。旧成功弹窗仍阻止新发送准备。申请响应与真实平台异常仍立即停止。不增加重发、导航、额度开关或新依赖。

不采用只看HTTP200（丢失页面核验），也不新建自动跨消息页核对流程（当前根因可由已存在弹窗最小修复解决）。

## 验证与交付

先将现有脱敏fixture升级为真实结构，原完整adapter成功测试应得到ambiguous而非succeeded，形成实际RED；修改解析后恢复GREEN。补足隐藏/错误或缺失标题/重复弹窗负例，原错岗位、错网络、申请响应、零重发与清理测试保持。

限定独立只读复核，仅审查本次范围；然后冻结最终源码运行完整离线门禁。修复后的真实外部发送不重复执行，下一次真实自动识别由最终人工验收确认。推送同名分支，不合并、不打包、不发布、不改版本。
