# 兼职选择与空薪资筛选

用户于 2026-09-04 确认本设计。基线代码为 `7c7d67e`。

## 用户行为

- 本地精筛增加“接受兼职”，默认关闭，继承和通用模式都适用。明确为兼职的岗位在卡片或完整 JD 提供证据后排除，不进入推荐/沟通清单。
- 全职兼职均可保留全职机会；不接受兼职、管理兼职人员、有兼职经验等文字不能被误判为本岗位兼职。按用户补充，岗位薪资字段的 `元/时`、`元/小时` 作为兼职证据；明确全职的岗位不被计薪方式覆盖，描述中的加班时薪也不能代替岗位薪资。日薪本身不证明兼职。
- 通用模式启用兼职时，已有全职类型范围包含兼职；不改变继承模式的真实 BOSS 搜索范围。平台范围和本地偏好共同生效。
- 保留旧方案明确选择的“仅兼职”平台范围与可编辑选项，不在读取时扩大为不限。本地接受兼职开关独立生效；平台只支持单选却选择多类型时不拼接非法多值参数，使用平台不限并由本地资格检查筛选。接受兼职不等于接受实习。
- 初次生成方案的薪资范围留空，不采用模型建议或画像薪资自动填充。本地画像的期望薪资仍可展示，不自动变为筛选条件。
- 保存过的薪资不批量删除。用户可输入上下限或清空；空值在刷新、保存和再载入后仍为空。
- 未设薪资范围时不按薪资高低加减分、限制经验放宽、标记高薪备选或排除；只保留薪资展示和面议提示。只填一边时仅对应边界生效，另一边不补固定值。
- BOSS 页用户已选薪资档位继续生效，不因本地为空而取消。

## 实现

沿用方案 JSON，增加布尔 `allowPartTime`，无数据库迁移。资格检查输出 `part_time_role`，沿用既有本地硬边界及沟通拦截。方案快照、运行策略及缓存版本包含新规则。

在初次方案推荐边界清空薪资；常规归一化只保留输入值，不从画像回填。运行期只构建用户实际提供的薪资阈值，删除默认 24/35/18K 对空薪资方案的影响。零继续作为内部“未设置”兼容表示，UI 不展示为 0。

## 验收与边界

回归覆盖默认/启用/撤销兼职、全兼混合及否定描述、卡片与 JD 两阶段、沟通清单、初次模型返回数值但最终方案空白、已存薪资清空、单边范围、空薪资下高低薪与面议排序中立、继承平台薪资保留。

使用 fixture、假浏览器和临时数据库。不访问真实 BOSS、不改真实方案、不推送、不合并、不打包、不改版本号。已用只读连接核对用户给定 URL 的本机岗位记录：标题、薪资单位和详情均包含兼职证据；不会把真实 JD 或用户资料写入 Git。在线页面尚未重新获取，不能声称完成当前页面真实复验。
