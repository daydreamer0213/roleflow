# 消息与求职体检的用户可见闭环修复

本轮由用户提出并授权处理四个真实问题。基于现有设计实施，具体实现已委托主控；不增加方向确认环节。工作树 `D:/DevData/RoleFlow-worktrees/zhaopin-readonly`，基线 `80998f05c2da8d868e9e6c2d8a5a85103982868d`。

## 已确认的断点

- 真实体检页有18个成熟岗位、1个已读回复，却将“已读后的回复比例1/1”以“招聘方回复100%”展示。计算口径是条件转化，页面却让人理解为总体回复比例。
- 列表直接使用为了限制原文进入学习记忆而生成的通用摘要，忽略已有可展示HR原文和简历请求卡。
- 岗位来源标记 `message-discovery-detail` 作为关键词直接展示。
- 3个智联岗位已有 `resume_requested` 进度事件，没有求职样本；另有1个已验证的智联 `contact_started`，也因旧只读边界被排除。2026-09-08设计明确暂缓智联漏斗，本轮用户要求补上关联。

## 用户看到的结果

1. 主反馈区显示各环节已确认的岗位数及其占本轮成熟岗位的比例，明确共同分母。“1个回复/18个成熟岗位”为5.6%，不称100%回复率。既有条件转化诊断保留，收进“环节转化明细”，注明上一环节分母。缺少观察以具体原因说明，例如“尚未获取阅读状态”，不把它当失败。空结果写“尚未发现”，不展示含糊的“暂无明确状态”。
2. 消息列表用已允许展示的原文生成短预览；有简历请求时同时明确显示“索要简历”。详情保留完整原文。学习记忆继续使用原有安全摘要，不扩大原文持久化范围，不增加模型调用。
3. 岗位记录将内部标记展示为“消息发现”，正常搜索词保持原样；内部来源身份与历史数据库不改写。
4. 已验证成功的智联普通招呼进入既有样本、48小时和策略轮次体系；扫描、复制、未核验发送、仅显示已沟通过但时间不明都不创建新统计样本。对旧成功记录按已有可靠事件与原策略时间区间补接，幂等，不要求重复发送。
5. HR主动联系且没有可确认投递起点的岗位，单独显示为“消息中发现的机会”，展示已收到回复、索要简历、面试邀请数和消息入口。明确这些机会尚无可核验投递时间，不参与本轮投递转化。旧轮次迟到反馈仍归原轮次；不为显示数字伪造投递或修改策略边界。

## 实现边界与取舍

只改文案无法补齐真实智联样本；把所有入站消息当作用户已投又会污染成熟样本。采用已有可信沟通事件补接样本、无投递起点的反馈按事实另列。复用现有SQLite事件、消息原文展示上下文、Dashboard组件和统计计算，无依赖、无新表、无新平台读取任务。智联已读/送达没有真实来源时继续缺省，不合成BOSS观察。

验收覆盖真实本地页面与只读数据，常规回归使用合成数据；本轮不发真实招呼/回复、不处理简历或申请。继续当前分支，合并发布仍等用户最终验收。
