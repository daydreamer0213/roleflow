# 智联列表重复岗位：本次扫描内去重

## 真实前提与批准范围

用户要求持续完成现有真实验收，具体修复自主安排，不重复征询方向。07:36同一run/batch46在第45张卡中断，92岗位/91完整/1既有短JD保存。错误为身份未确认，不是详情加载超时。07:39只读真实页面：同一可靠岗位编号在索引4与44重复，两张均被智联标记active，完整详情452字符、两个详情编号一致；helper的findIndex只返回4。站点60张卡内另有3对重复编号。证据仅留仓库外 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/duplicate-selected-card-20260909-0738.json`。

本次仅解决扫描再次读取本目标内已经成功核验的、完全相同的卡片。优先沿用现有seen集合，不拓展选中状态协议，不放宽详情身份或沟通校验。

## 最小设计

当前seen使用含列表位置的完整signature，因此同一岗位在后续分页的重复卡片会再次成为目标。对已有可靠sourceId且signature符合当前 `index|...` 格式的卡片，扫描访问键排除位置前缀，保留其余完整signature（含编号、标题、薪资、公司、地点与全文）。没有可靠编号或格式不匹配时保留原signature键。仅在详情读取及检查点成功后标记seen，且每个关键词目标重新建seen集合。

这样，同一本目标中已核验的完全重复卡片不再访问、不重复扣物理额度，也不占新岗位数量；读取下一不同岗位，仍凑够原有唯一岗位目标或达到真实列表结尾。相同编号但卡片内容变化、不同编号的同名岗位、无可靠编号的卡片均不因此跳过。跨关键词/新scan/旧数据库缓存仍走既有实时身份、详情和内容核验，不把此优化扩展为跨轮缓存直通。正文过短的既有待补记录仍保留，不能因重复而变为完整。

保留原严格索引/签名/双详情编号匹配、单次空详情恢复、120秒加载等待、节奏/冷却/额度、异常即停及完整JD质量；不改浏览器helper、沟通或页面UI。

## 验证与交付

原代码行为RED：后续重复卡片原生同时active、helper返回第一位置，scan中断，后续唯一岗位未读。修复后同一输入应完成唯一岗位目标，重复卡片不再点击/预留详情访问；检查点与逻辑岗位各一次。覆盖内容变化、不同编号、无编号不误跳过，以及跨目标仍重读。既有4项智联/扫描关键回归，限定独立复审，冻结后新的严格完整门禁，再正常UI继续原轮。原 `da6e1c9` 的157通过不认证后续源码。

无前台激活、截图/显示参数修改、刷新真实页面、私有接口重放、新沟通/回复/简历/申请、推送/合并/发布/版本变更。本任务只做离线源码修复；真实浏览器、服务和最终门禁由主控负责。
