# 智联真实验收检查点（2026-09-09）

## 2026-09-10 修复收口（优先于下方历史）

“真实招呼已发出但页面结果未识别”的已确认结构缺陷已修复，功能提交`c63d469638c995a53247618e787cd1f48eaf4fc7`。真实页面及已加载客户端证明成功提示使用`.deliver-greeting-modal`和专用标题，另有正文及按钮；旧代码既未匹配专用根节点，也错误比较整个弹窗文字。现在只对专用根节点读取标题，继续要求可见、唯一且与当前岗位及本次网络响应共同核验，不扩大成功条件或增加发送重试。

- 改动：`src/adapters/sites/zhaopin_communication.js`、`tests/fixtures/zhaopin/communication.html`、`tests/zhaopin_communication_adapter_smoke.js`。未加依赖或改其他生产模块。
- 真实结构脱敏fixture先在旧代码下得到实际RED（`ambiguous`而非`succeeded`），修复后4项定向GREEN；隐藏、错误/缺失标题、重复成功弹窗仍不认可。限定独立复核通过，无阻塞发现。
- 最终功能SHA新鲜完整离线门禁 **157/157、退出0、起止SHA一致、工作树干净**。日志`D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-c63d469-20260909-160148.log`。后续只修改交接文档，另核对非文档差异为空。
- 本轮只做真实平台后台只读结构检查，没有再次发招呼、回复、简历或申请。旧唯一招呼仍保留一次点击及人工核验依据，未重放；修复后的真实自动识别留待用户最终人工验收，不能把脱敏fixture当作新实站发送通过。
- 用户授权完成后推送`codex/zhaopin-readonly`保存；暂不合并、打包、发布或改版本。远端SHA回执保存在仓库外`D:/DevData/RoleFlow-zhaopin-message-context-20260908/push-receipt-20260910.json`，以实际回执为准。
- 原4条短JD、1个模型契约失败、22项消息身份/资料不足仍保留，不计入全量成功。当前下一入口为最终人工验收，不重开已完成修复。
- 9月9日临时额度授权已于北京时间9月10日00:00失效，BOSS/智联继续各自独立计数；验收服务后续使用默认额度，生产设置不变。

以下保留修复前的真实验收事实及证据，不再作为当前待修复状态。

## 结论

后台岗位定位修复完成；一次普通招呼已真实发出，并在对应消息会话中只读核验。新 HR 回复、岗位资料、模型草稿、编辑自动保存和刷新保留已有真实通过样本。**整条自动流程尚未完全通过**：发送请求成功后，产品没有识别到页面结果，仍进入了结果待确认；后续由助手按实际会话证据，通过本地“确认已沟通”完成核对，没有重复发送。

工作树 `D:/DevData/RoleFlow-worktrees/zhaopin-readonly`，分支 `codex/zhaopin-readonly`。最终功能代码 `cbf5d9d9b65ea1e172baed7ba5154ce1d769dd8c`；本检查点仅更新文档。未推送、合并、打包、发布、更改版本或替换安装版。

## 当前已验证结果

### 找岗、JD 与分析

- 继续同一真实 run `25641478-c928-403a-8d3d-c3f189895298`、batch46，没有重新造一轮样本。
- 204 个去重扫描岗位：200 个完整 JD，4 个短 JD 保持待补资料。
- 两个方向各完成50/50；第三方向覆盖50但存在短 JD，整轮仍为 `SCAN_TARGETS_PARTIAL`，页面正确显示完成2/3、剩余1。
- 实际模型分析12成功、1个 `MODEL_CONTRACT_INVALID`、191个本地规则跳过；没有待运行分析或重复岗位/观察记录。不能把这一批说成全量分析通过。

### 一次普通招呼

- 不可变目标：batch6/item42/job339，`CC202740210J41005667704`，AI应用开发工程师 / 润建股份。
- 先前定位失败发生在点击前；后台定位修复后，14:50 UTC 完整生产定位路径只读核验 ready，岗位/公司/选中卡片/详情编号吻合。原页恢复，5个标签及活动标签不变。
- 目标已有14:07 UTC真实截图与遮蔽图；14:50新 DOM 核验成功，但可选截图超时，不把它写成新截图成功。
- 15:14:35 UTC，从正常 UI 的“验收这个岗位并自动暂停”恢复一次；**平台实际点击仅1次**，15:15:14 UTC记录。平台普通招呼接口 HTTP200/business success，耗时466ms；结果页核验未通过，15:17:15 UTC以 `COMMUNICATION_RESULT_AMBIGUOUS`停止。未观察到申请接口。
- 15:24 UTC仅刷新既有智联消息页后，对应岗位会话出现在列表；15:25:22 UTC只读打开并核对该会话，岗位/公司一致，存在 `fromMe=true`、`flow=out` 的招呼文字及服务端消息编号，发送时间15:15:16 UTC与唯一点击对应。另一条custom99是“23:15”时间提示，不是简历卡。
- 15:29:33 UTC，本地 `/api/communication-resolve` 一次POST成功，将item42记录为`succeeded`并保留原自动核验失败与`manualResolution`证据；平台点击数仍1。批次仍保留`interrupted`历史，不将人工核对冒充自动核验通过。
- 本地验收辅助脚本首轮遗漏必填依据，随后后台表单导航延迟；激活既有后台渲染支持后，已提交本地结果落库并导航，导致脚本后续DOM命令失效。最终以HTTP303、数据库与页面结果为准，不将辅助脚本调用成功/失败等同产品结果，也不再次提交或发送。

### 消息和可编辑草稿

- 14:59 UTC正常UI开始只读消息发现，智联20条可见会话；BOSS没有连接，未访问。
- 15:06:44 UTC正常UI“安全停止”保留剩余工作，已处理1条新回复，排队18、未解决22；这是有界验收样本，不是全队列通过。
- 新样本包含HR原文及简历邀请待办、真实JD分析、两份本地模型草稿17/18。智联累计6份草稿，4份此前草稿重启后保留；累计208个智联岗位中204来自扫描、4来自消息补JD。
- 草稿17实际键入“谢谢。”后自动保存revision1，刷新后文字完全一致。智联回复发送按钮不存在，reply-send批次/条目均0。
- 没有发送回复、点击简历同意/拒绝、申请或投递；原BOSS副本记录和已安装程序保持不变。

## 修复设计与验证

根因是沟通定位只在“搜索页就绪”期间短暂保持后台渲染，后续加载列表时已释放；JobCard组件虽有岗位身份，DOM标题尚未渲染，现有身份规则不能认可。现在让整个定位和详情读取复用既有`openSearchRenderScope`，完成/取消/错误时释放；复用现有`releaseSearchRenderScope`保留原错误与清理错误。

改动文件：

- `src/adapters/sites/zhaopin_communication.js`
- `src/adapters/sites/zhaopin.js`（仅导出现有清理函数）
- `tests/zhaopin_communication_adapter_smoke.js`

实际旧代码RED，修复后4项定向GREEN，唯一限定复核及一次修复复核通过。未改变身份规则、20次滚动上限、随机节奏、冷却、额度账本或发送重试策略。

最终功能SHA `cbf5d9d9b65ea1e172baed7ba5154ce1d769dd8c` 上运行新鲜完整门禁：**157/157，退出0，起止SHA一致，结束时工作树干净**。日志：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-cbf5d9d-20260909-145726.log`。最后一个私有链路测试中的`fatal: Needed a single revision`属于其已通过的回退场景，不是门禁失败。

## 当日额度授权

- BOSS与智联本来独立记账：`site_access_budget.js`按site建立用量；`scan_store.js`的访问查询过滤site；`communication_store.js`的已用与预留沟通额度也过滤平台。不是共享总额度导致本轮阻塞。
- 用户授权9月9日突破访问额度。仅仓库外隔离验收进程配置智联当天搜索导航32次/详情读取450次；正常短时限制、节奏、发送限制和历史用量不变，不清空计数。
- 限定精确验收数据库与智联，动态策略北京时间9月10日00:00失效，运行中的控制器也恢复默认；未新增产品开关或修改生产默认配置。通过平台隔离、数据库隔离、当日边界和到期回退检查。
- 这次使用临时额度的验收不证明默认额度足够跑完全部流程。

## 剩余入口

1. **优先修复发送后的结果识别**：已证明普通招呼真实成功，但当前仅凭搜索页成功弹层或同标签IM正文的核验未通过。先读取当前真实页面/已加载客户端的具体成功呈现路径，再用脱敏fixture做最小修复。HTTP200不能单独代替页面核验；不重放batch6，不再发送第二条来复现。
2. 结果页单项模型分析失败重试入口仍缺少；1个模型契约失败与4个短JD保留，不降低质量、不反复重扫消除partial。
3. 消息队列中公司/岗位身份不足的22项仍待处理，不伪造关联，不声称队列全部通过。

浏览器操作保持后台；实际活动标签`1995703199`与5标签基线未改变。后续运行须重新获取绑定，不能复用这里的数字作为当前授权。

## 仓库外证据

目录：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/`。

- `confirmed-target-6-proof-2026-09-09T14-50-08-978Z.json`、`confirmed-target-6-20260909-redacted.png`和对应redaction回执。
- `single-outgoing-verified-20260909.json`：真实对应会话、出站文字与发送时间；含真实账号沟通内容，不提交Git。
- `new-draft-edit-20260909.json`：编辑、自动保存、刷新保留。
- `checkpoint-20260909-live-acceptance.md`：服务与运行断点。当前隔离8788服务PID18092、桥接PID12208均只作为本次记录；没有正在运行的沟通或消息发现。
