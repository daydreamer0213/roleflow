# 统一消息发现实施与验证

状态：本地实现、独立复审及新鲜严格完整门禁通过，可以准备开发分支的人工只读验收。工作树 `D:/DevData/RoleFlow-worktrees/zhaopin-readonly`，分支 `codex/zhaopin-readonly`。不据此替换安装版或宣称真实全流程通过。

## 用户可见范围

复用现有消息页，一次手动发现后台串行读取已连接的 BOSS 与智联；默认合并、可按来源筛选。保留 HR 原文、安全卡片摘要、可信岗位分析、可编辑自动保存草稿及复制学习。来源筛选不是找岗平台切换。

智联仅做只读发现和本地处理，不执行发送、投递、简历同意或拒绝，也不生成求职漏斗样本。BOSS 已有确认发送维持原授权边界。未连接、加载中、岗位资料待补不能显示为成功读取零条。

## 实现依据与根因

- [批准的设计](../specs/2026-09-08-unified-message-discovery-design.md)；[执行计划](../plans/2026-09-08-unified-message-foundation.md)。
- 复用既有消息队列、SQLite、草稿学习和页面组件；按持久化岗位来源隔离平台，不新增依赖、消息调度器或第二套消息产品。
- 智联实际时间线曾延迟出现正文；观察不能证明网络慢、后台休眠或必须前台。读取器有可取消的有界等待，正文未就绪不提交已处理标记。
- 简历请求卡转为人工待办；我方样式的平台提示不是用户回复，不得截断此前 HR 消息。
- 完整岗位资料只按同平台、同用户、当前方案及准确岗位编号关联；缺少可信完整资料时保留原文，不用同名岗位猜测。
- 复审发现空读取可能清除待处理项、读取失败未持久化、旧正文与新职位名称混合，以及持久化读取缺少平台来源。任务修复与复核已关闭这些缺口，包括真实读取器的明确时间线加载失败；没有放宽登录、风控、目标身份或取消边界。

## 阶段证据

| 范围 | 代码与验证 | 结论 |
| --- | --- | --- |
| 平台隔离基础 | `6028908bd4e280a6d38a9ce4efae428819fd8d6d`；7 项定向回归、任务复审、新鲜严格完整检查 149/149，退出码 0 | 基础完成；不是最终统一功能门禁 |
| 智联只读读取器 | `1b60d06`、修复 `fd3240b`；读取器/预览/消息发现三项回归及修复复核 | 正文、请求卡、平台提示、身份核对与取消通过任务检查；运行环境兼容项留给界面集成任务收口 |
| 持久化消息处理 | `5c0e70f` 至 `218ad5933904af7d95872aeca2908ed9e6740850`；7 项定向回归，最后失败分支修复后主控重新跑 4 项，退出码 0；两轮修复复核 | 空读/失败保留、实际文件库重开、跨平台隔离、缓存归属、SQLite 失败回滚、旧数据迁移及复制学习边界通过任务检查 |
| 统一入口与界面 | `09d4dc05e490af01db2d6b887b8dd75df85e3b80`；主控静态提交六项串行回归通过；停止入口修复 `517d5d7d96a1842e7bee6e27c0d8b99da9e6c592` 上主控重新跑严格统一消息旅程、BOSS 回复发送、消息发现三项，退出码 0，修复复核通过 | 任务完成；全分支收口见下一行 |
| 全分支复审与最终修复 | 全分支 `15dc548..3424352` 审查；唯一修复波 `23dad6e5f4737209bec793a6fac368e7a061d86d`，最终改动后 15 项严格定向检查通过；限定复审逐项确认六项 Important 与两项 Minor 均解决、无新问题 | 复审完成 |
| 首次最终完整检查（失败记录） | `23dad6e` 首次完整门禁退出码 1：`dashboard_runtime_smoke` 仍断言消息发现无条件准备 BOSS 工作区，与已批准的独立平台入口冲突；原始日志 `final-full-23dad6e.log` 保留 | 未通过，不作为完整成功证据 |
| 旧测试对齐 | `bd659782497941a842e22c46ca6b15518d9bd414` 仅修改 `tests/dashboard_runtime_smoke.js`：去掉多余响应，断言消息入口的画像校验及不调用 BOSS 准备；保留扫描登录拦截和零启动断言。三项定向回归及独立小范围复审通过 | 生产代码与 `23dad6e` 相同，没有跳过或削弱测试 |
| 新鲜严格完整检查 | 在静态 `bd659782497941a842e22c46ca6b15518d9bd414` 从第一项重新执行 `npm test`，实际 **152/152**，退出码 **0**，末行 `All 152 offline checks passed.`，开始和结束 SHA 一致 | 最终代码及测试树完整门禁通过；未拼接上次部分结果 |

本次完整门禁设置 `ROLEFLOW_REQUIRE_PLAYWRIGHT=1`，使用既有 Node/浏览器测试运行环境；临时数据放在 D 盘，未新增依赖。完整日志为 `D:/DevData/RoleFlow-zhaopin-messages-20260908/final-full-bd65978.log`。既有 Node SQLite 实验性警告、Git 合成仓库提示和故意无效修订的诊断均保留，不关闭诊断来美化输出。

文档提交后的精确 SHA、与 `bd65978` 的代码/测试树一致性及六项关键复验结果统一记录在仓库外 `D:/DevData/RoleFlow-zhaopin-messages-20260908/final-verification.json`。完整 152 项属于上述静态代码提交；不把文档提交后的定向复验说成再次完整运行。最终提交复验以该回执的实际成功状态为准，回执缺失或失败不能视为通过。

界面回归覆盖双平台串行、单智联不要求 BOSS、中途停止保留先前结果、来源筛选前保存及保存失败保留文字、刷新与人工待办重启恢复、编辑后复制学习、智联发送接口拒绝和来源导航保留。主控查看了仓库外 `unified-messages-1440.png` 与 `unified-messages-390.png`，桌面双栏、手机堆叠，截图有意展示保存失败后仍保留编辑的合成测试状态。一次并行执行既有智联旅程停在暂停后关闭/重开处，未确定原因；仅终止了已核实的合成测试进程并保留现场，随后实施者与主控各自串行新跑均成功，不能把被终止那次算作通过。

任务复审另修复了智联筛选隐藏在途 BOSS 停止入口的问题：新发起发送动作隐藏，但已有批次的进度和停止保持可见。真实本地执行器回归启动三条合成草稿、切换来源并刷新后停止，三条均停止，发送点击和发送端写入均为零；没有额外批次、进度、学习或漏斗写入。截图也验证了停止按钮的可读性。

最终全分支复审（`15dc548..3424352`）发现的六项重要问题已在 `23dad6e` 修复并通过限定复审：真实找岗的裸岗位编号在消息查询边界正确转换；空正文和未知我方内容保留待处理，不清原文或写已处理基线；列表加载按原 120 秒窗口有界等待且可停止；缺失方向标志保持未知；待处理分支保留既定访问间隔和每十条短休息。另清理正常等待与取消后的监听，智联已读/送达在初始化、等待、未连接、停止及完成状态始终保留未知值，BOSS 汇总数字不变。对应回归使用本地公共入口、真实身份生成器、SQLite 及合成 DOM，不代表真实网络原因或真实账号出现过这些全部问题。

## 修改文件

本阶段主要修改文件按职责归为：

- `src/adapters/sites/zhaopin_message_reader.js`：真实结构合同、身份校验、后台只读和加载等待。
- `src/core/message_discovery.js`、`src/core/message_preview_state.js`、`src/application/message_discovery/zhaopin_job_context.js`：来源隔离、待处理保留、可信岗位关联和共用处理链。
- `src/core/storage.js`、`src/core/candidate_progress.js`、`src/storage/message_reply_send_store.js`：迁移、入站来源、草稿恢复与 BOSS 写入限制。
- `src/dashboard/message_discovery_controller.js`、`src/dashboard/message_discovery_view.js`、`src/dashboard/server.js`、`src/dashboard/ui/navigation.js` 和一条现有 CSS：统一入口、来源筛选、用户操作与接口边界。
- 对应存储、读取器、消息处理、Dashboard 测试及 `tests/dashboard_unified_messages_journey.js`：临时库和本地浏览器行为回归；`tests/run_all.js` 注册新增检查；`tests/dashboard_runtime_smoke.js` 对齐统一入口的既有运行校验。

## 真实平台访问与未验证项

仅按用户授权，在现有普通 Edge 智联消息页做最小只读结构核验和选择会话。实际读取到两条文本、一张简历请求卡及一条平台提示；一条未读正常变为已读。主控没有激活前台、填写或发送、同意或拒绝简历、投递、操作生产数据库，未调用真实模型。

证据位于 `D:/DevData/RoleFlow-zhaopin-messages-20260908`。截图已遮蔽侧栏身份，但仍包含用户在会话中提供的消息内容，仅保存在仓库外；不能笼统声称所有正文均已脱敏。结构记录与本地合成夹具不携带真实账号或凭据进入 Git。

本地测试使用临时数据库、假平台及合成消息。即使最终通过，也不代表账号完整历史覆盖、实站产品端到端或真实模型草稿质量通过。首版只读当前页面已加载的近期会话。

## 交付边界

当前版本仍为 1.3.2。上一阶段分支推送 `ffb0a32` 是历史授权成果；本次新增代码不推送、不合并、不打包、不发布，也不替换用户安装版。后续先准备开发分支的人工只读验收，再按用户实际授权决定集成。
