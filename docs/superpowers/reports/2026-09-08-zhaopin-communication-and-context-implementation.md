# 智联普通沟通、消息岗位补全与统一布局：实施回执

更新于 2026-09-09 02:38 UTC。**真实消息/JD/模型/草稿/编辑保存样本已通过；短JD隔离实现及增量复核已关闭，正常恢复同一轮后正在扫描，当前42岗位/41完整JD/1待补。整轮分析、单岗位招呼和最终完整检查尚未完成，不代表全流程通过。**

## 当前实现与恢复结果

- 短JD隔离 `fdc7d21368ffb7a42f731e99cda7e5ffa7ebf896` 只修改 `src/adapters/sites/zhaopin.js`、`tests/zhaopin_workflow_smoke.js`。实际旧代码RED、4项指定GREEN/语法/diff通过；实施者另重复同4项均通过，但不把重复次数当额外能力证据。独立增量审查 Approved，无Critical/Important/Minor；未改动质量/CLI消费者由主控另行追踪，当前 `workflow_scan_analysis_smoke` 退出0，证明短项不分析、完整项进入分析。先前公司/新区/扫描任务保持关闭。
- 主控仅恢复自己的隔离8788（PID6648/session34298），健康与完整命令核对代码/数据根正确；桥接44114未换浏览器。普通UI一次“继续本轮”后服务端同一run进入scanning，02:38UTC为42岗位/41完整JD/1待补，无错误。实际已越过短项继续后续岗位，但尚不能证明下一关键词和整轮分析完成；新区原失败岗位仍待当前路径重访。
- 为并行推进，文档冻结后由主控运行唯一新鲜完整离线检查，同时继续只读验收，不改冻结树；若出现新源码缺陷则旧结果不能认证最终SHA。任何普通招呼前仍须完整门禁通过、当前精确目标及实际截图/DOM证明。背景截图尝试目前得到错位/空白或超时，已保留为不可用证据并恢复临时页面样式/焦点模拟；不使用旧岗位图片替代，不抢前台。
- 非阻塞观察：运行页“流程体检”的历史BOSS链接来自全方案历史视图，不是当前智联run/批次的分析或沟通输入。只记录显示范围问题，不通过旧链接重试历史任务。无新招呼、回复、简历或投递；正式安装版和旧BOSS不变，不推送/合并/发布。

以下为修复发现时的历史，旧服务状态和待审查文字不代表当前状态。

## 当前：短正文待补项隔离

- 01:57:50UTC同一run在34岗位/33完整JD中断；01:59/02:00UTC当前DOM确认编号/标题/公司/薪资/地点匹配、非加载。职位描述含标签70字符，正文只有几句话且无展开，不是此前公司/区名问题复发，不应反复等待或降低质量标准。
- 代码在保存 `detailRead=false / ZHAOPIN_DETAIL_INCOMPLETE` 后直接抛错，CLI无法进入有效JD分析。独立只读追踪确认：adapter正常返回partial后，现有CLI会分析本批次；短项自动成为 `DETAIL_REQUIRED` 不调用模型；partial目标保留恢复，队列结束不会自动重新扫描。
- `2026-09-09-zhaopin-short-jd-isolation.md` /7802be4 单任务：短项也计预算/节奏/seen并保存，继续其他卡和关键词；目标仍partial、整轮无fatal异常。加载/身份/风控/掉页/取消/持久化异常仍停止，预算/滚动上限不放宽。实施者只拥有adapter和既有workflow测试，主控文档/真实验收/最终全测。
- 主控确认工作流已中断并释放lease后，核对health及完整命令，仅停止11344/8788。桥接和安装版未改；没有新招呼/回复/简历/投递。以下保留此前检查点，不再等待已关闭审查。

## 最新：26份完整JD后的新区后缀

- 当前更新：`490390ec91a13ca873fca3e9a718bc600c2338e7` 仅一行正则与合成真实adapter回归；实际RED后 readonly/workflow/communication_adapter 三项GREEN、两个JS语法及diff通过。原审查交接未返回且已不在运行，主控恢复该唯一任务复审；结果 Spec compliant / Approved，无Critical/Important。主控核对完整编号前提与无编号严格地点拒绝回归，关闭未改动代码的待核对项；既有SQLite实验警告记录为非阻塞，不隐藏。
- 主控自行恢复现有Edge桥接和仅拥有的隔离8788，未重启浏览器或要求用户重开页面。普通今日任务→继续本轮链接→继续按钮：后台文档最初未实际提交，恢复页面生命周期及后台渲染后原提交完成，第二次输入在身份保护处退出、未再点击。服务端确认仅一次resume、同一run进入scanning；01:53UTC已从26推进到28岗位/28JD，6标签、无前台切换。新区原失败岗位尚未重新经过当前扫描，不能把恢复运行说成该项实际通过。
- 以下是该问题发现时的历史。当前不再等待审查或重做修复；只读扫描/分析稳定后才最终完整门禁，普通招呼前必须通过。无新招呼/回复/简历/投递，不改用户条件及安装数据。

- 公司字段任务1053b96已完成复核；主控在04359e7重启自己的隔离服务，正常继续同一run。原失败代招岗位已通过实际产品路径持久保存正确company/client_company与完整JD；20→40滚动也已实站通过。
- 22:47:11UTC同一run在26岗位/26JD后中断，`ZHAOPIN_DETAIL_IDENTITY_UNCONFIRMED`；最小当前DOM22:48UTC显示四ID/公司/标题/薪资完全一致，卡片“上海 浦东 周浦”、详情“上海·浦东新区”，JD2315字、非加载。现有末尾单字区/县处理把浦东新区变成浦东新；不是公司问题复发或网络诊断。
- 限定 `2026-09-09-zhaopin-new-district-suffix.md`，只扩充host函数行政区尾缀的整体移除，精确ID前置条件/城市/不同区/其余身份规则均保留，不新增字典或模糊匹配。主控核验后停止仅拥有的11280/8788，保留26结果。未开始最终全测、分析或普通招呼；无新回复/简历/投递，安装版及旧BOSS不变。

以下为此前检查点，最新执行入口以上一节为准。

## 最新：当前面板的客户公司字段

- **修复复核完成：** dc984dd 首次分离字段；独立审查发现普通摘要可能遮蔽冲突publisher，1053b964466b40f9e02cf54f486a1ba0ada12fd9 让publisher独立严格核对、内部证据不进入最终岗位并升级helper6。初次1RED、修正2RED、两次最终3项GREEN/语法/diff，唯一复核问题已关闭。主控22:19UTC同一真实页最小读取获得正确publisher/clientCompany、644字JD及精确编号，6标签/前台不变；这不是整轮通过。实际扫描/分析和最后完整检查继续由主控执行。

- `1bc7879` 同一run正常继续后保存4个新岗位和4完整JD，未再出现检查点错误；新的 `ZHAOPIN_DETAIL_IDENTITY_UNCONFIRMED` 自动出现在页面，证明终态刷新已实际生效。
- 22:06 UTC 最小后台DOM读取确认当前面板 summary 为明确“客户公司：…”、company-info 为与卡片完全相同的发布公司；全部岗位编号/标题/薪资/地点已核对且不在加载。不是网络慢或实际岗位错位。限定计划 `2026-09-09-zhaopin-panel-client-company.md` 复用既有 company/clientCompany 语义并保留发布公司冲突拒绝；无新匹配框架。
- 主控取消 `1bc7879` 在途全测，session33035 Ctrl-C退出1、未完成；不是测试断言失败，不能声称157通过。日志 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-1bc7879-20260908-215629.log` 保留。最终全测安排在只读扫描/分析稳定后、普通招呼前。
- 22:08 UTC 只读数据库：智联7岗位（3消息JD+4新扫描）、4草稿、21待处理；BOSS160岗位及23历史成功/1不明/7待处理不变；智联普通招呼批次、回复发送和投递均0。核验后仅停止拥有的14748/8788，未改安装版/桥接或用户方案。

以下章节是当时检查点记录；当前任务及状态以上一节为准。

## 扫描恢复修复已复核，进入重新验收

- Task1 `8cb39ff`：后台渲染覆盖导航就绪及滚动，严格区分默认筛选类别与实际选择，适配器按原存储合同保存完整进度；两条实际RED、5项指定及8项相邻GREEN、语法/diff通过。一次复审发现关闭失败可能覆盖主错误，`a61e34c306fa2094104fe7c28219ba954781f053` 用标准AggregateError保留主错误和清理错误，含原transport cause；另两条RED、三项GREEN、语法/diff后，唯一问题复核关闭。
- Task2 `e0258bc`：同一phaseKey进入终态时复用单次reload，使服务端正确显示原因及继续/结束入口；实际RED、两项页面GREEN、语法/diff及规格/质量复核通过。仅既有SQLite实验警告记录为非阻塞。
- 修改仅 `src/adapters/sites/zhaopin.js`、`src/dashboard/assets/workflow.js` 与三个对应测试。没有更改存储约束、模型、质量目标、用户方案、平台写权限、浏览器transport或BOSS流程。历史审查保持关闭。
- 接下来主控自行重启仅拥有的隔离8788、正常继续同一智联run，并在静态源码上执行新鲜严格完整检查。d70a0c8的157项是前一检查点，不能证明当前最终代码。本段尚不声称新实站扫描或普通招呼已通过。

## 最新：真实新扫描恢复任务

- `d70a0c88a1c02708bf8f6faaa91b96974051fdde` 严格全测末行 `All 157 offline checks passed.`，退出0、起止SHA相同且clean，日志在仓库外 `full-gate-d70a0c8-20260909.log`。不能覆盖之后的扫描恢复修改。
- 第二次普通只读消息运行：公司待核对项更新为明确原因 `ZHAOPIN_MESSAGE_DETAIL_COMPANY_UNVERIFIED`，保留原文且不关联/不生成草稿，随后队列继续处理另一有效会话。主控再次安全停止，在实际页面看到了对应通俗说明。21:13 UTC只读计数：智联3个消息关联JD岗位、4份草稿、21条待处理；没有新扫描岗位。BOSS160岗位及旧23成功/1不明/7待处理保持不变；回复发送批次/项、投递仍0。
- 新扫描run `25641478-c928-403a-8d3d-c3f189895298` 通过正常今日任务入口启动，首次导航后 `ZHAOPIN_SEARCH_RESTORE_TIMEOUT`，未采集新岗位。DOM证明卡片标题的截断组件未完成后台渲染；既有后台渲染开启后可见标题恢复，四ID仍一致。新增默认“融资阶段（不限）”又使9/10项标签全等比较失败。不是关键词/方案变更，不应要求用户重设已正确的条件。
- 同次错误暴露适配器异常/滚动/正常回调的检查点字段不符合既有合同；只读复现确认，不改存储约束。页面则因中断前后phaseKey同为acquisition而不重新加载，旧标题和“没有阻塞”残留、控制区为空；手动reload立即显示正确中断原因及继续/结束按钮。
- 新限定计划 `2026-09-09-zhaopin-scan-live-recovery.md`（c6dcea8），独立任务只修适配器及当前页面状态。既有DOM、消息、综合复核全部关闭，不重开历史审查。主控核验无活动任务后仅停止自己管理的PID3696/8788；正式安装版和Edge桥接不变。修复复核后继续同一run、新鲜全测后再验收至多一个普通初次招呼。
- **实际外部动作：本轮尚未发出普通初次招呼、回复、简历处理或投递。** 只读消息选择可能正常更新平台已读状态；资料和草稿只写隔离本地副本。截图取证一次超时，未声称获得新截图，不进行前台恢复。

## b4ed583真实样本检查点

以下保留当时检查点；最新计数、服务状态和下一入口以上一节为准。

- 公司资料隔离 `b4ed583a8877df9417c6658e05eeddd25393e30a`：3项真实失败回归及必要枚举接口失败后，5项定向/相邻检查、7文件语法/差异检查通过；独立规格与质量通过。非阻塞测试缺口：后续触碰队列测试时补“唯一草稿属于第二会话”的直接断言；当前已有第一项无岗位/分类、仅第二项分类及单草稿的验证。真实资料错位、登录/风控/清理终止集合未改。
- 主控隔离服务重新启动于PID3696/8788，运行正确D工作树和隔离资料。普通UI读取20条会话，在1条已分析结果出现后点击安全停止。Python两条HR原文、简历请求卡摘要、通信设备Python岗位内容/分析均在同一右侧详情显示；真实模型输出后保存草稿13/14，不是fixture草稿。分析为基础条件不满足，该岗位不用于主动招呼验收。
- 编辑草稿13一个标点，看到待保存→已自动保存；切到另一消息再切回、刷新都保留。还原标点后立即点击今日任务，再返回消息也已正确保存。只读数据库确认草稿13 revision2、14 revision0，两者仍开放、文字均回到原始生成内容，同属卡片34/岗位266/正确智联岗位编号。未点击复制或发送，未点击简历同意/拒绝。
- 此时智联2个已读取岗位、2份草稿、22条保留待处理；BOSS160岗位与历史23成功/1不明/7待处理不变；回复发送批次/项和投递表仍0。第二岗位在停止时保留阶段记录，不声称它分析完成。公司展示名未验证项尚为旧待处理说明，正常重读后才能验收新提示；不把有界样本说成20条全部通过。
- 接下来冻结源码执行新鲜严格完整npm门禁；期间只允许不改源码的补充只读验收。只有新门禁通过后，才执行至多一个来自新扫描且真正适合的普通初次招呼。没有推送/合并/打包/发布。

下列为之前检查点的历史记录，保留当时证据，不代表当前仍在修复。

当前限定计划 `2026-09-09-zhaopin-message-context-isolation.md`：同一明确编号/标题的会话公司展示名不能验证时，保留原文和原因，不关联/模型/草稿，但允许其他有效会话按原流程处理。编号/标题/会话变化及登录、风控、页面/清理失败仍终止，不增加公司模糊匹配，不下调120字门槛。此前DOM修复07de183/f6e83ab经历实际RED、8项初次/5项修复覆盖GREEN，独立任务复核关闭；真实后台搜索readSearchState+waitForSearchReady通过四编号/地址核对，Python原reader实读257字。另一个精确失败岗位实读同ID/标题、不同公司展示名、105字正文；六页基线和前台不变。消息队列20、已处理0、智联未解决23，尚无当前智联模型草稿或任何发送。主控隔离服务已重启，安装版不变。以下旧阶段回执保留当时事实。

## 用户变化与边界

- 已批准方向：消息待办采用同一左侧紧凑列表、右侧单条详情；智联消息缺少本地岗位资料时读取对应真实 JD 并复用现有分析和本地草稿；分析结果可进入现有独立沟通清单，发起普通初次打招呼。
- 真实验收由助手操作现有简历的隔离副本，停在回复草稿保存、未发送。普通初次沟通的本轮授权不包含回复发送、简历同意/拒绝、投递或申请。
- 不推送、合并、打包、发布、改版本或替换安装版。智联单岗位技术验收不继承 BOSS 的已验收结论。

## 历史可核对检查点（078b8fc阶段）

| 部分 | 当前状态 | 证据 |
|---|---|---|
| 统一消息布局 | 任务复核和本地浏览器回归完成；待阶段最终复核与真实验收 | `622e0ce`、`767c22b`、`e975a92`；整组草稿稳定保存后才切换。主控在 `17cc3d4` 新鲜严格旅程退出码 0 |
| 消息岗位资料补全 | 实现、定向回归和独立任务复核完成；真实验收待完成 | `ab7a246` 后的两项重要问题由 `5664310` 修复；补充回归通过，复核确认两项关闭且无新增重要问题 |
| 新版搜索结构与准备页等待 | 实现和独立任务复核完成；真实 prepare/save/start 待验收 | `5ffabe7` 通过 4 项定向检查；`6163af6` 补齐页面消失、严格清理归属、祖先隐藏 loader 与取消来源，新增失败回归后两项覆盖检查、语法和差异检查通过；四项复核问题关闭，无新增重要问题 |
| 智联普通初次沟通 | 来源控制、浏览器适配及产品入口全部完成任务复核；待最终阶段验证 | 底层 `d67e4ec`、`8c13026`；适配器 `9108e0c`；产品 `f975872`、`3b1d76b`、`d7e7340`；仅开放 `dom_verified / e2e_pending` 单岗位技术验收，未宣称正式通过 |
| 验收前代码完整门禁 | 157/157、退出码 0 | `078b8fc579a50b3f81e1f7cb1528f693867ee7a5` 新鲜严格完整运行，起止 SHA 相同、工作树干净；包含09e131e，不自动覆盖后续DOM修复 |
| 当前真实端到端 | 已开始，尚未通过 | 准备/保存条件成功；旧BOSS复用已消除，搜索在地址层级核对被拒。消息读到20条，但独立JD页正常账户区误判导致零草稿。新计划集中兼容实际Vue2/地址层级/详情页头及持续错误提示。尚未发起任何普通招呼、回复、简历处理或投递 |

## 078b8fc之后的真实DOM验收缺陷

完整门禁记录在仓库外 `full-gate-078b8fc-20260909.log`。主控通过普通UI启动一次，`95972154-38` 记录筛选恢复超时；保存的关键词/原生条件一致，实际原因是“北京 朝阳 建外”与“北京·朝阳区”的不同颗粒度。搜索和最后沟通guard仅读Vue3，而当前真实页面的Vue2能提供与链接完全一致的四个岗位ID。启动错误又被下一次就绪刷新覆盖。随后独立消息流程读取20条，增加一条待处理消息，但补JD时又被详情页不同的正常账户页头误判登录。主控用原详情reader及既有共享pacing/access再次窄读，证明Python JD有标题和完整正文、无实际登录面板，并记录不含账户的标题截图；所有临时页已关闭、活动标签未改变。

当前限定任务 `2026-09-09-zhaopin-live-dom-compatibility.md` 只处理上述根因，不改筛选方案/扫描限额/数据库/模型/依赖，不扩展发送授权。其新验证顺序是定向RED/GREEN与任务复核后先只读实站确认真实结构，再在源码稳定时跑新完整检查，之后才可至多一个普通初次招呼。旧078b8fc门禁不能证明这些新改动。主控已停止自己管理的PID16456/8788，正式安装版不变。实际计数仍为智联零岗位/草稿/沟通批次、旧BOSS不变，智联未解决21→22；没有回复发送、投递或简历操作。

## 全阶段复核后的集中修复

独立综合复核覆盖 `02535404a93e7c4d38bc2aa070699beb3b7f74cd..e2ed097dff0096b562bb6bbe32f947f9f084945d`，发现六项 Important（两项 P1、四项 P2），没有 Critical。`2d88e700bfb567feadf119a8eb51ee9c0dc0ddf4` 在一次集中修复中处理：

- 最后一次身份读取等待结束后再检查取消，防止失去执行权后继续点击。
- 存在未完成的沟通/投递观察请求时不能提前认定成功；缺失观察状态不能当成没有待确认请求。
- 观察器刚启动便记录清理责任，准备失败与首次清理失败后仍可重试释放；无效准备不能用于派发。
- 回复模型结束后，在现有提交事务中重新核对捕获方案及版本、资料、会话和岗位观察归属；失效时保留待处理消息，不写入草稿或已处理标记。
- 已确认没有搜索结果时正常完成等待；未加载完成或非空岗位仍保持原有就绪与身份检查。
- 普通离页复用消息切换的稳定保存流程，保存等待期间继续输入也会保存最新版；失败留在原页。

每项均先复现失败，再通过同一覆盖检查；另补同编号方案内容修改的失败回归。最终十项定向检查（ZL communication adapter、browser transport、communication executor、ZL message job context、ZL message discovery、message discovery job context、message discovery、ZL readonly、严格 unified messages journey、dashboard message discovery）、全部十个修改文件语法及差异检查退出码均为 0。集中检查发现两处旧测试预期漂移，已在修复基线代码上复现后仅同步预期：岗位投影既有 `availability: unknown`，以及智联已开放的唯一“发送记录”导航；不以修改产品行为迁就测试。

以上均为本地假浏览器、合成页面和临时数据库证据，不是发送成功或当前实站空结果结构的证明。唯一一次针对 `e2ed097..2d88e70` 的修复复查确认六项全部关闭，未发现修复引入的 Critical/Important，也没有范围外观察项；未重复运行实现者测试。当前完整门禁和真实验收仍未运行。

## 完整门禁首次运行及旧断言维护

主控在干净的 `465a456bceba24390d97b48fa7c1eb4bfc31fe01` 从头运行严格 `npm test`，退出码 1，起止 SHA 一致、工作树仍干净；停在 `dashboard_information_architecture_smoke` 的旧导航断言。后半诊断首先在 `dashboard_shell_smoke` 复现同一旧断言。日志分别为仓库外 `full-gate-465a456-20260909.log` 和 `remainder-diagnostic-after-465a456-20260909.log`；这次失败不算完整通过。

已批准且经任务复核的 `3b1d76b` 将选岗页面的“发送记录”从错误的选岗自链接改为记录中心，只有记录中心标为当前页面；两份测试仍要求旧自链接及高亮。`988bceaa1b400ba9df712cf788ff10679deb66ed` 仅同步这两份测试，并保留其他导航断言、补验中心精确批次/平台参数。信息架构、Dashboard 框架和智联沟通三项检查及语法/差异检查通过；主控核对差异确认没有产品变更。这是旧测试维护，不是重新开启已关闭的六项修复。后半诊断只用于尽早发现剩余问题，不能代替随后静态提交上的完整重跑。

## 新鲜定向验证

消息岗位补全恢复验证起点 `ab7a2469af279e924bd022ffd56d6a7f92a84d4c`，终点 `44010da6ad47c12e271ac8e898c157d46aa7448f`；期间只有两份交接文档变化，`src/` 和 `tests/` 不变。以下检查全部退出码 0：

- `zhaopin_message_reader_smoke`
- `zhaopin_message_detail_reader_smoke`
- `zhaopin_message_job_context_smoke`
- `zhaopin_message_discovery_smoke`
- `message_discovery_job_context_smoke`
- `dashboard_message_discovery_smoke`
- `dashboard_unified_messages_journey`

使用严格 Playwright 环境及 D 盘临时资料；保留已知 Node SQLite ExperimentalWarning。`git diff --check` 退出码 0。原实现者没有留下修复前失败记录，无法恢复历史 RED 证据，因此这里只主张当前回归通过，不虚构原始红绿过程。后续修复如有发生，须追加覆盖当前代码的验证。

独立复核随后复现两项缺陷：正确编号但标题/公司尚空的骨架页立即被误判为错岗位；详情层的 `ZHAOPIN_MESSAGE_RISK_CONTROL` 未被控制器识别，停止当轮却没有保存后续智联阻断。`5664310c1048badf2dc7f1c13e7701ec0d087168` 先补真实失败回归，再分别允许缺失字段等待、统一识别两种实际智联风险码。`zhaopin_message_detail_reader_smoke`、`zhaopin_message_discovery_smoke`、`dashboard_message_discovery_smoke` 和 `git diff --check` 均退出码 0。修复复核确认两项均已处理，没有新增 Critical/Important；普通加载超时不会进入风险冷却，BOSS 状态不变。

## 实际环境与资料保护

产品接线 `f975872e82dc50fc0b84d609c62fdd1c93b7b5d3` 的真实 HTTP/临时 DB 回归先复现智联 builder 混入 BOSS，以及 CLI 未按冻结来源选适配器；实现后 8 项定向检查全部退出码 0（dashboard communication batch、CLI authority、communication application、workflow communication、ZL workflow、ZL dashboard 含严格 Playwright、ZL communication storage、新 ZL communication HTTP）。独立审查发现失锁后的页面恢复、默认岗位记录缺少下一批入口，以及智联共用错误仍指向 BOSS 三项问题。`3b1d76b` 先补失败回归，再修正入口/文案和两平台取消恢复，5 项覆盖检查通过；内层读取的取消空隙由 `d7e7340317b7c9015a39230980f9136cf750a56f` 通过真实基础 reader 的失败回归关闭，adapter、readonly、CLI 三项检查及语法/差异检查通过。最终定向复核确认三项全部关闭，无新增 Critical/Important。BOSS 正常行为保持，仅新增失去共享执行锁时的清理后停止；没有修改选择器、节奏数值或身份/结果证明规则。

以下底层任务的 `executionEnabled:false` 是其当时的中间检查点；当前产品仅将智联技术执行开关开启，仍保留 `e2e_pending` 和单岗位约束。所有本节测试均使用合成平台、假浏览器和临时数据库，不是当前真实验收副本上的假成功。

普通沟通浏览器适配 `bf407876be73987ebce9e73a7c4948cd4b08f26f` 的 6 项定向检查（adapter、browser transport、message reader、readonly search、communication executor、CLI authority）全部退出码 0，8 项 JS 语法和差异检查通过。独立审查提出 5 项重要问题：焦点释放过早、IM 成功后不能继续下一项、等待网络时不独立检查风险、损坏证据误分类，以及不应缩小 BOSS 既有方法范围；`44555a0` 补齐逐项失败回归并修复，adapter/transport 检查通过。该修复又暴露一次清理失败后资源引用丢失的问题；`9108e0c1ee6032856ebecf301d64972b3c2fcdab` 保留未释放资源、确保结束操作，并通过真实调用计数的合成恢复回归。最终差异复核确认原 5 项和清理恢复问题全部关闭，无新增 Critical/Important。以上均为本地合成验证，未运行本阶段完整门禁或真实发送。

普通沟通存储/控制任务在 `d67e4ec` 完成 6 项新鲜定向检查：`zhaopin_communication_storage_smoke`、`communication_batch_storage_smoke`、`communication_application_smoke`、`communication_runtime_smoke`、`communication_calibration_gate_smoke`、`communication_executor_smoke`，均退出码 0。独立复核发现“同计划、不同用户资料的观察记录”可过晚才被拒绝；`8c130267bbf877da192996b309e9fd0cfbcea8cb` 新增失败回归后，提前在创建事务核对观察记录的资料/计划/来源，拒绝时不产生批次。3 项覆盖回归、语法和差异检查通过，修复复核无新增重要问题。BOSS 行为不变，智联仍为 `dom_verified / e2e_pending / executionEnabled:false`。

用户重新打开页面后，主控于 12:51 UTC 只读查看已加载 `job-apply` 客户端。调用模块引用环境模块 `36040` 与请求模块 `69508`，确认普通 prechat 为 GET `https://cgate.zhaopin.com/imapi/imV2/createAndUpdateContextV2`，application 为 POST `https://fe-api.zhaopin.com/c/pc/alan/jobs/application`；后者仅作为停止信号，绝不执行。资源 SHA256 为 `211af7a360cd141e27a6931f17e7dc639b9c89dcba42001af2853f8a3da6ce94`，浏览器拦截器不改写这些绝对 URL。证据在 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/client-origin-evidence.md`。没有点击、导航、新标签或私有 API 请求，前后台标签身份保持不变；这是代码依据，不是发送成功证据。

- 开发工作树：`D:/DevData/RoleFlow-worktrees/zhaopin-readonly`，分支 `codex/zhaopin-readonly`。
- 隔离验收数据：`D:/DevData/RoleFlow-unified-message-acceptance-20260908`，不是用户当前安装版或空白新用户资料。
- 验收前计数快照：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/acceptance-baseline.md`。含 BOSS 历史成功及未确认记录，不自动重试、改写，也不作为当前质量或成功证据。
- 当前检查时旧 8788 验收服务未运行。主控在代码冻结后自行启动和管理服务，不依赖用户手动启动。
- 真实页面仅由主控读取补证；实现与回归使用合成页面、假浏览器和临时数据库。原始页面证据保留仓库外，不把用户消息正文或凭证加入测试。

## 剩余交付门槛

主控最终完整重跑日志 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-95cb094-20260909.log` 末行为 `All 157 offline checks passed.`，退出码 0，起止为 `95cb094c78f9acc29b0da5ba50266d635d950742`。之后从真实产品 UI 准备并保存智联搜索条件成功，但开始按钮错误返回旧 BOSS 中断任务；独立只读追踪确认应用层和服务端提前复用查询遗漏 site。消息发现又把已登录账户页头的 login 类名误判为登录挑战；真实 DOM 有正常账户区域和 20 个会话，没有登录面板。旧记录计数不变，未重试旧沟通或发送新消息。两项是本次真实验收新发现，不是旧综合审查六项残留；按 `docs/superpowers/plans/2026-09-09-zhaopin-live-acceptance-fixes.md` 最小修复后再验证继续，不将 157 项结果扩写为实站通过。

该限定修复由 `09e131e5a431dc6af220f94c3b2d9168de500902` 完成：仅三个既有源文件传递平台参数/排除精确普通账户区域，三个覆盖测试补回归。先实际复现应用层参数 undefined、真实 HTTP/浏览器旅程复用旧 BOSS、正常账户页头 LOGIN_REQUIRED；最终 workflow application、严格 ZL Dashboard 旅程、ZL message reader、ZL message discovery 四项检查，以及六文件语法/差异检查退出码 0。独立任务复核确认计划符合和质量通过，无新增重要问题；同平台复用、旧 BOSS 不变、真正登录挑战和页头父级边界均有覆盖。没有改存储全平台查询默认或全方案重新评分保护。旧 157 项不能覆盖这次源代码变化，主控将另跑冻结树完整验证；当前 8788 服务已暂停，桥接与平台页面保留。

实现任务、全阶段综合复核及唯一集中修复复查已经完成，不重做搜索兼容或普通沟通三项任务。剩余为新鲜完整门禁；再从产品页面实际完成找岗、分析、单岗位普通打招呼、消息发现、生成和保存草稿。记录精确目标、实际动作数量、历史回复与本轮新沟通的区别，以及刷新/再次进入后的保留结果。最后交付用户最终验收，不把小样本或未发送回复扩写为所有平台功能验收完成。
