# 智联只读找岗首版：实现与验收记录

状态：**智联只读首版开发与复审完成，最终功能提交的 149 项完整检查通过，文档提交后 12 项关键复验通过**。保留隔离开发分支，尚未替换安装版；最终交付 SHA 和回执位置见下文。

## 范围与环境

- 用户已批准的设计：`../specs/2026-09-06-zhaopin-readonly-design.md`；实施顺序：`../plans/2026-09-06-zhaopin-readonly.md`。
- 工作树 `D:\DevData\RoleFlow-worktrees\zhaopin-readonly`，分支 `codex/zhaopin-readonly`，起点 `15dc54867921ab3f621aa14a1070f7df180a3954`。版本继续为 1.3.2，不替换安装版。
- 普通代码检查使用临时 SQLite、合成资料、假浏览器及本机 headless Edge；复用现有 D 盘依赖，没有新增运行时依赖。实际探查范围与限制在 `2026-09-06-multi-platform-discovery-research.md`。
- 没有推送、合并、打包、发布或真实投递/沟通授权；没有用用户真实数据库做开发验证。

## 开发前基线

初次完整检查停在 PDF 自检：新工作树没有可供 ESM 解析的依赖目录，单独设置 NODE_PATH 不解决 ESM 导入。将本工作树 node_modules 联接至已有 `D:\Guo\ZhiPing\node_modules` 后，直接导入和新鲜严格完整检查通过；没有修改产品或升级依赖。

在仅有设计文档改动的 `a8f93ee`，完整检查退出码 0，实际末行为 `All 144 offline checks passed.`。日志为 `D:\DevData\RoleFlow-multiplatform-research-20260906\baseline-offline-ready.log`；初次失败日志也保留。该证据不覆盖后续功能代码。

## 分段实现与复审

### 原生读取契约

`d6b5beb` 新增智联模板、岗位链接校验、同页卡片和完整 JD 读取器及脱敏 fixture。实际岗位编号取自当前详情链接，不用标题或临时卡片索引作永久身份；发布方与客户公司分列。

独立审查发现详情访问额度在点击之后申请。`a239abf` 改为动作前申请，并在等待结束后重新检查取消、标签和卡片签名；新增“额度拒绝”和“等待期间卡片变化”回归。`zhaopin_readonly_smoke`、`source_acquisition_smoke`、`inherited_search_scope_smoke` 通过；修复复审通过。

### 平台条件、数据与分析隔离

`3b74233` 实现按方案和平台分别保存原生条件；迁移 v29 为历史本轮回填 BOSS 来源，并以平台区分轮次唯一键；同一 SQLite 内 BOSS/智联浏览器租约互斥。智联继承范围和目标不经过 BOSS 城市或薪资分档映射，活跃度缺失不按 BOSS 未核实处理。可选客户公司字段参与新岗位内容与分析事实，没有该字段的旧 BOSS hash 和模型输入形状保留。

十项定向检查记录：`platform_search_context_smoke`、`zhaopin_analysis_smoke`、`zhaopin_readonly_smoke`、`inherited_search_scope_smoke`、`workflow_acquisition_smoke`、`scan_snapshot_smoke`、`scoring_url_smoke`、`screening_preferences_smoke`、`job_store_contract_smoke`、`storage_migration_smoke`。实现者报告均通过，退出码 0，只有现有 SQLite 实验性警告；独立复审结合 diff 核验，未将警告描述为无噪声输出。

复审及修复 `29fbc93`、`8ff6d01`：

- 年薪 `20-30万/年`、时薪 `150-200元/时` / `元/小时` 和天薪不再误当月薪。
- `1万-1.5万/月` 完整得到 10–15K；保留 `1.5-1.6万·13薪` 和 `10-20K·13薪`。
- 遮蔽金额不退化为部分确定值，覆盖 `10K-**K`、`**-15K`、`10K-20**K`。
- 新增实际 v28 父表结构及非空 workflow/task/analysis-attempt 的合成数据库，迁移后逐字段比较记录并检查外键；不拿历史空 attempts 场景代替数据保留验证。

最后一轮修复的三项薪资/资格相关检查通过；主控随后也实际重跑 `zhaopin_analysis_smoke`、`scoring_url_smoke`、`screening_preferences_smoke`，退出码 0。两轮 scoped 复审后没有剩余重要问题。运行链路中的调用方平台隔离、严格条件保存和不进入沟通/漏斗仍需在后续集成验收中验证。

### 实际运行与页面

`9048a7c` 已接入现有工作流、CLI、扫描检查点、恢复及分析；经 `d97f4f0` 修复后独立复审通过，尚不能当作完整产品交付。运行来源和原生条件随本轮冻结，扫描与分析使用同一实际来源；智联不使用 BOSS 沟通库存或成功数量规划，只读目标全部完成且分析队列清空后结束。尚有未完成目标时，即使当前分析队列为空，也保留继续入口。

同一小详情额度的恢复曾实际复现重复前缀，保存数量仍为 1 而不是预期的 2。修复复用既有完整详情缓存：现场核对真实岗位编号和完整内容后，未变化的结果记录为复用；新岗位或 JD/客户公司变化消耗新的 JD 额度。每次实际核验仍在动作前申请站点访问额度并计入节奏，达到新 JD 额度后不再访问下一岗。真实文件 SQLite 关闭重开、相同冻结额度的回归已得到保存数量 1→2→3，并验证复用访问次数；没有新建签名到岗位编号的猜测表。

同时补齐方案重检查保留客户公司、不同来源原生条件不串用、查询先按来源筛选再限制数量、智联自己的风险冷却，以及 BOSS 库存不回写智联运行。旧 Dashboard 回归曾发现 BOSS 启动就绪检查顺序改变；恢复实际调用顺序后原测试通过，没有更改旧预期。

实现者在该代码上报告十项最终定向检查退出码 0：`zhaopin_workflow_smoke`、`communication_runtime_smoke`、`workflow_inventory_smoke`、`workflow_control_smoke`、`workflow_recovery_smoke`、`dashboard_runtime_smoke`、`workflow_acquisition_smoke`、`workflow_end_to_end_smoke`、`scan_end_to_end_recovery_smoke`、`communication_cli_authority_smoke`。新检查走真实 CLI/HTTP/SQLite、假平台和测试模型；这不是实站或真实模型端到端证明。主控早先实际运行过新工作流检查，但那次运行不覆盖最后几处修复，不替代最终门禁。

运行链路独立复审发现一项重要缺口：CLI 已按智联读取共享节奏，但 `checkpointSharedPacing` 仍只保存 BOSS，因此同批次恢复正常，新一轮却丢失累计节奏。`d97f4f0` 补齐明确支持站点的实际来源写入。新增真实 SQLite/CLI 回归先保存 15 次累计状态，再启动独立新批次读取 3 岗，验证累计为 18，在第 16 次请求既有 90 秒阶段冷却（测试替身不实际等待），下一阈值为 32，BOSS 状态及时间戳不变；修复前实际失败是 `3 !== 18`。

修复后的 `zhaopin_workflow_smoke`、`boss_safe_pacing_smoke`、`site_access_usage_smoke`、`scan_store_contract_smoke` 均退出 0；限定修复范围的独立复审确认已解决且无新重要问题。沿用旧节奏实现的控制台日志前缀仍是 `[boss]`，实际访问和存储来源为智联；这不是 BOSS 外部访问证据。

### 今日任务、来源结果与只读边界

`27b08f74ab2e3c3ac949c2edb810e87e2f3bbb28` 已接入今日任务的原生平台选择、准备搜索页和保存当前条件。首次进入智联先准备带方案关键词的搜索页；复用已有页且保留原生条件，拒绝跨窗搜索页和未知条件。选择智联不改写旧方案的 BOSS 生成模式、活跃度等值；状态栏和冻结本轮恢复使用实际来源。

岗位列表的来源筛选、最近批次和 CSV 导出保持一致；混合历史逐岗标识来源，但不改变正在执行的来源。智联卡片和本地报告显示完整 JD、分析及用人公司/发布方，不显示招呼语、沟通或投递动作。报告标题和文件名前缀只接受已知来源，空智联批次不会显示成 BOSS。

服务端批次资格同时要求 BOSS 来源和 BOSS 岗位链接；漏斗入账拒绝非 BOSS 岗位。真实 HTTP/SQLite 回归覆盖伪造的批次、已投及已发送进度请求。拒绝后岗位状态、事件、漏斗样本一起回滚，服务仍可响应；不只靠隐藏按钮保护只读范围。

精确提交上的本地用户旅程实际打开 headless Edge 和 Dashboard，使用临时 SQLite，通过按钮依次完成：选智联 → 准备并保存搜索条件 → 开始扫描 → 第二个分析期间暂停 → 关闭并重开服务和数据库 → 继续同一本轮 → 查看来源结果。执行真实 CLI 扫描、分析调度与检查点；平台传输和分析器是合成替身，子进程启动通过同进程测试接缝执行，不是实站或真实模型效果证据。最终 3 个完整 JD 和 3 条完成分析，分析调用总计 3 次，已保存分析未重复；页面错误与外部请求均为 0。

记录与十张截图：`D:\DevData\RoleFlow-multiplatform-research-20260906\task4-27b08f7`，本轮编号 `58fde483-c5ea-4c87-b3c0-4da6b3f58d6a`。1440 和 1024 宽度的首次、保存、暂停、完成、结果页面均无横向溢出，390 宽度做了结果页宽度检查。主控已实际查看精确提交的完成页和结果页：六格概览均衡，导出选择紧凑，无重复的红色通用风险占位。未扩大既有桌面视觉风格。

实现者在该 SHA 上报告新检查和 11 项旧回归退出码 0；其中旧 today 回归的单独命令未提供 Playwright 路径，布局段跳过，专用智联旅程则强制执行了 Edge。主控同一 SHA 的首次严格完整检查退出 1：`dashboard_shell_smoke.js:280` 要求默认 BOSS 继续请求 `/api/runtime-status`，新代码不必要地添加了 `?site=boss`。服务端接受两种格式，证据是兼容契约和门禁失败，不是已证明 BOSS 实际功能损坏。日志 `task4-27b08f7-fullgate.log` 保留；之前通过的安装暂存、智联页面旅程、存储、恢复和沟通检查不能替代整套通过。

Task 4 独立复审同时发现 CSV 导出丢失来源/用人公司、智联过期方案的重新确认链接丢失平台选择；暂停与提前开始提示还残留 BOSS 名称。`aacbb97f3698ff1a2af0c0666694cc840e247540` 修复这些可达问题：CSV 保留原 19 列并追加三列，新字段沿用公式转义；过期方案的真实点击保留智联；默认 BOSS 请求和恢复消息体保持旧契约，仅智联传入来源。

修复前分别实际复现 CSV 新字段为 undefined、真实页面点击后 site=null、旧状态请求断言失败及智联提示中含 BOSS。修复提交后的 `job_export_smoke`、`dashboard_job_export_smoke`、`dashboard_shell_smoke`、`today_dashboard_smoke`、`workflow_dashboard_smoke`、`dashboard_zhaopin_smoke` 六项全部退出 0；这次 Today 布局与智联旅程都提供 Playwright 并强制执行，没有布局跳过。实际 CSV 内容核对覆盖智联和混合来源，不只检查表头。精确修复证据目录为 `task4-aacbb97`，主控另以 `task4-aacbb97-fullgate` 保存新一轮整套运行证据。

限定修复范围的独立复审确认三项重要问题及一项文案问题均已解决，没有新问题；四个实施任务至此均通过分段审查。整个分支的独立审查和最终整套门禁另行记录，不以分段审查替代。

`aacbb97` 上第二次严格完整检查在 `outcome_analytics_dashboard_smoke.js:322` 退出 1。定位不是面板布局改变：旧 fixture 将岗位来源写成 `outcome-analytics-smoke`，新默认 BOSS 列表正确只接受 `boss`，所以岗位和重试按钮不显示。应将测试资料改成真实平台标识并保留原统计、隐私和位置断言，不能为旧 fixture 放宽产品来源隔离。其后 21 项检查单独续跑全部退出 0，日志 `aacbb97-remaining-diagnostic.log`；这只是收集后续失败的诊断，不拼接成完整门禁通过。最终分支审查及统一修复仍待完成。

### 全分支复审：必须解决的跨模块问题

对 `15dc548..aacbb97` 的独立全分支审查当时发现四项重要问题，没有 Critical；当时版本没有放行，随后统一修复：

- CLI 先按冻结原始 BOSS 通用模式派生配置，再改为智联继承模式，已派生的城市、经验和职位类型没有消除。北京/3–5 年的 BOSS 条件会将广州/经验不限的智联岗位错误拦截；分析恢复提前进入分支也需覆盖。修复必须由实际来源和冻结有效模式决定派生，不改写用户保存的 BOSS 方案。
- 真实 DOM 的“客户公司”分支将发布方置空，最终详情没有从已核验卡片补回。直接构造双公司对象的 UI 测试不能证明实际采集保留字段，需覆盖真实 DOM fixture 到详情、存储和分析事实。
- 详情轮询在第二次尝试后忽略加载标记，可能把尚未完成更新的正文采给新编号。必须等待真实就绪，超时保留目标待继续，不降低完整 JD 标准。
- 只有“准备搜索页”检查同窗；保存、启动、仍需采集的恢复和逐项绑定可绕过。需复用同窗归属检查，标签移动到另一窗口时保存进度并停止，不激活页面或新增浏览器调度体系。已经存有完整 JD、仅剩本地分析的恢复不访问网页，应继续允许浏览器关闭或移窗；不能为此新增无关前置阻塞。

这些是实际可达路径与合成探针发现，不是实站已经造成错误的声明；与旧 outcome fixture 来源修正一起交给一个修复任务。旧 `[boss]` 节奏日志和 SQLite 警告已披露，为非阻塞项。

统一修复提交 `2ef0b0a42ced869f5786ebc2b9f6ddfbd4171aac`（五个源码、五个测试文件）：

- 在派生配置之前传入冻结实际来源/模式，分析恢复先应用冻结平台策略，混合记录按各自来源重检查。真实本地旅程的 BOSS 方案故意设为北京、3–5 年、实习，而智联岗位为广州全职；修复前规则拦截导致第二次分析无法到达，修复后正常扫描与仅分析恢复均完成，原 BOSS 设置不变。混合重检查中 BOSS 仍保留自己的城市限制。
- 身份和选中卡片核验后，详情发布方缺失时从该卡片补回。真实 HTML fixture → 读取器 → SQLite → 分析事实均验证发布方与客户公司分开保留。
- 所有详情轮询都要求加载完成；持续加载与加载期间停止的回归验证首岗不丢、第二岗不落错正文，目标和扫描保持部分完成。继续沿用有上限等待。
- 复用同窗校验到准备、保存、启动、仍有采集任务的两种恢复入口和逐项绑定。CDP 与 Edge 已有接口实际提供窗口编号，不靠仅测试中虚构的新能力。跨窗恢复不启动子进程，保留暂停和已有岗位；真实本地分析恢复则在页面移窗后继续完成，没有给纯分析增加浏览器前置条件。
- outcome 只将合成岗位来源改为 `boss`，原统计、隐私诱饵和位置断言保留。

修复代码提交前，25 项相关回归串行全部退出 0；其中包括实际本地 UI/CLI/SQLite 旅程，3 条完整分析、总分析调用 3 次、页面错误与外部请求为 0。预提交证据 `finalfix-precommit` 不冒充精确提交证据；主控在静态 `2ef0b0a` 另跑完整门禁，日志和页面证据分别保存为 `finalfix-2ef0b0a-fullgate.log`、`finalfix-2ef0b0a-fullgate`，同时进行唯一一次限定修复范围的复审。

该精确提交的严格完整 `npm test` 已退出 0，实际末行为 `All 149 offline checks passed.`。复审确认原四项及 outcome fixture 均解决，但发现同窗修复新增一项误拦截：将当前工作台路由限定为 `/`、`/plan`、`/workflow`，用户仅切到同窗的岗位记录或设置页也会中断采集。因此没有以 149 项通过代替产品审查放行；另作一次有界修正，取消用页面路径判断持续窗口归属，保留窗口编号校验和实际跨窗停止。修正后仍须新的精确提交门禁与复核。

最后的有界修正为 `c27b2dfd2028427339af67c34a43b041ea524207`：生产代码只移除一处路径限制。真实读取器扫描期间，工作台依次切换到岗位、设置和初次设置页；实际 HTTP 准备/保存也经过这些路由，均在改代码前稳定复现误拦截。修正后八项相关检查通过，真实跨窗、缺失窗口和纯本地分析恢复的既有检查保持。沿用原本 localhost/127.0.0.1 的工作台识别，不声称新增了严格应用来源鉴权。主控在该静态提交重新运行整套检查，并安排仅针对这一修正的独立复核。

该提交的本地用户旅程已经实际完成，证据目录为 `D:\DevData\RoleFlow-multiplatform-research-20260906\c27b2df-fullgate`：同一本轮暂停、关闭并重开服务/数据库后继续，最终 3 条完整分析、总分析调用 3 次、页面错误和外部请求为 0。主控实际查看首次进入 1440、完成页 1024 和结果页 1440 的截图。没有开启新的真实平台探查。

最终结果：`c27b2df` 上重新运行的严格完整 `npm test` 已退出 0，实际末行为 `All 149 offline checks passed.`，日志为 `c27b2df-fullgate.log`，没有报告跳过项。独立最小复核确认最后的页面切换问题已解决，没有新 Critical/Important；至此全部本范围审查问题关闭。保留 SQLite 实验性警告、Git 测试资料的警告与有意失败输出，不将输出描述为无警告。

## 主要修改位置

- 平台读取：`src/adapters/sites/zhaopin.js`、`src/core/zhaopin_search_scope.js`，以及既有适配器注册入口。
- 条件与数据：`src/storage/platform_search_context_store.js`、`src/core/storage.js` 和现有岗位/轮次/扫描存储；原生条件按方案及平台保存，v29 迁移保留旧数据。
- 扫描与分析：`src/cli.js`、`src/application/workflow/index.js`、`src/core/platform_runtime_policy.js` 及现有工作流、搜索快照、分析模块。实际来源先于规则配置派生，继续复用原简历分析链路，不新建服务或分析系统。
- 页面与导出：`src/dashboard/server.js`、今日任务/本轮页面及状态脚本、`src/reports/render.js`、`src/core/job_export.js`。沿用原生控件和现有桌面布局，没有换 UI 框架或主题。
- 只读边界：`src/storage/communication_store.js`、`src/storage/funnel_store.js`，排除智联进入 BOSS 沟通和反馈。
- 新增五项离线检查：`zhaopin_readonly_smoke`、`platform_search_context_smoke`、`zhaopin_analysis_smoke`、`zhaopin_workflow_smoke`、`dashboard_zhaopin_smoke`，并补充旧迁移、导出和 BOSS 兼容回归；详细改动以本分支 Git diff 为准。

## 开发版使用顺序

1. 在“今日任务”选择“智联”，点击“准备智联搜索页”。
2. 到已打开的智联原生搜索页设置城市等条件，回到 RoleFlow 点击“保存智联条件”。平台搜索范围分别保留，简历和本地偏好继续共用。使用当前已配置的浏览器模式；若该浏览器尚未登录智联，需要人工登录，不会自动切换到另一套登录身份。
3. 点击“开始一轮岗位发现”，后台读取完整 JD 并分析。需要时可“暂停本轮”，重开后继续同一轮；“结束本轮”保留已存结果，但该轮不再继续。
4. 完成后点击“查看智联结果”，查看完整 JD、分析或导出结果。下一轮回到今日任务开始；本版不投递、不沟通、不读取智联消息、不增加投递统计。

这是隔离开发版的入口，不是当前已安装 v1.3.2 的新增能力。

## 最终门禁与未验证前提

- 最终功能提交 `c27b2dfd2028427339af67c34a43b041ea524207`：严格完整 `npm test` 为 **149/149、退出码 0**，没有沿用修复前的旧 149 项结果。
- 新平台本地用户流程与十张桌面截图：该精确提交已重跑并查看，见 `c27b2df-fullgate/receipt.json` 及相邻截图；结果不是实站或真实模型验收。
- 全分支审查与所有修复复核：通过，没有未关闭 Critical/Important。按 subagent-driven-development 的收尾要求尝试清理本计划临时目录时，执行环境在运行前拒绝了删除命令，因此没有删除任何文件，也没有换方法绕过。28 份材料已另存到 D 盘证据目录的 `review-archive-c27b2df`，逐文件 SHA-256 校验一致；原目录仍保留，这是非阻塞的内部整理事项。
- 危险测试资料检查已完成，见外部 `c27b2df-fixture-audit.md`。完整门禁执行过实际安装暂存检查（StageOnly），没有编译、安装或发布安装器。
- 文档提交 `b28e9cc0a0075d060424ca0c1b79edb53e531d55` 上 12 项关键复验退出 0：五项新增智联检查，以及 storage_migration、job_export、dashboard_shell、workflow_control、workflow_recovery、communication_runtime、outcome_analytics_dashboard。强制本地浏览器验收，没有跳过；日志与截图分别为 `b28e9cc-final-focused.log`、`b28e9cc-final-focused`。源码、测试、脚本及依赖清单与完整门禁的 `c27b2df` 相同。
- 此后只提交本计划与报告的验收记录更新。最终交付 SHA、再次复验与干净工作树检查保存在 `D:\DevData\RoleFlow-multiplatform-research-20260906\final-verification.json`，避免把文档自己的未知提交号写进提交。没有将只读结构探查或合成分析结论当作真实产品质量证明。
- 真实智联产品端到端、真实模型质量、长期平台稳定性和真实投递/消息：未验证；实时 DOM 探查不能替代这些结果。
- 未访问真实 BOSS；没有向招聘方发送、投递、同意或拒绝简历操作。临时探查页已按证据回执关闭，用户原有页保留。
- 非阻塞限制：沿用已有节奏实现的控制台 `[boss]` 前缀仍存在，实际智联事件/额度按自身平台存储；未知原生筛选条件会提示重设，尚未声称覆盖所有平台条件组合。
- 下一入口：让用户查看隔离开发版页面，并按明确范围进行真实智联只读验收；需要推送、合并、安装或真实外部写时单独取得授权。之后才进入智联投递/消息/反馈和 51job，不自动扩大本阶段。
