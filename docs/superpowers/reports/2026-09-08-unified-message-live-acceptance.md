# 统一消息页：助手只读验收与现场修复

状态：三项现场误判已修复并通过独立复审；助手已通过真实浏览器读出 21 条会话的原文和请求卡，并验证来源筛选、刷新/重启保留、安全停止。新鲜严格完整门禁 152 项通过；后续仅修正统计标签，两项页面复验通过。验收副本没有智联完整岗位缓存，真实岗位分析/模型草稿尚未达到；不宣称整条回复链验收通过。

## 现场结论

1. 现有 Edge 智联消息页实际为 HTTPS 的 `i.zhaopin.com/im`，含 `refcode`、`sessionId` 查询参数。后台只读 DOM 核验有 21 条会话、正文区域和标题区域，`document.hasFocus()` 为 false。旧公共读取器却返回 `ZHAOPIN_MESSAGE_TAB_MISSING`：平台清单、标签解析、快照及选择会话检查都要求完整 URL 等于无参数地址。
2. 当前发现 API 返回本轮未解决 0，两个来源未连接；页面却显示未解决 3 和 BOSS 城市不匹配提示。只读数据库核验这三条来自 8 月 25/30 日的旧 BOSS 记录。页面用持久待办覆盖了当前轮次的状态、数量和原因，并非本轮发现了三条智联错误。
3. 验收数据里有 160 个 BOSS 岗位、没有智联岗位。当前实站验收可以验证读取原文、请求卡待办、来源隔离及持久保留；没有完整可信智联岗位缓存时不能宣称真实岗位分析或模型草稿链通过，也不能使用 BOSS 同名岗位代替。
4. 在修复提交 `9e24f7c` 上重新启动服务，通过本地“开始只读发现”实读后，智联已被识别，当前未解决 0，不再被旧 BOSS 原因覆盖；但读取被 `ZHAOPIN_MESSAGE_LOGIN_REQUIRED` 阻止。真实 DOM 命中的仅是 `.home-header__right` 内“我要招人”和“登录/注册”两个普通链接；同时会话、Vue 组件、正文均正常。两处宽泛 login 选择器误认了页头入口。后台截图裁剪超时，未聚焦或重试；DOM 补证显示两条文本、一张简历请求卡和一条平台提示，时间线未在加载，未发现实际登录失效。

修正范围见 [计划](../plans/2026-09-08-unified-message-live-acceptance-fixes.md)：按准确 HTTPS 来源与 `/im` 路径识别页面，保留会话身份检查；当前轮次和保留的待办分别展示，不清空历史。

## 数据与运行环境

- 工作树：`D:/DevData/RoleFlow-worktrees/zhaopin-readonly`，分支 `codex/zhaopin-readonly`；本轮计划基点 `4ba2ac149781d998bdb79136df2102f5a80e98f3`。
- 隔离数据及证据：`D:/DevData/RoleFlow-unified-message-acceptance-20260908`。源为 9 月 4 日安装验收前备份，一致性复制后运行迁移；源备份和安装版资料未改动。这不是新用户空库，也不是求职质量评估样本。
- 本地入口：`http://127.0.0.1:8788/messages?profileId=1&workSite=zhaopin`，使用已有普通 Edge。仅后台、串行、只读；真实消息内容和身份不写入 Git。
- 首次 `Start-Process` 后台启动请求曾被工具策略拒绝；用户手动启动后助手复现了上述界面问题。之后 8788 服务停止，停止原因未确认，不能归因为用户关闭或产品崩溃。用户要求助手自行处理启动后，工具标准持续终端直接执行 Node Dashboard 成功，返回可管理的会话；没有提权或改动执行策略。此后启动和浏览器验收由助手负责。

## 验证状态

- Task 1：`9e24f7c728ef4b78574a5a1a2cfe3f52ba3b718f`，三项生产文件及三项已有测试；公共读取器参数地址和真实 HTTP/SQLite/浏览器当前状态回归先失败后通过，五项严格定向检查通过，独立增量复审 Approved，无新增问题。
- 第一次助手实际发现：`2026-09-08T06:41:24.088Z`，上述地址和状态修复生效，但因另一个既有登录误判中断，尚未选择会话。不算完整实站通过，继续 Task 2。
- Task 2：`2a6902106512a257b9ea6965ffaf649e0968b263`，仅读取器和既有读取器测试。观察到的两个普通页头链接被精确排除，真正登录面板在扫描前、选择前仍拦截。公共入口真实样式 RED 后，严格读取器和统一页面旅程 GREEN；独立增量复审 Approved，无新增问题。
- 在上述静态提交重启服务后，第二次助手 UI 发现开始于 `2026-09-08T06:54:09.327Z`：读取当前 21 条会话，21 条均保留入站内容，其中均有安全简历请求摘要。用户提供的 Python 岗位两条文字均在项目页出现。因没有准确智联完整岗位资料，21 条处于明确待补资料状态，并非读取为零。
- 实际页面来源筛选：全部显示 BOSS 3 / 智联 21；BOSS 只显示其旧 3 条并标记来源；智联只显示 21 条，刷新后筛选、原文和请求卡均保留。智联下无可见发送按钮；实际视口无横向溢出。
- 安全停止：第三次发现开始于 `2026-09-08T06:58:13.983Z`。第一次自动化点击停止因页面尚在换页、按钮禁用而超时；重新读取界面确认按钮已启用后，点击成功，API 与 UI 均为 `stopped / MESSAGE_DISCOVERY_STOPPED`，21 条智联内容及旧 BOSS 3 条保留。未把第一次工具点击超时算作产品成功，也未重复点击开始。
- 停止后页面控制台 error/warn 为 0；真实 IM 仍为后台 `hasFocus=false`，编辑器空白。现有浏览器多出一个与本任务无关的本地文档标签，未读取或关闭；不能笼统声称整个浏览器标签总数不变。
- 验收副本 SQLite `quick_check=ok`。相较开始前，草稿 12、发送批次/条目 0/0、入站已归档上下文 8、进度事件 85、漏斗项 18、沟通批次 3 均未增加；仅待办从 3 增至 24，新增 21 条为智联且均保存原文。没有修改安装版或源备份。
- 新鲜严格完整 `npm test` 在静态 `2a6902106512a257b9ea6965ffaf649e0968b263` 从头运行，实际 **152/152、退出码 0**，起止 SHA 相同。日志 `D:/DevData/RoleFlow-unified-message-acceptance-20260908/full-2a69021.log`，SHA256 `37B8A8F8BB63397F7EEE95DD89E61EB83D436C8D003269D76A6B94E5DBCFA6A9`。未完成的真实模型验收不以合成页面测试替代。
- 文案提交 `d657f8321e9849c68c623ce60188597d1036e5a5` 只将“HR 新回复”改为“已分析回复”，同步一条已有显示断言；计数表达式和数据逻辑未变。原有 Dashboard 消息 smoke、统一消息旅程两项严格复验通过，独立微小增量审查 Approved。服务重新启动后，实际浏览器确认标签正确，21 条智联原文及 3 条旧 BOSS 记录仍在，来源筛选保留。
- 文档提交后的精确最终 SHA、三项关键复验、代码/测试树与文案提交的一致性及最终工作树状态写入仓库外 `D:/DevData/RoleFlow-unified-message-acceptance-20260908/final-verification.json`。成功状态以回执实际结果为准；不把三项复验说成在最终文档提交上又跑了一轮完整 152 项。

## 修改文件

- `src/adapters/sites/zhaopin_message_reader.js`：准确 URL 识别及普通页头链接与登录挑战区分。
- `src/dashboard/message_discovery_controller.js`：平台清单共用同一 URL 规则。
- `src/dashboard/message_discovery_view.js`：当前状态与保留待办分离、来源/原因显示、准确统计标签。
- `tests/zhaopin_message_reader_smoke.js`、`tests/dashboard_unified_messages_journey.js`、`tests/dashboard_message_discovery_smoke.js`：公共读取器、实际 HTTP/SQLite/本地浏览器回归和现有断言对齐。
- 当前计划、`NEXT_PHASE.md`、`PROJECT_HANDOFF.md` 与本报告：实际证据、下一入口、未验证前提；不改历史成功或失败记录。

## 剩余产品前提

- 当前实现只有成功关联完整岗位、完成分类的消息才增加相应计数，因此标签已改为“已分析回复”，不再将它误称为“HR 新回复”。本次 21 条原文已读取但都待补岗位，已分析回复仍为 0；服务重启后的本轮可见/排队会归零，持久待办单独保留，不能把这些计数当作账号历史消息总数。
- 完整真实回复草稿验收仍需要同平台、同用户及当前方案的准确完整智联岗位资料。暂不创建伪缓存，也不扩大到真实投递或外部发送。

## 边界

本轮没有发送消息、投递、同意或拒绝简历，没有推送、合并、打包、改版本或发布。读取消息可能产生平台正常的已读状态；这不授权任何外部沟通动作。
