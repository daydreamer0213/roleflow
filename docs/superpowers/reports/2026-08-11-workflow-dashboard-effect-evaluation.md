# Task 2.3 Workflow dashboard effect evaluation

Code-fix commit: `46525282bb1cb8a0db28fcc7f1f236544e0fff35`.

The workflow client now transfers the unique `data-workflow-primary="true"` marker and focus between pause and resume during polling. The strict evaluator rejects both visible and hidden duplicates. Paused model settings links use the secondary white, bordered treatment; the primary command remains solid teal independently of its orange focus outline.

## Canonical evidence

`docs/superpowers/reports/evidence/2026-08-11-workflow-dashboard/` contains exactly:

- `baseline/`: 16 PNG files and `baseline.json`, generated by the final evaluator against detached `8fedac5b8dddfe5e647c771d9993ba8221f5e1a6`.
- `current/`: 16 PNG files and `current.json`, generated by the same evaluator with `--expect-primary` against clean code commit `46525282bb1cb8a0db28fcc7f1f236544e0fff35`.

The current JSON records 16/16 pages with `primaryCount=1`, `visiblePrimaryCount=1`, primary focus, solid outline, initial-viewport visibility, successful structured interaction, and no captured console/page/request/external errors. The baseline records `primaryCount=0` for all 16 historical pages and is intentionally non-strict.

I inspected the 375x812 current paused and scanning screenshots plus the baseline paused screenshot. The current paused page clearly shows the teal `继续本轮` primary and two white secondary model-setting links even without treating the orange focus outline as the hierarchy signal.

## Offline verification

Use the existing bundled runtime dependency path; do not install dependencies:

```powershell
$env:NODE_PATH='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules'
$env:ROLEFLOW_REQUIRE_PLAYWRIGHT='1'
node tests/workflow_page_migration_smoke.js
node tests/run_all.js
```

Focused Playwright workflow regression passed. It covers scanning -> paused -> scanning polling, asserting total primary count 1, visible count 1, no hidden marker, and a focusable/enabled resume command. It also directly exercises the evaluator's fail-closed duplicate-primary gate. The full suite ended with `All 80 offline checks passed.`

Without `NODE_PATH`, the strict evaluator correctly fails closed before evaluation with `Error: Cannot find module 'playwright'` (`MODULE_NOT_FOUND`). The explicit command above is therefore a test prerequisite, not an installation instruction.

No real BOSS page, model, communication, or network action was used.
