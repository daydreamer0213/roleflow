# 求职体检平台页验收 · 2026-09-10

## 交付结果

按用户批准的产品方向完成：后台处理观察规则和策略归属，主页面只呈现帮助用户了解投递反馈的结果。没有新增依赖、表或前端框架。

- 一张BOSS/智联对照表，显示已联系、已回复及比例、索要简历、面试邀请。
- 当前方案按各平台有效策略跨批次累计；累计记录覆盖当前候选人的所有本地方案，不代表平台账号完整历史。只调整BOSS不重置智联，反之亦然。
- 积极反馈读取后立即计数。48小时/周末和30/50/70仅参与后台诊断，页面不陈列成熟样本、证据尺、轮次、条件转化链等内部概念。
- 有依据才显示一条优先建议，入口指向对应的等待回复岗位、消息、简历或面试进展。方案调整表单默认折叠。
- 修改前后的比较须各自有足够成熟且回复状态明确的记录；未知旧基线不能显示为0%。不声称修改导致改善。

## 设计与实现

根因是旧页面把后台的单次策略记录和条件转化直接当作用户视图，未正确表达多平台、多批次和用户真正关心的结果。本轮复用原事件与快照，通过平台范围折叠策略修订，保留原岗位归属和迟到反馈，未重写工作流。

| 修改范围 | 文件 |
| --- | --- |
| 分平台统计、累计及比较 | `src/application/funnel_analysis/index.js`、`src/storage/funnel_store.js` |
| 简历优化不使用跨平台混合诊断 | `src/application/resume_optimization/index.js` |
| 平台表、范围切换、调整表单、响应布局 | `src/dashboard/pages/funnel.js`、`src/dashboard/assets/roleflow.css`、`src/dashboard/server.js` |
| 行为回归与测试调度 | `tests/funnel_platform_feedback_smoke.js`、`tests/funnel_diagnosis_smoke.js`、`tests/funnel_message_linkage_smoke.js`、`tests/dashboard_funnel_smoke.js`、`tests/dashboard_shell_smoke.js`、`tests/run_all.js` |

## 独立复审与回归

一次限定只读复审发现三项重要问题，全部修复并增补行为验证后关闭：面试安排建议未接入新页面、建议入口误进未联系队列、未知旧反馈被当成0%比较。复核关闭源码为 `4481a729f9e594ea62d2d41e415e17422afaad62`；之后只改测试，`src`树保持 `abe7f6c7df75d994bc2ae6e43103264da7645fe8`。

真实SQLite合成场景覆盖两平台各20条不合并凑样本、早回复、单平台及共享调整、跨方案累计与候选人隔离、迟到反馈、旧快照、205次单平台调整、并发/幂等和未知旧基线。HTTP及真实无头Edge交互覆盖当前/累计数值变化、表单折叠和保存、无修改拒绝提交、过期提交拒绝、建议目标岗位、1280/390布局、转义和零分母。

## 新鲜完整门禁

- 精确冻结SHA：`b4b6b722f8ee6fcc0e07fb0f4bd1a50ffe31d388`。
- 从头运行 `npm test`，强制执行Playwright浏览器验证，临时数据放D盘。
- 实际结果：**All 159 offline checks passed.** 退出0；起止SHA一致，工作树干净。
- 完整日志：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-b4b6b72-20260910-040619.log`。

保留两次未完成结果，不用旧门禁替代新证据：

1. `4481a72`门禁在旧shell测试要求已删除的标题ID/空状态文案处退出1；`4c20336`仅更新该页面验收要求，另补查剩余73项全部通过。
2. `4c20336`门禁在启动测试整组240秒处退出1。当时已进入失败路径清理场景。仓库外逐步骤计时保留所有原断言及单项时限，完整通过耗时231878ms，仅余约8秒；此前各步骤累计略慢导致总时限先结束。`b4b6b72`只把该组总时限设为300秒，产品启动脚本、每个场景超时与失败断言均不变。计时证据为同目录 `startup-duration-trace.jsonl`；失败日志均保留。

## 实际本地页面验收

验收服务为 `http://localhost:8788/funnel?planId=1`，使用已有隔离用户数据。只在身份和空闲状态确认后重启自己拥有的服务，未替换安装版；最后核验PID14456，后续须重新识别，不能永久信任该PID。

- BOSS：18个已联系岗位，1个已读取回复，显示5.6%；智联：1个已联系岗位，0个已读取回复。
- 另有3个已核验岗位索要简历，但没有本地可验证投递起点，保留“其他消息”入口，不伪造投递计入分母。
- 现有记录尚无策略调整，两种视图数字相同是正常数据结果；不同数值及调整后的隔离由合成数据交互测试验证，未在用户DB造数据。
- 当前/累计各1440和390截图均已查看，五列表格可见，页面不溢出；表单初始折叠，没有页面异常或外部请求。
- 证据目录：`D:/DevData/RoleFlow-zhaopin-message-context-20260908`，文件 `platform-feedback-live-capture.json`、`platform-feedback-{current,lifetime}-{1440,390}.png`。
- 只读核验用户记录仍为19条联系起点、1份初始策略；历史沟通点击计数25不变，回复发送项目0。没有新增真实招聘平台访问、沟通、回复、简历同意/拒绝或申请。

## 边界与下一入口

本轮证明本地页面及离线行为，不声称重新完成真实招聘平台全流程或证明求职效果提升。反馈仍取决于已读取消息及可信投递记录；跨策略真实效果须等用户后续使用产生足够反馈。

仅推送当前 `codex/zhaopin-readonly` 保存，不合并、不发布、不改版本。文档提交后的精确最终SHA、定向复验、工作树和远端结果记录在仓库外 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/platform-feedback-push-receipt.json`，本报告不预填尚未执行的推送结果。下一入口是用户在8788验收展示，不再重开本轮已关闭功能。
