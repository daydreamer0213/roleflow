# 消息与求职体检：用户视角验收

## 结果

源码SHA：`d7fa23ea673e880eecaec2b88c1b6ea27c86de3a`。分支：`codex/zhaopin-readonly`，版本保持1.3.2。

| 用户问题 | 根因与修复 | 本地页面验收 |
| --- | --- | --- |
| 百分比难以理解、未知不清楚 | 条件转化比例被当成总体进展展示；主图改用本轮成熟岗位共同分母，条件率独立折叠，缺失观察用具体原因说明 | 18成熟岗位中1个回复，主图5.6%，明细明确已读后回复1/1；不再显示“暂无明确状态” |
| 摘要不知道HR说了什么 | 学习记忆的安全通用摘要被直接用作列表预览；改读既有展示原文，较新问题优先，简历卡显式标识 | 三个智联岗位呈现不同的HR原话，直接看到邀请发送简历；展开仍有完整原文及草稿 |
| 关键词出现内部字段 | 来源标记未经过展示转换 | 真实统计表显示“消息发现”，总数14不变，普通关键词不变 |
| 索要简历没有进入体检 | 旧智联只读边界排除了成功沟通样本；无已验证投递起点的入站机会也被全部忽略 | 补接1次成功沟通，19样本/18成熟/1等待；另列13个已确认机会中的3个简历请求；25条待核对会话中22条含简历请求，明确不混入岗位数/转化率 |

没有可核验投递时间的HR主动机会，不能伪造成“本轮投递成功”；待核对会话没有方案身份，故只在账号级提示，不按岗位去重或相加。新回复应继续归原投递策略轮次，补账不重置48小时。简历版本从原轮次保留，用户后来编辑/停用不会使历史样本失去版本归属。

## 修改文件

- `src/storage/funnel_store.js`、`src/core/candidate_progress.js`：成功资格、原轮次补接及事务。
- `src/application/funnel_analysis/index.js`、`src/dashboard/pages/funnel.js`：体检数据与可理解的展示。
- `src/dashboard/message_discovery_view.js`、`src/dashboard/server.js`：消息预览及来源名称。
- 新增`tests/funnel_message_linkage_smoke.js`，补强现有沟通存储、体检、消息及统计页面测试，加入完整门禁。

## 验证

- 四个用户问题有定向RED→GREEN：缺失智联样本、入站请求不可见、列表看不到HR问题、内部来源未翻译。复审又复现并修复“停用简历后旧样本补账丢版本”。
- 回归覆盖原轮次/原时间、重复刷新、不同账号/方案、pending/ambiguous/已沟通过无起点排除、复制不视为发送、沟通进度/体检写入失败一起回滚、不同原文预览及HTML转义。
- 一次有界独立只读复审，无未解决Critical/Important；其测试补强建议已落实。
- 新鲜完整`npm test`：**All 158 offline checks passed.** `GATE_EXIT=0`，起止均为上述源码SHA，`GATE_STATUS`为空。包含必须执行的浏览器UI回归。
- 完整日志：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-d7fa23e-20260910-020410.log`。
- 真实本地页面已走消息→选中岗位→原文/草稿→岗位记录→体检→展开明细，检查DOM内容及实际截图，而非仅检查HTTP返回成功。记录：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/feedback-usability-live-acceptance-20260910.md`。

## 边界与下一入口

本轮只更换自己拥有的隔离8788服务，原记录保留，安装版8787未动。当前服务PID2520，重启后不得盲用此PID。历史全部批次沟通点击总数25前后不变；其中既有智联验收只有1次，不能将两者混淆。回复发送项仍为0。本轮未访问真实BOSS/智联、未发招呼/回复、未处理简历或申请。

只推送当前分支保存，不合并/发布；远端状态以实际Git回执为准。源码门禁后的收口仅改文档，需核对源码无差异。最终真实完整流程和新版本发布仍等用户人工验收与授权，不用本地展示通过代替实站自动发送结果验证。
