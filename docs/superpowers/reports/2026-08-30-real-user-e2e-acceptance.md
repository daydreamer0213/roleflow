# RoleFlow 真实用户全流程验收记录

> 验收日期：2026-08-30。范围是现有简历与方案下的一轮真实岗位发现、岗位分析、用户明确授权的批量沟通，以及随后的只读消息发现。本文只记录本轮实际观察；不会把未出现的消息类型或未执行的动作写成已验证。

## 结果概览

1. 工作流 `3ad4bf20-4f3c-4248-88e0-7e0acf3f1fc9` 收集去重岗位 69 个，完成岗位分析 57 个，进入沟通复核 17 个。
2. 用户明确确认本轮不可变沟通清单后，沟通批次 #3 串行完成 17/17；数据库中 17 项均为 `succeeded`，没有歧义、目标错配或中途停止。
3. 随后的真实只读消息发现读取 40 条可见会话，排队 11 条；2 条形成项目内处理结果，9 条保留为未解决。未解决项实际分为：城市身份不一致 3 条、岗位分析未完成 4 条、岗位上下文不可用 2 条。
4. 本轮真实样本没有形成可编辑回复草稿，数据库中的开放消息草稿为 0。因此，本轮证明了消息页读取、会话选择、岗位详情补读和结果保留，但不能冒充“真实 HR 问题生成草稿并确认回复发送”已经验收。

本轮唯一外部写是用户明确授权的沟通批次 #3。没有发送消息回复，没有同意或拒绝附件简历，没有投递或申请。

## 后台运行问题与修复

首次消息发现暴露了三个连续问题：

- Edge 最小化或被其他窗口遮挡时，所有页面都可能报告为隐藏；这不应阻止只读后台详情读取。
- 隐藏的消息页会暂停计时器和异步回调；会话同步选中后，后续内容可能不加载。
- 临时详情页关闭成功后，Edge 的目标清单可能短暂仍包含刚关闭的页面，或在读取它时返回一次命令失败。

修复后，RoleFlow 不恢复窗口、不切换前台，也不调用 `Page.bringToFront`：

- 固定消息页在每次读取、选择和岗位身份复核前，用 CDP 恢复页面生命周期，再重新核对固定页绑定。
- 临时详情页只有在证明同窗、唯一新增、目标正确且保持后台后才恢复生命周期，随后重新核对可见页与固定页。
- 详情页关闭后只等待浏览器目标清单稳定；不会重开详情、重复消耗外部访问，也不会放宽最终精确基线。
- 真实复验已跨过原来稳定出现的关闭后失败点并进入岗位分析；复验随后由操作者发出安全停止，没有产生新的外部写。

## 12 个“待补详情”的原因

工作流指标记录的是 61 个岗位需要详情、57 个已读、4 个真正待补；页面任务却显示 12 个 `DETAIL_REQUIRED`。逐条核对后得到：

- 4 个确实没有完整 JD，且卡片信息不足以决定是否排除，属于真正待补详情。
- 另外 8 个在卡片层已经有明确排除证据：4 个城市不符、2 个实习岗、2 个硬性排除方向。

根因是分析任务先统一检查“有没有完整详情”，再进入确定性排除逻辑，导致这 8 个也被标成“信息不足”。这不是 12 次详情读取失败；用户看到的待补数量应是 4。

## 验收后的收口结果

1. 提交 `66d89d4` 让确定性排除优先于详情完整度。迁移 26 只清理仍有 `hard_boundary/blocked` 证据的历史误分类；真实运行库的一致副本从 12 个 `DETAIL_REQUIRED` 变为 4 个，真正缺详情的 4 个保持不变。
2. 同一提交把消息发现的取消信号贯穿到岗位分析、模型网络请求和重试等待。用户停止时保留原始停止原因，不转成模型超时；取消后不保存模型缓存或完成分析。
3. 对开启思考模式且已在真实日志中证明会产生长输出的三类岗位深度分析，首轮改为 8192 输出额度、最多 240 秒；只有截断重试才扩大到 16384、最多 300 秒。普通消息、其他任务和非思考批量分析维持原预算，JD、结构化字段、契约校验和串行执行均未减少。

## 验证边界

- 后台读取全程保持串行；临时详情页最多一个，读取后关闭。
- 真实 BOSS 页面没有被带到前台；复验结束后，临时 Dashboard 标签也已关闭，只保留原工作台和两个固定 BOSS 页。
- 没有把招聘方原文、简历正文、Cookie、Token 或安全参数写入仓库、测试或本文。
- 收口实现使用保存 fixture、假模型、临时数据库和真实数据库的一致副本；没有访问真实 BOSS、调用真实模型或产生外部写。
- 形成提交 `66d89d4` 的代码树通过全部定向回归、`git diff --check` 和完整 `npm test`；完整门禁退出码为 0，实际为 131/131，末行为 `All 131 offline checks passed.`。真实 DeepSeek 长输出完成耗时和点击停止后的实际体感仍需下一次自然样本验证。
