# 收到的联系与消息页验收

2026-09-10。本轮实现、独立复核和本地验收已完成，下一入口是用户在开发分支上验收页面。仅保存 `codex/zhaopin-readonly`，不合并、打包、发布或改版本；最终文档 SHA、复验和远端状态以仓库外 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/incoming-focus-push-receipt.json` 为准。

## 用户可见变化

- 求职体检将“主动联系的效果”和“收到的联系”分开。BOSS、智联分别展示已收到联系、索要简历、面试邀请；数字能展开对应记录，再进入同一条消息。没有本地投递起点或完整 JD，也不排除已读到的请求。
- 明确反馈立即显示，不等 48 小时。48 小时、周末顺延和 30/50/70 只用于原有主动联系效果诊断；各平台策略隔离和回复率分母不变。缺少投递记录不能据此推断 HR 主动发起。
- 消息页默认待处理，按“HR 原话 → 岗位判断 → 当前草稿”排列；备用草稿、读取详情和次要操作折叠。批量发送需要主动进入，默认零选择；活动批次停止入口不受消息筛选影响。
- 窄窗先看列表，进入后只看当前详情。返回、筛选和切换先保存，失败保留文字与编辑位置；历史只读，不重建草稿。重复保存的同一会话合为一条，已有草稿及待核对事项都保留。
- BOSS 确认发送、智联复制后人工处理的能力边界不变，保留复制点击时文字和不可变发送目标。

## 根因与实现范围

原展示把收到的请求依附于主动联系起点，未关联岗位的消息因此游离于主要统计之外。新只读聚合复用会话上下文、可靠分类事件和待关联内容，按用户、平台、可靠会话身份去重；不新增表、迁移、依赖或前端框架。列表使用完整的当前用户资料集合，分类和来源只影响显示，统计入口不会截掉其他联系。

主要文件：

- `src/application/funnel_analysis/incoming_contacts.js`、`index.js`：联系聚合及输出。
- `src/dashboard/pages/funnel.js`：分平台统计、精确明细与消息入口。
- `src/dashboard/message_discovery_view.js`、`message_discovery_controller.js`、`assets/roleflow.css`：本地身份投影、待办状态、消息布局与保存/筛选/窄窗交互。
- `tests/incoming_contacts_smoke.js`、`funnel_message_linkage_smoke.js`、`dashboard_funnel_smoke.js`、`dashboard_unified_messages_journey.js`、消息/发送/shell 回归及 `tests/run_all.js`。

未改平台执行器、生产筛选规则或外部写授权。

## 验收与失败历史

1. 初始消息页提交 `4b34877` 未通过主控实际页面验收，出现 14 项断言失败；独立复审归为八组重要缺口：筛选丢记录、未关联深链缺失、错误目标兜底、活动批次控制缺失、历史误列待办、备用稿全展开、闲置控件/单项错误占首屏、窄窗保存与焦点状态错误。初始完成报告已被后续修复证据取代。
2. `214e429`、`74c6e01` 修复上述问题。恢复接口可同时保留旧草稿和新一轮未关联事项，因此按可靠平台/会话合并展示，不删数据库记录。新增回归保留三个人工处理入口、键盘 Space、首行点击、双分类、历史和保存失败等行为。限定独立复核八项全部关闭，规格及质量通过，无新增重要问题。
3. 六项定向检查全部退出 0：`dashboard_message_discovery_smoke`、`dashboard_message_reply_send_smoke`、`dashboard_communication_profile_smoke`、`incoming_contacts_smoke`、`funnel_message_linkage_smoke`、`dashboard_unified_messages_journey`。真实浏览器使用合成临时数据库和假平台，覆盖读取恢复、统计入口、编辑/复制、并发保存、筛选、返回与在途批次停止。
4. 首轮完整检查在干净 `74c6e01` 因 shell 仍要求旧消息页标题而退出 1。随后单跑体检页面又发现旧范围说明断言。`5a98d19`、`9e8c655` 只对齐这两行已批准文案，未削弱导航、计数或链接检查；两个单项及 `dashboard_wave2_acceptance_smoke` 通过，测试差异也经独立复核。失败结果不算作完整通过。
5. 在冻结的 **`9e8c655f67d37f8163470f3074fcb18a3f89b333`** 从头运行严格 `npm test`，实际 **All 160 offline checks passed，退出 0，起止 SHA 一致且工作树干净**。生产源码与 `74c6e01` 页面验收/独立复核版本完全相同。日志：`D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-9e8c655-20260910-074813.log`。旧 159 项和本轮首次失败都不是最终证据。

额外使用实际存储接口在内存数据库构造 520 条智联会话及 1 条 BOSS 会话，计数 521/521，未受页面恢复的 500 条上限截断；只有请求卡、没有重复 manualActions 字段的情况，以及真实格式的人工面试确认事件也通过。原漏斗诊断、阈值和消息观察回归保持通过。

## 现有资料与可见页面

在任务拥有、空闲的 8788 服务上只读验收，原安装版 8787 未替换：

- 收到联系 BOSS 10，其中索要简历 1；智联 25，其中索要简历 25；两平台面试邀请均 0。原先的 3 条智联请求已包含，另 22 条虽待岗位关联但确有请求卡；主动联系仍为 BOSS 18 / 智联 1，未伪造起点。
- 1440、800、584、390 宽度检查均通过：无页面横向溢出、零默认发送选择、一版可见主草稿、闲置批量区域隐藏；窄窗列表/指定详情分开显示。首条消息位置为桌面 y405、窄窗 y504，在 694px 高窄窗内能看到内容预览。
- 数字明细可反复打开及切换；已关联、未关联入口定位对应会话；缺失/错平台目标不会打开其他人的草稿。已目视桌面体检、桌面消息及窄窗列表/详情截图，而非仅检查 HTML。
- 页面错误 0，外部和写请求 0；草稿、回答记忆、进度事件、主动联系及发送记录前后摘要一致。原始私人截图不入 Git，回执位于 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/incoming-focus-visual/2026-09-10T07-30-06-038Z/receipt.json`。

## 边界与下一入口

本轮只验证本地页面、已有资料和离线合成流程，未重新访问真实招聘平台、发起沟通/回复、处理简历或申请；不代表重新完成实站全流程验收。统计仅覆盖已读入 RoleFlow 的可靠联系，不宣称包含账号完整历史或提升求职效果。

用户下一步刷新 `http://localhost:8788/funnel?planId=1` 和 `http://localhost:8788/messages?planId=1`，查看数字到消息的连接、原话与当前草稿的层次。最终人工验收后再决定合并/发布；不自动开启 51job 或其他新阶段。
