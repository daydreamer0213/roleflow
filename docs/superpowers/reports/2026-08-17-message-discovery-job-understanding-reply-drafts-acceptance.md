# 消息发现：岗位理解与回复草稿验收报告

日期：2026-08-17

结论：通过。课题 2 已完成真实后台只读验收，四个独立课题至此全部达到 100%。

## 1. 范围与安全边界

- 使用用户现有的同一 Edge 登录窗口和当前分支 Dashboard，只处理一个明确的真实消息样本。
- BOSS 访问严格只读；没有填写输入框、发送回复、发起沟通、投递或其他外部写。
- 消息发现允许的唯一临时详情页必须与固定标签同窗口、以 `active: false` 创建、全程后台、串行读取，并在检查点完成后关闭。
- 常规岗位详情主线仍只有 `trusted_pane`；没有启用、修复或验证 `search_page_api`，也没有恢复一般用途的 `standalone_detail`。

## 2. 隔离与数据保护

- 岗位读取验收数据库：`D:\DevData\RoleFlow-message-discovery-acceptance\topic2-accepted-20260817.sqlite`；最终决策卡验收使用新的隔离数据库 `D:\DevData\RoleFlow-message-discovery-acceptance\topic2-decision-card-verdict-20260817.sqlite`。
- 验收起始数据来自隔离的暂存数据库：`D:\DevData\RoleFlow-mode-acceptance\staging\source-v15.sqlite`。
- 模型设置仅从 `D:\Guo\ZhiPing` 只读加载；验收前后文件哈希一致。
- 正式生产数据库 `D:\Guo\ZhiPing\data\jobs.sqlite` 未修改。招聘者消息正文没有落盘，报告不记录岗位 ID、详情地址、`securityId` 或消息原文。

## 3. 验收过程与实际修复

1. 当前消息页只读发现一个待处理样本，本地没有对应的完整可信 JD。
2. 第一次读取只创建一张同窗口后台临时详情页。创建结果保持 `active: false`，Dashboard 活动标签未变化；临时页短暂处于空白加载状态后进入精确目标地址。
3. 独立详情页使用自己的唯一详情根节点、地址岗位身份、标题和公司核验，不再借用固定搜索页右栏字段。完整 JD 成功读取并提交后，临时页关闭，三标签基线恢复。
4. 真实流程暴露出两个恢复问题：硬边界岗位不能进入消息上下文语义分析，以及消息已读后恢复检查仍按未读变化判断。两处均先加入失败回归，再做最小修复。
5. 最终恢复运行命中本地完整缓存，没有再次创建临时详情页，也没有增加详情访问。
6. 用户查看实际结果后确认界面结构可用，但要求把分析过程列表改为岗位职责、公司业务、匹配度、薪资和机会结论。决策卡直接使用已有完整分析，不增加 BOSS 访问、模型调用、数据库迁移或公司外部调查。
7. 独立代码复审发现低匹配和不推荐理由可能优先显示正面匹配项。修复后，规则保护结论优先，其次为硬性缺口、岗位质量风险或主要差距；中匹配长摘要也会同时保留匹配方向与关键缺口。

## 4. 最终用户可见结果

- 发现状态为完成：排队 1、处理 1、未解决 0、结果 1。
- 结果被识别为面试邀请，并显示六个清晰区块：这个岗位是做什么的、公司及业务、你的匹配度、薪资范围、这份机会值不值得继续聊、消息与下一步。
- 真实样本显示低匹配和不建议优先投入时间，两处均带同一条关键原因；原始薪资可见。JD 没有可靠公司画像时，页面明确说明只能评价这份岗位机会，不能评价公司本身。
- 旧的匹配依据、硬性阻断、待补信息和建议核实列表均不再显示；`interview_invitation` 等内部枚举改为中文标签。
- 面试时间等内容必须由用户亲自确认，因此系统按设计不生成回复草稿。
- 页面没有填写、发送、沟通或投递动作；全程没有对 BOSS 执行外部写。

## 5. 浏览器与风控证据

- 验收前后均为同一窗口三张标签：两张固定 BOSS 标签和一张 Dashboard；最终活动标签仍是 Dashboard，临时详情标签数量为 0。
- 临时详情页创建、URL 等待、身份读取和关闭期间没有调用 `Page.bringToFront`，也没有激活 BOSS 标签或抢占前台。
- 每次已发出的临时详情尝试都进入共享 `detail_open` 与 `pane_detail_read` 预算，并沿用既有随机节奏、冷却和恢复检查点。
- 没有登录失效、风控提示、页面丢失或身份不一致的未处理结果。
- 最终决策卡验收开始和结束时均为两张固定 BOSS 标签、零详情标签；用户原有前台标签保持不变，结果页只在后台本地 Dashboard 标签刷新。

## 6. 自动验证与复审

- 针对性回归覆盖：空白地址后成功、持续空白超时关闭、独立详情身份、慢消息内容、已读恢复身份、内部消息分析能力、伪造 HTTP 参数不能绕过普通分析门禁，以及硬边界语义状态一致性。
- 最终提交 `e74ba61` 后，核心投影、Dashboard、回复安全三项聚焦测试通过；允许执行的全量离线套件在最终代码树上重新运行，100/101 项全部通过。
- 唯一排除的 `startup_scripts_smoke.js` 会生成未签名的 `msedge.exe` 测试桩并触发 360 防护；没有为测试启动该程序。
- 静态检查没有发现消息路径调用 `Page.bringToFront`、前台激活、`search_page_api` 或一般用途 `standalone_detail`；`git diff --check` 通过。
- 独立只读代码复审关闭了网页参数绕过内部分析能力的问题；决策卡复审提出的低匹配、岗位质量理由和长摘要问题经修复后再次复核，未留未关闭问题并给出 Ready 结论。

## 7. 访问台账说明

- 从 `2026-08-16T02:04:21.831Z` 起的共享工作区台账记录为：列表导航 16、列表滚动 75、右栏/详情读取 106、独立详情打开 7。
- 最终成功的缓存恢复运行没有新增详情打开或详情读取。
- 最终决策卡验收同样复用完整本地缓存，保持零详情标签。验收中一次本地程序化按钮形成普通表单导航并额外启动了排队 0、处理 0 的只读检查；它没有生成详情访问、草稿或外部写，最终结果在新的隔离数据库上重新验证。
- 验收准备时的一次本地台账对账把 849 条原本已存在的历史访问记录合入了工作区台账；这些记录全部在滚动 24 小时时间窗之外，没有改变当前访问额度。为保留审计证据，没有删除或改写这些历史记录。

## 8. 完成结论

- 当前分支已经证明新沟通岗位可在不抢前台的情况下取得可信完整 JD，并可在恢复运行时优先复用本地缓存。
- 岗位理解和消息分类只基于完整、同观察的岗位资料与分析；消息卡只展示五项用户决策和消息下一步，不再暴露分析过程列表。敏感或必须人工确认的结果可见但不生成草稿。
- 课题 2 完成度为 100%，四个独立课题均已完成。剩余事项不再是功能实现，而是由用户决定是否整合分支、规划版本、推送或发布。

## 9. 用户决策与消息语义修正验收

- 设计/计划提交为 `c3c9108`，既有决策卡基线为 `4a2c466`，实现提交范围为 `f1a7260` 至 `a2ce0c4`。独立只读复核为 Ready，未发现 Critical 或 Important。
- `git diff --check 4a2c466..HEAD` 通过；指定消息链路的浏览器边界和 Dashboard 隐私搜索未出现新增命中。101 项注册检查中，除会触发 360 防护的 `startup_scripts_smoke.js` 外，其余 100 项全部通过。
- 本轮新建隔离数据库 `D:\DevData\RoleFlow-message-discovery-acceptance\topic2-user-decision-semantics-fix-20260817.sqlite`；正式生产数据库、两份源证据数据库和模型设置未修改。
- 一次本地 HTTP 只读运行完成：排队 1、处理 1、未解决 0、结果 1。实际类别为“其他沟通”；脱敏结果为招聘方说明业务/岗位相关信息而非发出面试邀请，摘要存在且没有面试邀约表述。
- 页面可见顺序为“建议、对方说了什么、岗位概况、公司及业务、匹配情况、薪资、下一步”。旧问句标题、公司免责声明和面试邀请标签均未出现；本样本无本地草稿。
- 验收开始和结束均有两张固定 BOSS 标签、零临时详情标签，活动的无关前台标签未改变；BOSS 编辑器为空。没有调用 `Page.bringToFront`，也没有填写、粘贴、发送、沟通、投递或任何外部写。

## 10. 验收后的整分支安全闭环

- 第 3 至第 8 节记录的是较早一轮中间验收；第 9 节的“其他沟通”分类、结论前置页面和无公司免责声明结果，才是当前用户可见语义的最终真实验收证据。
- 真实验收之后，整分支审查发现并关闭六项 Important：面试阶段与后续消息耦合、模型声明事实键可信度、终态模型结构错误降级、消息结果清理计时、固定 BOSS 标签绑定复核，以及扫描/消息发现跨流程与跨档案共享详情冷却。
- 修复范围为 `c58a63f` 至 `27e4e7a`。原审查者复核确认六项全部解决后，又发现确认无关旧草稿会注销正在运行的消息发现；提交 `ed0b27d` 删除了该错误分支，并用真实本地 HTTP 进度操作建立回归测试。窄范围复核为 Ready，没有新增 Critical 或 Important。
- 最终安全修复只使用临时或内存数据库与离线浏览器替身，没有访问真实 BOSS、Edge 或生产数据库。最终代码树的 100/100 项允许离线检查通过；`startup_scripts_smoke.js` 继续因 360 防护明确排除且未运行。
