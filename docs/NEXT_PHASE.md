# RoleFlow 下一阶段主控索引

> 更新于 2026-09-10。消息学习与确认发送、阶段二策略轮次求职体检、阶段三整份可编辑定向简历、阶段四简历通用模拟面试和 Dashboard 桌面端整体改造均已完成本地实现和离线验证。不再回到岗位分析或积分路线比较；后续真实验收或专项扩展必须按当时范围重新确认。

## 当前开发优先级

**当前唯一入口（2026-09-10）：用户验收收到联系统计与新版消息页。** 实现、独立复核及现有资料页面验收完成：分平台统计已读取联系、索要简历、面试邀请，数字可到对应消息；缺少投递起点/JD 不漏计，不推断 HR 主动发起，不改原回复率或 48 小时/周末及 30/50/70 诊断。消息以原话、岗位判断和一版主草稿为中心，批量主动进入且零默认选择，窄窗列表/详情切换与保存失败保留已验证。干净冻结 `9e8c655f67d37f8163470f3074fcb18a3f89b333` 新鲜完整检查 **160/160、退出 0、起止 SHA 一致**。详情、失败历史和边界见[验收报告](superpowers/reports/2026-09-10-incoming-opportunities-message-focus-acceptance.md)；最终文档 SHA 复验及推送以仓库外 `incoming-focus-push-receipt.json` 为准。只保存 `codex/zhaopin-readonly`，不合并、发布、改版本或访问真实招聘平台，不再扩展本轮功能。

### 上一项已交付的平台页（历史证据）

**当前唯一入口（2026-09-10）：验收新版求职体检平台页。** 用户已批准并完成“当前方案 / 累计记录”的平台对照展示，多轮按各平台有效策略累计，单平台调整互不重置；后台负责成熟时间、样本强度与可靠比较，前端只保留结果和一条有依据的建议。冻结 `b4b6b722f8ee6fcc0e07fb0f4bd1a50ffe31d388` 新鲜完整检查 **159/159、退出0、起止SHA一致且树干净**，限定独立复审和真实本地页面检查完成。当前只推送 `codex/zhaopin-readonly` 保存，用户最终验收后才考虑合并、发布；不继续扩展功能或访问真实招聘平台。详见[平台页验收报告](superpowers/reports/2026-09-10-platform-feedback-dashboard-acceptance.md)。最终文档SHA、复验及远端状态以仓库外 `platform-feedback-push-receipt.json` 为准。

### 历史检查点

下方记录保留原始证据，不是当前待办。旧“成熟岗位主图、条件转化明细、策略轮次”展示已被上方新设计取代，不能据此恢复旧页面或重开已完成开发。

**最新入口（2026-09-10，本轮用户反馈优先于下方历史）：** 消息与求职体检四处展示/关联问题已修复，源码 `d7fa23ea673e880eecaec2b88c1b6ea27c86de3a` 完整离线 **158/158** 通过，起止SHA一致且树干净；限定独立复审及真实本地页面验收完成。主图统一成熟岗位分母，保留条件转化明细；通用消息摘要改为已有原文预览；内部来源改为“消息发现”；已验证智联沟通接入原轮次/48小时，未确认投递起点的简历请求另列，账号待核对会话单独说明。下一步由用户验收现有8788页面，再决定合并与发布。当前仅推送`codex/zhaopin-readonly`保存；不重开已关闭的扫描/招呼弹窗开发，不新增真实发送。证据见 [四项问题验收报告](superpowers/reports/2026-09-10-feedback-message-usability-acceptance.md)。

**当前入口（2026-09-10，优先于以下历史）：** 招呼成功弹窗识别修复已在 `c63d469638c995a53247618e787cd1f48eaf4fc7` 完成，真实专用弹窗结构的脱敏fixture实际RED→GREEN、4项定向检查及唯一限定独立复核通过；该精确SHA新鲜完整门禁 **157/157、退出0、起止SHA一致且树干净**。不再将此缺陷列为待开发。旧唯一招呼真实成功且人工核对、点击数1；本轮没有重发，修复后的实站自动识别留待最终人工验收。原204岗位/200完整JD/4短JD、12分析成功/1模型契约失败/191规则跳过、6草稿/22未解决仍按实际保留，不称全量通过。用户现已授权推送当前`codex/zhaopin-readonly`保存；不合并、不打包、不发布、不改版本。**下一入口是最终人工验收**，具体回执见 [真实验收检查点](superpowers/reports/2026-09-09-zhaopin-real-acceptance-checkpoint.md)；推送SHA以仓库外`push-receipt-20260910.json`为准。昨日临时额度已到期，恢复默认且平台分开记账。

**当前入口（2026-09-09，覆盖下方历史）：** 用户确认网页显示恢复，要求持续推进、不因中途意见暂停。扫描中的“已选中且请求结束却无详情”单次后台恢复已实现于 `66e5467`，4项定向检查通过；限定复审已通过且无阻塞发现，最终冻结完整门禁和真实原轮恢复尚待完成。遵循 `2026-09-09-zhaopin-empty-detail-recovery` 设计与计划：完整详情复用、真加载等待、确认停止且为空时有节奏重激活同卡片一次，仍失败保留并停；不减覆盖/质量/预算，不增加前台操作或发送重试。同一真实run保留70新岗位/69完整/1短JD待补，尚无整轮分析或普通招呼通过证据。接下来重启仅拥有的8788隔离服务，从正常UI继续；不要复用旧进程号或把旧157项结果当成本次新代码门禁。不重新询问已确定方向，不推送、合并或发布。

**当前入口（2026-09-09 06:18 UTC）：** 用户已将后台只读重读的具体设计交给主控，不再询问批准。先处理用户截图中的整页裁切：DOM 布局正常不等于可见画面正常。已有线索指向此前诊断局部截图超时留下临时绘制覆盖，非已证实的网络或最小化原因。当前搜索页已一次清除该覆盖并释放调试连接，无前台切换、刷新、岗位点击或外部发送；可见恢复尚未核验。保持原中断进度70新岗位/69完整/1短JD待补，不机械增加重试或修改站点 CSS。现场证据见 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/checkpoint-20260909-layout-review.md`。源码未变；下方“尚未批准”及待确认措辞仅属历史，整轮真实验收仍未完成。

**当前验收状态（2026-09-09 04:59 UTC，优先于下方历史）：** 最终冻结提交 `8b0d719b9b63500ecc088192c418cf9f0bf0a3c4` 严格完整检查为 **157/157、退出 0、起止 SHA 相同且树干净**；完整日志在 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/full-gate-8b0d719-20260909-041719.log`。本次另有3项当前定向检查通过，源代码未变。用户反馈临时切前台仍空白、手动刷新才恢复；04:44正常UI一次继续同一run/batch，原超时岗位已保存1145字符详情，岗位与批次记录无重复。但04:53第20张卡又长期空白，中断时累计70新岗位/69完整/1既有短JD待补，完整扫描与分析、至多一条招呼仍未通过。04:55–04:57最小只读证据确认：智联详情使用的客户端设置3秒请求超时；本次资源约3021ms结束、状态0，当前网站jobDetailLoading=false且详情为空。请求失败机制与本次迹象一致，但具体网络错误/HTTP原因尚未捕获，不能归因于后台或最小化。用户再次要求正常操作后台运行，本次继续没有前台切换，6标签保留；没有新招呼、回复、简历或申请，不推送/合并/打包/发布。推荐下一步设计只读失败后的单次有间隔后台重读，仍失败保留并停；这是建议、尚未批准实施，不修改站点请求超时或重试发送。精确现场、后台观测与未验证范围见 `D:/DevData/RoleFlow-zhaopin-message-context-20260908/checkpoint-20260909-0459.md`。

**最新检查点（2026-09-09 04:16 UTC）：** 详情等待修复 `c6399c4af3e91ea9036fb135f35504a4104f4caf` 已完成，实际慢加载RED、4项定向GREEN、语法/diff检查；唯一限定复审PASS/APPROVED，无开放问题。真实UI已一次点击“继续本轮”，同一run从62岗位/61完整JD/1待补恢复为“正在筛选岗位”，6标签和原活动页不变。仅拥有的隔离服务为14696/session74679/8788，旧15540已停止。当前只是恢复扫描，不代表平台详情已恢复或整轮分析通过；主控继续只读验收并准备最终完整门禁。1690622全测是主动取消的历史不完整结果，不得引用为通过。当前目标截图缺口仍保留，无新招呼/回复/简历/申请，不推送/合并/发布。此前局部任务均已关闭，不重新审查。

**最新执行入口（2026-09-09 03:49 UTC）：** 标题修复2b345f0及真实原页核验已关闭。正常UI恢复同一run后，第二卡详情长期空白，03:39以旧身份错误中断，62岗位/61完整JD/1待补保留。03:41/03:42仅读及现有后台render scope内额外20秒观察均仍空，无新点击/导航。独立代码诊断确认详情仅6次采样、600ms显式等待；不是原有120秒等待失效，也无证据断言网络/休眠根因。当前执行 `2026-09-09-zhaopin-detail-loading-wait.md` / BASEe2e79fa：加载时最多120秒，超时准确提示未加载，非loading身份冲突仍及时停，不重点击、不降低质量/覆盖。原1690622完整门禁由主控主动取消，退出1、不完整、非断言失败；不得宣称157通过。仅拥有的15540/8788已停止，桥接保留，实施/限定复审后恢复只读验收再跑最终全测。真实消息/模型草稿/编辑保存有界样本已通过，整轮分析/普通招呼仍未完成；当前目标图缺口仍保留，无外部发送、简历或申请，不推送/合并/发布。

**最新检查点（2026-09-09 03:36 UTC，覆盖下方历史）：** 长标题修复在 `2b345f08d49521561b1c781629a32dfe03b4aab6` 完成，限定复审两项问题均关闭。真实原失败页03:35只读验证通过：同一adapter读出完整标题和精确卡片/详情编号，921字符JD，6标签及原活动页不变。最终方案按内层标题span优先读取完整aria-label、外层仅回退，helper升级8；真实嵌套fixture先得行为RED，3项定向GREEN，身份/质量/覆盖/预算边界不变。原run仍保存62岗位/61完整JD/1待补，主控接下来更换自己拥有的隔离服务并从正常UI恢复。`ac5647e` 的157项全测仅为修复前证据，最终SHA完整门禁待运行。消息/草稿/自动保存有界样本已通过；整轮分析、至多一条普通招呼未完成。当前目标截图仍缺失，不抢前台或使用其他岗位图，不执行缺少证明的招呼；不发回复/简历/投递，不推送/合并/发布。

**最新执行入口（2026-09-09 02:50 UTC，覆盖下方旧检查点）：** 同一真实run已保存62岗位/61完整JD/1待补，下一卡因长标题省略显示而中断。当前节点 `aria-label` 提供完整标题且与组件/详情相符，已确认是读取兼容问题。执行 `2026-09-09-zhaopin-clamped-card-title.md` / BASE40e2e0e 的两文件修复，读取同节点完整名称并统一签名；不放宽编号/字段检查，不降低JD门槛或覆盖。主控仅停止自己核验的6648/8788，桥接保留。`ac5647e` 新鲜完整157/157、退出0、起止SHA一致，但不覆盖接下来的标题修复。此前局部任务均关闭；真实消息/草稿/保存有界样本已通过，真实整轮分析/最终SHA全测/至多一条普通招呼仍待完成。后台截图最后一次不同假设验证仍超时，不能再盲试或抢前台，当前目标图缺失前不执行招呼。不发回复、简历或投递，不推送/合并/发布。

**当前检查点（2026-09-09 02:38 UTC，优先于下方历史）：** 短正文隔离已在 `fdc7d21368ffb7a42f731e99cda7e5ffa7ebf896` 实现并独立复核通过；不降低120字符及质量门槛，不减少覆盖或预算。实际从普通“继续本轮”恢复同一 run，已推进42个新岗位/41份完整JD，1项仍待补，正在扫描而非整轮通过。短项不再阻塞后续岗位；下一关键词、有效JD分析仍待实际完成。独立追踪与当前 `workflow_scan_analysis_smoke` 通过，证明既有本批次分析只处理完整项，短项为 `DETAIL_REQUIRED`。仅拥有的隔离8788为 PID6648/session34298，旧11344已停止，勿复用；桥接44114保留。源码任务/复审已关闭，主控冻结文档后可并行运行最终完整离线检查与只读验收；任何新缺陷必须重新评估最终SHA。消息/草稿/保存有界样本已通过；最终完整检查、整轮分析及至多一条普通招呼仍未完成。当前目标截图证据仍有缺口，不能用旧岗位截图替代或据此执行招呼。不发回复，不处理简历/投递，不推送/合并/发布。

以下至“上一轮只读消息验收”标题为修复过程历史；旧服务停止、旧待复审和旧计数不是当前状态。

**修复复核更新：** 上述当前面板公司问题已由 `dc984dd` / `1053b964466b40f9e02cf54f486a1ba0ada12fd9` 实现并完成定向复核，无剩余代码阻塞。原先出错的真实页面已读出正确发布公司与客户公司；继续同一run、实际分析及最终完整检查仍待主控完成。下段“正在执行”指真实验收尚未结束，不重新实施已关闭源码任务。

**最新收口状态：** 扫描恢复计划两任务及复审已关闭，`a61e34c` 的实际恢复已保存4个新岗位/4份完整JD，终态页面自动显示也已生效。随后遇到当前详情面板把明确“客户公司”当成发布公司的新解析缺陷；最小真实DOM已证实两者在同一面板分别存在。正在执行 `2026-09-09-zhaopin-panel-client-company.md`，不重开已关闭的扫描/消息审查，不放宽公司或岗位身份。最终完整测试与单岗位招呼仍待验收。

**最新优先级（用户已批准，正在开发）：智联普通主动沟通 + 消息关联 JD 自动补全 + 待处理消息纳入同一左列表/右详情。** 助手负责真实用户流程验收，停止于回复草稿保存、尚未发送。采用 `2026-09-08-zhaopin-communication-and-message-context-design.md` 及其独立子计划。现场还复现了新版搜索 DOM 字段读取失配、准备页导航抢先判错，属于完整验收路径的必要修复。当前仍不推送/合并/发布，不替换安装版。下方是上一只读阶段证据，不是这轮通过结论。

当前执行断点：真实消息原文/完整JD/模型/草稿及自动保存、切换、刷新、立即跨页已通过；公司待核对项说明和队列继续也已实站验证。22:08 UTC 智联7岗位（3个消息JD岗位、4个新扫描岗位）、4份草稿、21条待处理，不代表全部消息完成。同一run `25641478-c928-403a-8d3d-c3f189895298` 当前 interrupted，源于上述客户公司解析。主控已核验并停止自己管理的隔离8788，修复复核后正常继续该run。`1bc7879` 完整测试因新实站缺陷被主控取消，未跑完，不是通过；旧 `d70a0c8` 的157项只作历史记录。只读扫描/分析稳定后才重跑最终全测，之后至多一个普通初次招呼。尚无新招呼/回复/投递/简历操作，正式安装版/旧BOSS不变；不推送/合并/发布。详细回执见当前实施报告。

### 上一轮只读消息验收（以下保留当时证据）

**最新入口：助手已完成真实只读消息读取验收，可查看开发分支效果；真实回复草稿仍待岗位资料前提。** 三项现场误判已修复并独立复核：参数化 IM 地址、正常页头登录入口、旧 BOSS 待办覆盖当前状态。`2a6902106512a257b9ea6965ffaf649e0968b263` 的新鲜严格完整门禁为 **152/152、退出码 0**，起止 SHA 相同；随后 `d657f83` 只把误导性的“HR 新回复”标签改为“已分析回复”，原统计不变，两项页面回归与微小增量复核通过。助手自行管理隔离验收服务，实读 21 条智联会话，原文、请求卡、来源筛选、刷新/重启保留及安全停止已验证，未发送或处理简历。这份旧备份副本无智联完整岗位缓存，21 条明确待补资料，不是读取失败，也不代表真实模型草稿已通过。详情与最终 SHA 复验回执见 `docs/superpowers/reports/2026-09-08-unified-message-live-acceptance.md`。当前不推送、合并或发布。

### 统一消息开发检查点（现场验收前）

**智联消息发现接入统一消息页已完成本地实现与验证，下一步是开发分支的人工只读验收。** 当前 `codex/zhaopin-readonly` 分支一次手动只读发现串行读取已连接平台；默认合并展示，来源筛选只影响显示；复用原文、岗位分析、可编辑草稿、自动保存、复制与学习。智联不开放发送、投递、简历处理和漏斗扩展。设计见 `docs/superpowers/specs/2026-09-08-unified-message-discovery-design.md`，实施计划见 `docs/superpowers/plans/2026-09-08-unified-message-foundation.md`。四项任务复核、全分支审查和唯一最终修复波均完成，生产代码 `23dad6e`；随后仅对齐一项旧测试，在静态 `bd659782497941a842e22c46ca6b15518d9bd414` 从头重跑严格完整门禁，实际 **152/152、退出码 0**。修复包括列表/正文加载等待、准确岗位关联、待处理消息保留、平台来源隔离，以及智联筛选下仍能停止在途 BOSS 发送。失败历史、修改文件、限制和文档提交后精确 SHA 复验回执见 `docs/superpowers/reports/2026-09-08-unified-message-discovery-implementation.md`；不将本地合成测试等同于实站或真实模型验收。

真实消息正文已经补证：纯文字、131 招呼文本、11 简历请求卡及 255 平台提示可区分；用户也提供了可见正文核对。先前后台空白属于当时加载状态，不能据此断言没有消息或必须前台。页面访问仅后台只读选择会话，一条未读正常变为已读，未发送或处理简历。详见设计的证据段落。

### 上一阶段：智联只读找岗与完整 JD 分析（已完成并推送分支）

按轮次选平台 → 带关键词打开原生搜索 → 用户设置并保存筛选 → 后台读取完整岗位 → 复用现有分析 → 查看只读结果、继续下一轮。两个平台条件和轮次分别保存，共用浏览器执行互斥；智联不进入 BOSS 沟通或投递统计。

四段实现、全分支审查及修复复核已完成，最终功能代码为 `c27b2dfd2028427339af67c34a43b041ea524207`，该静态提交的严格完整检查 **149/149、退出码 0**。今日任务、平台条件、运行/恢复、来源结果/报告/CSV 和服务端只读边界均已接入；BOSS 条件串用、发布方缺失、加载中正文误读、同窗检查遗漏和普通页面切换误拦截均已修复并补回归。真实本地 Dashboard/CLI/临时数据库流程使用合成平台和分析器，已跑通开始、暂停、重开后继续和结果查看；不等于实站或真实模型验收。当前进入开发分支验收准备，不替换安装版。实现与最终复验记录见 `docs/superpowers/reports/2026-09-06-zhaopin-readonly-implementation.md`；只读页面补证见 `docs/superpowers/reports/2026-09-06-multi-platform-discovery-research.md`；设计和执行入口分别为 `docs/superpowers/specs/2026-09-06-zhaopin-readonly-design.md`、`docs/superpowers/plans/2026-09-06-zhaopin-readonly.md`。

上一阶段已按用户“推送但暂不合并”的授权推送 `ffb0a327e6deed08e2854cef0de91015b25c788a`，回执 `D:/DevData/RoleFlow-multiplatform-research-20260906/push-receipt-20260906.json`。这不授权本次新增代码再次推送；当前不合并、打包、改版本或发布。统一消息之后再评估智联独立投递/分平台反馈和 51job，均无外部写授权。BOSS 既有流程与用户 v1.3.2 两轮人工验收并行保留。下方旧状态保留当时证据，不代表当前开发入口。

## 当前结论

- 默认路径已定义为“RoleFlow 专用 Edge（推荐）”，不需要 Edge Control。
- `%LOCALAPPDATA%\RoleFlow\BrowserProfile` 保存并复用浏览器登录资料；安装版数据库等数据位于独立的 `%LOCALAPPDATA%\RoleFlow\Data`，更换程序安装目录不会改用新数据目录。便携包或旧自定义目录不自动搬迁。
- “使用当前 Edge（高级，需要浏览器连接组件）”只允许显式选择，且不会成为默认路径的自动 fallback。
- 两个固定 BOSS 页仍是基线；消息发现只有一个受控后台临时详情页例外。
- 用户启动工作区时可以有一次前台引导，之后所有产品工作都保持后台。
- 迁移显式、只复制并保留源；普通/静默卸载保留 profile，删除需要独立确认。
- 启动测试不再创建假的 `msedge.exe`；Edge Control 不进入普通安装包。
- 工作区会主动补齐专用 Edge 和固定 BOSS 页面；除启动引导外不抢占前台。
- 搜索页已登录但尚无关键词时，工作区现在先在后台补齐唯一消息页和工作台，再使用当前有效求职方案准备首个 A/B/C 关键词；准备成功后自动复检，不再要求用户点击“刷新工作区”。没有 onboarding 运行记录的旧数据也适用；已有 query 和城市、地铁、商圈等筛选不会被覆盖。
- 新装和覆盖升级都会显示安装位置选择页。升级默认带出上次安装目录，用户可以直接继续，也可以点原生“浏览”按钮更换程序目录；用户数据目录不随程序目录移动。
- 专用 Edge 本地冷/热短测均只有一个浏览器根进程，但单空白页进程组私有内存约 0.83–0.92 GB；暂未发现 RoleFlow 重复启动浏览器的证据，也没有证据支持牺牲后台运行可靠性来增加省内存参数。
- 完整 JD 后增加岗位资格闸门，明确不符合实习/届别/在校要求的岗位不会进入推荐或沟通。
- 专用 Edge 已具备受限网络结果验证能力；能力或观察准备失败发生在点击账本之前。
- 消息发现已把招聘方沟通意图与消息话题拆开：只有正式面试邀约推进面试阶段，单纯提到“面试”不会被当成邀约。
- 消息结果现在显示 HR 原文或安全卡片摘要、岗位理解与既有分析结论，再显示本地回复草稿；已读与送达只计数，不逐条展示。草稿生成或自动保存不会触发平台写入。
- 已验证的附件简历请求卡片现在显示为 BOSS 人工待办；同一轮的纯文字仍独立生成草稿，平台竞争提示不进入模型。未知、位置或面试卡片在缺少真实结构证据时继续安全停止。
- 回复草稿现在可以直接编辑并在本机自动保存；复制或标记“已手动发送”会直接采用当前文字完成本地学习。点击单条或批量确认时，当时的目标、草稿版本和文字会成为不可变发送授权，由固定后台消息页串行执行，不再二次确认。
- 只有用户真正改过且仍启用的回答会作为后续同类沟通口径；未修改模型草稿不是权威事实，事实提取失败也不会丢失用户改好的完整回答。
- 新增“我的沟通资料”，可修改或删除当前事实、编辑或停用已记住的回答，并查看自然语言事实修改历史。
- 岗位主卡现在先给推进结论，再自然显示岗位职责、公司与机会、匹配、薪资和需要确认的问题；内部决策来源仍在展开明细。
- 首次分析失败后重试成功的岗位会保留失败历史，同时显示“失败后已解决”；当前未解决和流程体检不再被历史失败污染。
- 岗位判定的生产决策已经冻结为继续使用二维表。43 条可比较样本中 38 条一致、5 条积分更保守、硬边界逃逸 0；仓库没有人工确认标签或同岗位重复模型样本，因此不能声称积分准确率或稳定性更差，也不再把“是否切换积分”列为近期开发任务。
- “求职体检”只统计用户确认已投、已验证发起沟通或确认已发送回复的岗位。默认 30/50/70 分别给初步观察、可比较结论和正式诊断，三项可按用户求职强度调整且必须严格递增。
- 每个求职动作至少等待 48 小时，周末截止顺延到周一；岗位成熟后若出现新的已读，“已读未回”从该次已读重新等待 48 小时。等待、未知、用户关闭/拒绝均不进入失败分母，明确回复、简历请求和面试邀请会立即显示。
- 每个真实求职动作永久绑定到发生时的策略轮次。用户在外部修改招呼语或求职策略后，可在本地点击“修改完成，开始验证新方案”；旧岗位和迟到结果仍更新旧轮，新动作进入新轮，不再按滚动批次混算或在 70 个时自动切池。
- 当前诊断只读当前轮；同方向的前后轮都达到各自 50 个成熟样本且历史边界可靠时，才展示描述性前后变化。方向变化显示不可比较，同时调整多项显示“无法区分单项影响”，不声称因果、准确率或效果提升。
- 阶段三现已按新设计直接生成一份完整、可编辑、自动保存的定向简历：用户只选源简历与方向，系统自动选取最多 5 个可信完整 JD，并用只读账本说明改了什么、为什么改、依据和修改原则。原简历与系统生成基线始终冻结；启用当前全文会创建新简历版本，并与新策略轮次在同一事务中生效或一起回滚。
- 定向简历现在会在自动保存后即时检查当前全文：占位内容、姓名或联系方式丢失、生成基线异常以及无当前依据的新日期、数字或联系方式会阻止启用；改动过少、篇幅明显增加和用户继续编辑只做提醒。用户修改后的当前全文检查通过即可直接启用，不增加二次确认；历史已启用版本不重新判断或改写。
- 模拟面试现在默认进入“简历通用面试”：不需要岗位或面试邀请，只冻结当前启用简历、全局/经历范围内的有效回答与事实，并让每道题显示对应简历依据；追问仍必须引用用户刚才回答中的真实片段。原岗位上下文面试保留为可选“岗位专项面试”，才会使用完整 JD 与适用的岗位/公司口径。
- 真实本地简历首次生成的问题曾把简历中的“参与”问成“你负责的……”。提交 `af39901` 已把职责强度同时写入模型指令和问题保存契约：引用证据只写参与、协助或支持时，问题不能先假定用户负责、主导、牵头或独立完成；中性追问和确有强职责证据的提问仍可正常通过。
- Dashboard 已完成桌面端优先的统一改造：分组侧栏负责定位，首页负责每天多轮岗位发现和跨天反馈，岗位与消息页面突出当前需要处理的结果，求职体检突出结论和证据进度，简历与面试页面突出当前编辑或回答任务。内部运行细节、历史记录和修改依据仍可查看，但不再与用户主行动争夺视觉焦点。
- 2026-08-30 已按用户明确授权完成一轮真实全流程验收：69 个去重岗位、57 个完成分析、17 个进入并完成批量沟通，批次 #3 为 17/17 成功。后续只读消息发现和本地恢复复验读取 40 条可见会话，最终处理 8 组消息、保留 3 条城市身份不一致；6 组生成 12 份可编辑草稿，2 组因确实缺少候选人事实只提示补充信息。没有发送消息回复、简历同意/拒绝、投递或申请，因此“真实草稿生成”已验收，“真实回复发送”仍未验收。
- 专用 Edge 最小化或完全在后台不再是消息发现的阻塞条件。固定消息页和唯一临时详情页会在不聚焦、不恢复窗口的情况下恢复页面生命周期；详情页关闭后等待目标清单稳定，再核对精确基线，不会重新打开或重复访问。
- 真实验收暴露的三项本地问题已收口：确定性排除不再误计为“待补详情”；安全停止会取消正在运行或等待重试的模型请求；开启思考模式的岗位深度分析使用与长结构化输出匹配的有界预算，普通消息与非思考任务保持原预算。
- 岗位记录现在支持可逆归档、恢复、导出当前筛选结果和导出已选岗位。归档只整理用户的行动清单，不改求职状态、消息、岗位分析、漏斗样本或策略轮次；有未完成沟通或消息发送的岗位不能归档。CSV 由服务端重新核对当前候选人和方案，固定导出岗位字段并防止表格公式执行，不包含简历正文、候选人事实、HR 消息正文、回复草稿、密钥、Cookie 或诊断日志。
- HR 回复和无回复跟进草稿现在会参考最近 20 条已复制或已发送消息，遇到高度相似表达时最多自动改写一次；第二次仍相似只显示温和提醒，不阻止用户编辑或发送。草稿中的联系方式、薪资、到岗、面试时间、加班/出差/异地偏好及数值成果必须能由当前启用简历、当前候选人事实或适用范围内的有效用户回答支持，JD 不能证明候选人事实。数值依据按“表达的事实含义”匹配，不要求简历与草稿逐字相同；同时区分用户在向 HR 询问岗位情况与声称自己的经历，避免把普通问题误拦成个人成果。
- 未编辑模型草稿在创建发送批次前会按当前有效依据复查；若原先依据已经撤回，发送在本地冻结前停止且不创建批次。用户实际编辑后的当前文字就是权威确认输入，不增加二次确认；仅空白差异仍按未编辑处理。
- 已归档岗位在页面和服务端都保持只读：不再显示标记状态、沟通或追加进度入口，直接调用这些接口也会被拒绝；恢复后才重新允许操作。这样归档不会成为绕过既有工作流边界的第二条路径。
- 定向简历草稿从生成、自动保存到启用都绑定同一候选人和搜索方案。数据库迁移 v28 只为能唯一确定方案的旧草稿补齐绑定；存在多个可能方案的旧草稿保持未绑定并拒绝误用，不靠猜测归属。

## 当前门禁

v1.3.2 已合并、推送并公开发布，标签目标 `638d6699b85a8e5cdea3a66370320a061bfd48ce`。该精确 SHA 新鲜本地严格完整门禁 **144/144、退出码 0**，合并后 6 项关键回归通过，main CI run `33861738497` 和发布 run `33862701527` 成功；安装器公开摘要与 sidecar 已核验。后台沟通缺陷已修复，但仍不能断言它是此前真实 BOSS 中断的唯一原因，也没有自动处理历史未确认项。

当前入口是用户亲自执行两轮验收：当前账户旧资料已完整备份并改名隔离，先人工卸载旧版（保留资料）、把公开 v1.3.2 安装到固定的 `D:\Apps\RoleFlow`，从空白 RoleFlow 本地资料走第一轮；第二轮不重置、不重装，验证再次使用与记录恢复。BOSS 账号历史不变。没有自动安装或执行真实平台操作，两轮结果尚待用户反馈。完整回执见 `docs/superpowers/reports/2026-09-04-v1.3.2-release-acceptance.md`。

### v1.3.1 与修复候选门禁（保留当时状态）

v1.3.1 已按用户授权推送和公开发布，标签目标 `da3513cdee2f582c4ea21234f5a56e31d205db3b`，本地严格完整门禁 **143/143**，公开发布工作流 run `33841857321` 成功。22 个旧 stage 已按用户确认删除，约 3.60 GiB；不再重复清理。公开包下载与新用户模拟验收进度以 `docs/superpowers/reports/2026-09-04-v1.3.1-release-acceptance.md` 为准；模拟只用独立 D: 数据与示例简历，不接管真实 BOSS 批次。

公开包已下载校验，环境自检、新用户页面上传/确认/方案编辑和重启保留通过，另有 12/12 项公开包离线检查通过。该次模拟未发现流程阻塞；空平台薪资档的说明、消息页未连接浏览器的直接恢复指引仍是非阻塞改进。真实平台和全新 Windows 安装向导不在该次已验证范围，当前运行安装版未替换。

用户随后报告真实沟通中断，当前优先入口改为 `codex/communication-background-dispatch` 的后台焦点生命周期和脱敏诊断收口。双本地页证实旧焦点连接过早关闭，但尚未证明这是此次 BOSS 失败的唯一原因；诊断时批次的 1 项未确认与后续 7 项未点击均未由本次任务修改。候选实现 `14e2a887aec63ed1a8628e3f78a6a537f469df13` 已通过独立复审及新鲜严格完整门禁 **144/144、退出码 0**，不用上述 143 项旧结果替代。实现、验证和未验证边界见 `docs/superpowers/reports/2026-09-04-communication-background-dispatch.md`。下一步按新授权安排本地验收包和真实单条验证；当前未推送、合并、发布或安装，历史未确认项不自动重试。

### 历史集成门禁（不是当前待发布状态）

当前集成提交为 `ae9d1805f7e6f8ff9e3545e48e8f6f51e18e4104`，已快进进入本地 `main`；该精确 SHA 的严格 `npm test` 为 **143/143 通过、退出码 0**，13 项关键回归与独立复审通过。由同一提交构建的本地安装器位于 `D:\DevData\RoleFlow-acceptance-fixes-20260904\output\RoleFlow-Setup-1.3.0.exe`；2,621 个暂存文件哈希一致、禁入项 0、sidecar 一致、独立数据自检 `SELF_CHECK_OK`。没有推送或公开发布，尚未替换真实安装版。完整路径、哈希和边界见 `docs/superpowers/plans/2026-09-04-acceptance-fixes-integration.md`。

### 历史分支门禁（不能替代当前集成结果）

本轮筛选修复的实现提交为 `c7733c2`：对应代码树新鲜完整 `npm test` 为 143/143 通过（含严格本地 Edge 页面检查），精确提交的 7 项关键回归再次通过。独立复审无剩余 Critical/Important；候选仍保留本地，不代表已安装版更新。

2026-09-04 当前新增候选为 `codex/part-time-salary-policy`，范围仅为默认排除兼职及新方案薪资留空。标题/明确 JD/岗位时薪提供兼职证据，允许兼职不放开实习；未设置薪资时不再应用固定薪资阈值或面议排序惩罚，旧用户保存值与平台显式筛选范围保留。用户验收入口为“今日任务 → 调整筛选条件 → RoleFlow 本地精筛”。不得把本机缓存核对说成当前真实页面验收；未推送、合并、打包。实际验证记录见 `docs/superpowers/plans/2026-09-04-part-time-salary-policy.md`。

状态：**消息学习与确认发送、阶段二策略轮次求职体检、阶段三整份定向简历、阶段四简历通用模拟面试均已完成本地实现和离线验证；生产岗位判定继续使用二维表且该决策已冻结**。

2026-09-03 在隔离分支 `codex/first-use-startup-installer-polish` 完成 v1.3.0 人工验收暴露的首次启动与安装体验收口：`e963ffa` 补齐空关键词状态下的消息页/工作台，`d6e8dd2` 使用当前有效方案续接关键词并自动复检，`ddd811b` 始终显示安装位置选择。六项关键回归全部通过；在精确代码提交 `ddd811b` 上运行新鲜完整 `npm test`，退出码为 0，实际 142/142 项通过，末行为 `All 142 offline checks passed.`。本地性能测量使用 D 盘独立测试 profile 和空白页，没有访问真实 BOSS；候选尚未推送、合并、改版本号、打包或发布。

2026-09-03 在隔离集成分支完成首轮无回复跟进助手：只有主投/可投、已等待至少 48 小时并按周末顺延、最后仍是我方消息且没有新回复的岗位才进入列表；今日任务只在确有候选时显示紧凑入口。用户可以逐岗准备草稿、直接编辑自动保存，并复用现有单条/批量确认发送链路。发送成功记录为幂等的 `follow_up_sent`，继续归属于原漏斗样本和策略轮次，不新建投递样本。跟进草稿不会混入 HR 新消息页面。提交 `07d3d8c`、`1c9f19c` 对应代码通过新鲜完整 `npm test`，实际为 136/136 项，原始末行为 `All 136 offline checks passed.`；全程只使用 fixture、假浏览器和临时数据库，没有访问或写入真实 BOSS。

2026-09-03 同一隔离集成分支提交 `4c80d2f`、`2f8b004`、`951569b`、`210178c` 完成岗位归档、恢复与 CSV 导出。默认岗位记录和所有行动清单隐藏已归档岗位，但求职体检继续保留其原始样本；用户可以单独查看并恢复归档岗位。当前筛选和已选导出均由服务端按当前候选人、求职方案和岗位编号重新读取，拒绝跨用户、未知、重复或超过 500 个岗位的选择。对 `210178c` 运行新鲜完整 `npm test`，实际为 140/140 项，原始末行为 `All 140 offline checks passed.`；全程只使用 fixture、假浏览器和临时数据库，没有访问真实 BOSS，也没有执行外部写。

2026-09-03 同一隔离集成分支提交 `2cec6a8`、`772deb4`、`4ba82c2`、`b7cd57d`、`11d9a10` 完成消息草稿质量收口：本地检测近期重复表达和高风险候选人事实，模型最多修正一次；消息发现与跟进只保存通过事实检查的模型文字，相似提醒不禁用发送；未编辑模型草稿在发送冻结前按当前事实再次检查，用户编辑仍直接生效。对 `11d9a10` 运行新鲜完整 `npm test`，退出码为 0，实际为 142/142 项，原始末行为 `All 142 offline checks passed.`；Playwright 视觉项因当前 `NODE_PATH` 未提供 Playwright 按既有规则跳过，纯渲染、HTTP、假浏览器和临时数据库检查均执行通过。全程没有访问真实 BOSS、调用真实消息模型或执行外部写。

2026-09-03 同一隔离集成分支提交 `bf8dc0e`、`f5ad9ca`、`c359958` 完成定向简历启用完整性：校验只使用冻结源简历、系统生成基线、当前候选人事实和适用范围内的有效用户回答，岗位 JD 与体检结论不能证明个人经历；页面把错误和提醒翻译为可直接处理的中文，启用失败保留全文并恢复按钮，成功仍直接创建新简历版本和新策略轮次。首轮全量门禁还暴露后台简历分析与 Dashboard 并发写入时的 SQLite 快照升级竞争，提交 `773b286` 让资料保存事务在读取前排队取得写权限并补契约回归。对 `773b286` 运行新鲜完整 `npm test`，退出码为 0，实际为 142/142 项，原始末行为 `All 142 offline checks passed.`；Playwright 视觉项按既有规则跳过，其余纯渲染、HTTP、fixture、假浏览器和临时数据库检查均执行通过。没有访问真实 BOSS、使用用户真实简历、调用真实消息模型或执行外部写；没有改写历史已启用简历。

随后独立复审发现并修复三类跨边界问题：定向简历草稿可能跨搜索方案保存或启用、已归档岗位仍可从旧入口追加操作、数值事实检查过度依赖字面相同。收口提交从 `fb3b693` 到 `4d915e7`，包含迁移 v28、前后端双层方案绑定、归档只读闸门，以及区分“询问岗位”和“陈述个人经历”的语义数值校验。独立复审在精确代码提交 `4d915e79c595dc604c6f3fe533fb2aab612fcbe7` 上确认 Critical 0、Important 0；新鲜完整 `npm test` 退出码为 0，实际 142/142 项通过，原始末行为 `All 142 offline checks passed.`。唯一非阻塞边角是把个人成果与“应该如何表述”连成一句、又不带明确履历标记的低频写作咨询，可能被当作普通提问；常见 HR 问答和履历陈述路径均已有回归覆盖。本轮仍只使用 fixture、假浏览器、纯渲染和临时数据库，没有访问真实 BOSS、真实简历或执行外部写。

2026-09-03 已将该集成分支快进合并到 `main` 并正式发布 v1.3.0。首次打包内置门禁在全套负载下发现标签并发检查的低频迟到竞争，发布当即停止；`8350c34` 用可控延迟回归稳定复现并修复，同一聚焦检查连续 30 次通过。修复后打包入口和精确标签提交 `72a14d9` 的完整门禁均为 142/142；`Publish release #10`（run `33709848604`）成功，Release 为最新版、非草稿且非预发布，三个公开资产齐全。公开安装器已下载到 `D:\DevData\RoleFlow-release-v1.3.0\published\run-33709848604`，SHA-256 `77173e56ffdf5a8e60fc8bb57b03c3dab454acf968ceb8f9aecbfd64a2318efb` 与公开 sidecar 一致。真实人工验收由用户安装该公开包后独立执行。

2026-09-02 已完成真实验收暴露的“暂停后修改 BOSS 筛选再继续”修复：暂停请求会在下一安全浏览器边界生效，岗位卡片不再越过冻结上限；运行中条件漂移不会把新卡片混进旧批次。暂停或可恢复中断后可在同一逻辑轮次选择按当前新条件重开，旧批次保留但解绑；新关键词会真正进入新批次，选择页之后条件再次变化则只刷新选择、不写入或启动。工作流错误先展示原因、影响和下一步，技术编号折叠保留。v1.2.2 已在精确标签提交 `c2e6c02` 上完成云端完整测试、便携包校验、安装器构建和公开发布；Release 为最新版、非草稿且非预发布，三个公开资产齐全，安装器哈希与公开 sidecar 一致。没有访问真实 BOSS 或执行平台外部写。

2026-08-29 对阶段三原子启用提交 `eae13f4` 运行新鲜完整 `npm test`，退出码为 0，实际为 130/130 项通过，原始末行为 `All 130 offline checks passed.`；8 项阶段三聚焦门禁全部通过。定向简历页另用本地假数据和 headless Edge 完成 1440px、390px 视觉检查，无横向溢出、控制台错误或不可编辑问题，截图位于 `D:\DevData\roleflow-ui-check\resume-optimization-*.png`。本轮没有访问真实 BOSS 或用户真实简历，也没有推送、合并、打包、改版本号或创建 Release。旧阶段四在 `b813b7a` 上的历史证据继续保留，但不能冒充已批准新设计的完成证据。

2026-08-29 对阶段四界面提交 `da3e33d` 之后的代码运行 7 项聚焦门禁和新鲜完整 `npm test`，均通过；完整门禁退出码为 0，实际为 130/130 项通过，原始末行为 `All 130 offline checks passed.`。模拟面试页另用本地假数据和本机 headless Edge 完成 1440px、390px 检查：默认通用模式、专项模式切换与岗位必填联动、简历证据显示均正常，无横向溢出或控制台错误，截图位于 `D:\DevData\roleflow-ui-check\interview-*.png`。没有访问真实 BOSS、用户真实简历或真实面试数据，也没有推送、合并、打包、改版本号或创建 Release。

2026-08-29 完成阶段一至阶段四的用户旅程收口：消息页面刷新采用 SQLite 当前开放草稿；定向简历与模拟面试的模型失败留在当前页面并保留输入；面试未提交回答可刷新恢复；简历生成/启用、面试问题/复盘/重答会刷新当前内容并定位结果；求职体检明确“至少 48 小时；周末顺延”并阻止空策略边界。对提交 `61c5520` 对应代码树运行新鲜完整 `npm test`，退出码为 0，实际为 130/130 项通过，原始末行为 `All 130 offline checks passed.`。本地假数据 headless Edge 验收覆盖 1440×980、375×812 和面试 844×390 横屏，无横向溢出、控制台错误、页面异常或过小移动端操作控件；没有访问真实 BOSS、用户真实简历或真实面试数据。

2026-08-29 开始用本机已有真实简历做阶段四首轮本地验收。项目按正常打开数据库路径把本机数据从 schema v16 迁移到 v25，迁移前备份为 `C:\Users\Administrator\AppData\Local\RoleFlow\Data\data\backups\jobs-before-v16-to-v25-2026-08-29T14-47-25-233Z-9576.sqlite`；`quick_check` 为 `ok`，一份简历文档和一个启用版本的数量及正文长度保持不变。首个问题暴露职责夸大后，以 `af39901` 修复并重新生成第二个会话；新问题保持“参与”的原始边界，再中性询问实际承担的编码、调试与问题处理。页面复验为 1440×980、无横向溢出、控制台错误或页面异常，答案框为空且系统没有替用户作答。4 项面试聚焦检查和新鲜完整 `npm test` 均通过，完整门禁退出码为 0，实际为 130/130 项通过，原始末行为 `All 130 offline checks passed.`。没有访问真实 BOSS，也没有把真实简历正文写入仓库、测试或文档。

2026-08-30 对 Dashboard 桌面端整体改造代码提交 `cd933706e139ed08d8184108bd7000c14e3f434c` 运行严格本地离线浏览器验收：15 个页面状态 × 4 个视口，共 60/60；横向溢出、控制台错误、页面异常、请求失败、外部请求、无标签控件和标题层级错误均为 0，清单位于 `D:\DevData\RoleFlow-ui-redesign-acceptance\desktop-redesign-cd93370.json`。同一精确提交的新鲜 `npm test` 退出码为 0，实际为 131/131 项通过，原始末行为 `All 131 offline checks passed.`。改造没有新增框架、组件库、运行时依赖、数据库迁移或业务接口；没有访问真实 BOSS、真实简历或外部服务，也没有推送、合并、打包、改版本号或创建 Release。

2026-08-30 真实用户全流程验收记录位于 `docs/superpowers/reports/2026-08-30-real-user-e2e-acceptance.md`。本轮确认“12 个待补详情”中只有 4 个真正缺详情；其余 8 个已经具备卡片级排除证据（城市不符 4、实习 2、硬性排除方向 2），只是任务归类顺序把它们误标成信息不足。提交 `66d89d4` 已完成三项本地收口：迁移 26 清理历史误分类且新任务按同一规则归类；消息发现的取消信号贯穿岗位分析、模型请求与重试等待，取消后不写缓存或完成结果；开启思考模式的 `understandJob`、`matchResponsibilities`、`matchRequirements` 首轮使用 8192 输出额度和最多 240 秒，截断重试使用 16384 和最多 300 秒。形成最终提交 `c7695f5` 的代码通过 131/131 完整离线门禁。随后正式运行库已在后台重启中从 schema 25 迁移到 26，数据库完整性为 `ok`；同一 69 个任务的 `DETAIL_REQUIRED` 从 12 变为 4，8 个误分类归零。真实本地页面显示“已读 57、待补 4、无需详情 8”和“完成 69、当前未解决 0、本地规则处理 8”，无横向溢出、控制台错误、失败请求或外部请求；证据位于 `D:\DevData\RoleFlow-verification\v26-workflow-ui-evidence.json`。

同日最终消息草稿复验又修复三处真实运行问题：每次页面快照重新安装当前 helper，避免 Edge 跨服务重启保留旧实现；队列完整保留 `friendKey`，让后台会话选择继续执行严格身份核对；模型提示与运行时契约共同约束 `missingFact`、`responseItems/coverage` 和普通意向回复的结构，严格事实边界没有放宽。代码与回归提交为 `b8e6ccc`。失败分类在整库备份后按精确 25 条事件和 8 个基线事务撤销，再次只读运行得到 8 组结果：12 份开放草稿、2 个缺失事实提示、3 条城市不匹配待解决、0 发送批次，`quick_check=ok`。消息页实际显示 8 个结果、12 个可编辑自动保存草稿和批量发送面板；新鲜完整 `npm test` 为 131/131 通过。

## 本轮已完成记录

- `34fdea0`：专用 Edge、固定页和登录等待的单飞自愈。
- `4961db2`：完整 JD 后的岗位资格判断与沟通清单拦截。
- `995ad3e`：专用 Edge 原生沟通网络观察、点击前能力预检和安全顺序收敛。
- `e1396a1`：允许最小化专用 Edge 在不抢前台的情况下补齐后台标签页，并让升级等待正在结束的启动过程。
- `508781d`：统一网页兼容字形，修复 JD 明写实习周期但使用 `⽉` 时的漏筛。
- `157ed8f`：复用现有一次深度分析调用，独立输出招聘方沟通意图与消息话题。
- `50e094e`：只把安全意图枚举写入流程事件并返回页面，模型结构异常降级为人工判断。
- `ca58194`：消息页按“结论 → 这份机会 → 岗位/公司/匹配/薪资 → 下一步草稿”展示，不向用户显示内部枚举。
- `8900ee4`：读取同一方案下最新岗位分析，区分历史失败和当前是否已解决。
- `de4c102`：流程页与本地轮询统一显示直接完成、失败后已解决和当前未解决。
- `5138a36`：存储级回归证明失败尝试仍保留、重试成功不会触发当前分析未完成。
- `b7ead39`：岗位主卡改为“结论 → 岗位 → 公司与机会 → 匹配 → 薪资 → 需要确认”。
- `c88564c`：新增职责 40% + 要求 60% 的独立连续积分影子及不可抵消的安全保护。
- `76a729c`：新增二维表与积分离线比较报告，并纳入第 108 项完整离线门禁。
- `d1d6cbf`：按真实 DOM 完整签名区分平台竞争卡、纯文字、简历请求和未知卡片。
- `8f92287`：读取器只放行两个已验证的结构化消息类型。
- `8468b6c`：模型只接收纯文字；简历请求产生本地人工待办，混合组同时保留草稿并完整去重。
- `78bcdab`：页面先显示 BOSS 人工操作，再显示本地回复草稿；任意网页文案和未批准类型被过滤。
- `a5031ff`：新增本地草稿、用户回答记忆和候选人事实修订历史，保留可撤销来源。
- `475f609`：把复制/已手动发送作为学习完成动作，模型提取失败时仍保存完整用户回答。
- `37c766e`：复用现有模型适配器从用户改动中提取有限事实，后续生成只接收启用的用户修改回答。
- `bb3905b`：消息发现持久化回复草稿，重启后可恢复，公开状态不返回草稿正文。
- `d2f91e4`：草稿可编辑、自动保存、复制后学习，并新增“我的沟通资料”管理页。
- `a80a221`：串行处理自动保存与复制完成，已发送草稿拒绝迟到保存，避免旧文字覆盖最终回答。
- `77bccaf`：完成消息学习独立复审修复，补齐安全摘要、否定事实、作用域与运行中撤回、复制/已发送竞态、单一已发送路径与事务回滚，以及 digest 和事实权威顺序兼容回归。
- `2721bf8`、`c9790fc`、`998d4dc`：建立可见会话的 HR/已读/送达分类、迁移 22 的本地上下文与不可变批次，并在卡片显示 HR 内容和岗位分析。
- `8a8a9b9`、`e9baa48`、`9c909b9`、`01ef19d`：建立单条/批量确认、固定后台消息页守卫、串行执行和消息卡片交互；点击确认冻结当时目标与文字，不二次确认。
- `2ffc87e`、`15821f5`：关闭同会话替代草稿双发、旧批次重启调和、编辑器清理归属，以及聚焦/回读期间会话漂移的竞态；不明确结果不重试。
- `2826780`、`c3fd183`：记录阶段二实施计划、复用调研和只读校准边界；现有工具无法在不切换前台的前提下取得新的真实页面证据，因此没有访问真实 BOSS，也没有用猜测补字段。
- `f846c83`、`45e43fe`：建立 48 小时/周末成熟规则，并把默认结论强度修正为可配置的 30/50/70。
- `f3b395b`、`a2724e6`、`5be3160`：新增迁移 18、滚动批次存储、真实求职动作入池和安全结果观察；只保存本地标识、摘要、类别、来源和时间，不保存 HR 原文。
- `7306c03`、`465f407`：建立无因果夸大的诊断服务和独立“求职体检”页面；桌面与 390px 手机宽度本地视觉验收无横向溢出、无控制台错误。
- `7510ab5`：完成五轮独立复审修复，补齐最新已读独立等待、条件分母、用户纠正、滚动批次切换、历史策略、跨候选人隔离、终止状态、原子入池和比较证据门槛。
- `efa2c53`：新增迁移 23、策略轮次存储、旧滚动批次回填和 30/50/70 轮次快照。
- `874a8fc`：让每条新漏斗记录永久绑定当时的策略轮次，重复动作和迟到结果不重新归类。
- `ee0d12a`：当前诊断只读当前轮，并按证据量、方向一致性和单项/多项变更规则比较上一轮。
- `2d7708e`：加入“当前策略轮次”、前后对照和本地手动边界页面；桌面与手机视觉检查通过。
- `2b5c3fc`：把六类修改原则、证据引用、数字与职责边界校验纳入定向简历契约，并让通过校验的修改自动合成完整草稿。
- `5088126`：新增迁移 24，分开冻结源简历、系统生成全文、用户当前全文与只读修改账本；旧逐条建议记录继续只读兼容。
- `0540f9f`：用户只选方向，系统按方向、推荐级别、新鲜度和公司去重自动选择 3–5 个代表岗位，全部校验通过后才创建草稿。
- `3a44e36`：页面改为完整全文编辑器、600 毫秒串行自动保存、复制当前全文和只读修改依据；启用提交点击当下的最新文字。
- `eae13f4`：启用新简历版本与开启 `resume` 策略轮次进入同一 SQLite 事务；失败整体回滚，同一全文重试保持幂等。
- `2bbced4`、`c24dd71`、`411ef06`、`d6c4394`：建立有证据约束的定向简历契约、迁移 19、本地草稿/新版本存储、生成服务和独立工作区；`b9609f2`、`1fbf5fd` 完成契约数量与阶段三验收记录。
- `6ce3f35`、`c9943a0`、`bd633fd`、`899b929`：建立文字模拟面试契约、会话/逐题/重答存储、动态追问服务和独立工作区；`24320d3`、`6aedc8b` 补齐重放、方案隔离、启用简历、真实回答片段、原子保存和计划题数边界。
- `b9f5557`：保留原迁移 20 并新增迁移 21；旧 v20 会话只有在唯一可证明归属时才恢复计划绑定，歧义会话不猜归属。`b813b7a`：在 Dashboard 生命周期复用同模型配置下的面试服务，两条并发完成请求只生成一次复盘。
- `81e277e`：新增迁移 25，区分 `resume_general` 与 `job_specific`，允许通用会话不绑定岗位；旧会话按岗位专项完整迁移，并保留会话、逐题、复盘和重答。
- `11968aa`：为冻结简历建立稳定的逐行证据目录；每道新问题必须引用 1–4 条真实简历证据，后续追问还必须逐字承接上一回答片段。
- `a001718`：默认构建不含岗位的通用面试上下文，只带入全局/经历范围的有效回答、事实和既往通用弱点；岗位/公司专用内容不会串入。旧进行中会话可在内存中补出证据目录，不改写冻结记录。
- `da3e33d`：Dashboard 默认显示“简历通用面试”，岗位专项作为可选模式；当前问题和逐题记录显示“这道题来自简历”的依据，桌面与手机本地视觉检查通过。
- `af39901`：问题生成和保存契约共同保留简历职责强度，防止把“参与/协助/支持”预设成“负责/主导”，并保留中性追问和强证据正常路径。
- `27e6b26`、`98a6c16`：异步工作流失败由 Dashboard 接住；消息页刷新使用持久化草稿，关闭草稿不再被旧运行快照恢复。
- `109fdf1`、`5f6ef43`、`40fc135`：面试草稿刷新恢复、简历失败留页与成功定位、求职体检成熟文案和策略边界校验。
- `61c5520`：同页工作流成功后刷新服务器最新内容并把当前步骤或结果滚入视口；消息发送测试补齐逐条保存状态契约。
- v1.1.0 预发布 `StageOnly` 已在 `D:\DevData\RoleFlow-installer\release-8b050b9d7abf\stage\1.1.0` 完成：3,144 个条目、2,586 个文件、175,268,212 字节；没有发现 profile、测试、SQLite、Key、`.env`、secrets、运行目录、报告/日志或 Edge Control bridge，伪造 Edge 创建模式为 0。
- `8b050b9d7abf702be02083aab29d35fa5fa13c89` 的 `npm test` 退出码为 0，108/108 项通过；聚焦消息回归、`git diff --check` 和干净工作树检查也通过。
- v1.1.0 预发布安装包：`D:\DevData\RoleFlow-installer\release-8b050b9d7abf\output\RoleFlow-Setup-1.1.0.exe`，大小 39,891,075 字节，SHA-256 `fea7bbef5bb7dcc92812c19bf54b3a0d89743d64a2763583eaa440fd425e338e`。它只证明构建链路，正式 Release 使用最终标签提交重新构建的资产。
- 消息页模拟人工验收通过：结论和岗位信息优先，简历人工操作位于草稿之前，没有同意/拒绝按钮，草稿复制成功，外部请求为 0；截图为 `D:\DevData\RoleFlow-acceptance\v1.1.0-message-discovery.png`。没有执行 v1.1.0 的真实覆盖安装或启动后 BOSS 验收。
- 最新真实只读运行 `0b747bea-3332-41f0-ad3d-36fa257cc3c4` 已停在 `review_required`：3/3 搜索目标、35 个岗位、28/28 份所需 JD、待语义分析 0；隐藏实习样本被硬拦截，沟通批次为 0，未出现浏览器超时或风险控制。

## 下一项开发入口

2026-09-04 的集成与后台沟通修复已进入正式 v1.3.2，发布授权已执行。下一步由用户使用公开安装器，按上述空白资料首次使用、同资料再次使用的顺序人工验收。不再交付旧版本号的本地临时包，也不接管真实沟通。旧资料完整保留；若以后恢复旧资料，其中已保存的薪资不会自动清除，可由用户手工调整。

Dashboard 桌面端改造、无回复跟进、岗位归档/导出、消息草稿质量和简历启用完整性，以及其后的首次启动、关键词续接、安装目录、结束本轮、产品图标、兼职/空薪资和沟通恢复修复均在当前正式版本中。旧章节中的“候选未合并”仅保留为当时记录，不代表当前待办。不重新设计已冻结的岗位判定路线。其余需要外部条件或用户参与的入口如下：

本次发布已完成精确版本门禁、构建和安装器哈希核对；真实首次启动、安装位置页面与两轮实际使用仍由用户验证。任何新安装都不自动继承此前沟通或投递授权。

1. **完成首轮真实简历通用训练**：本机真实简历已经生成职责边界正确的第二个会话，下一步由用户亲自回答并完成至少一轮，再依据实际追问、复盘和重练体验决定是否调整；当前只验证了首题质量，不能据此声称整轮面试效果已验证。
2. **岗位专项面试的可选扩展**：只有用户收到真实面试邀请且再次确认范围后，再考虑公司/行业资料、外部题库或更深的 JD 研究；当前版本不含公司研究、外部题库、语音或真实平台访问。
3. **真实确认发送验收**：真实消息草稿生成、编辑入口、自动保存和批量发送面板已经验收，但没有点击发送。后续由用户检查当前草稿；只有用户对当次不可变草稿明确授权时，才验收一次真实回复发送，历史批次授权不能复用。
4. **消息卡片扩展**：只有拿到新的真实只读 DOM 证据后，才分别实现位置确认或面试排期；不能复用简历卡选择器或靠文字猜类型。

## 持续安全边界

当前 v1.3.0 已正式发布；真实 BOSS 操作仍是独立授权边界。没有任何真实 BOSS 外部写授权。继续遵守：

- 真实浏览器检查保持串行、低频、后台和失败即停。
- 任何外部写仅限用户确认的不可变批次；不自动重试歧义结果。
- 主控不得自动恢复或重放旧中断批次。用户可在产品既有恢复入口核对后续接尚未执行的清单；结果不明确项仍先人工核对，不能重复发送。任何由主控执行的真实沟通验收，都需针对当前不可变目标取得当次明确授权。
- 筛选质量验收前建立新的空 operational baseline，保留画像、简历、搜索方案和模型设置；旧岗位历史只作恢复证据。
- 1.1.0 的 `StageOnly` 目录不是可发布安装包；Release 只能上传最终 SHA 构建且哈希已核对的 `.exe` 和 `.sha256`。
- 不以减少 JD 覆盖、召回或推荐质量换取速度。
- Edge Control 仍只属于显式高级路径，不进入普通交付包。
